**Owner:** Head of Specs Team
**Status:** Active

# Change Log — Post-Ship Closure Engine

This file contains the historical change log for `claude/system/post_ship_closure.md`.
The prompt itself contains only the current version — full history is here.

---

| Version | Date | Change |
|---------|------|--------|
| 2.28 | 2026-08-17 | ST-29 (BLG-GOV-293, EPIC-07, v8.8): STEP 10 Global State Update is now the sole authoritative, unconditional writer of `.claude_current_state.json.prior_cycle`, writing this run's own `<cycle_id>` — the field was previously written by no engine at all (found stale at `2026-08-08__release-v8.5` release planning). Placed here rather than in `release_planning_prompt.md` STEP 9 (which already owns the analogous `next_release`/OA-1 fix) because by STEP 9, `active_cycle` has already been advanced to the new cycle, losing the old value; Post-Ship Closure's `<cycle_id>` unambiguously identifies the cycle that just closed at the exact moment of closure. **Cross-branch version-collision resolved at merge (CLAUDE.md §8 step 2a):** originally cut as v2.27 on the `EPIC-07` branch, independently and in parallel with `EPIC-03`'s ST-13 (row below), which was also cut as v2.27 and merged to `main` first (PR #1424) — a real test merge confirmed the identical version-line text would have auto-merged silently with no conflict marker, so this was caught only via PR #1428's Director of Quality dual-role review, not by `git merge` itself. Renumbered here to the next free slot (v2.28) rather than let two different changes share one version number. Authority: Head of Specs Team (agent-mediated sign-off, Sprint Execution Engine, ST-29, 2026-08-17). |
| 2.27 | 2026-08-16 | ST-13 (BLG-FE-161, EPIC-03, v8.8): STEP 1.1 `Changes shipped` entry template gains a `User Impact` column (3rd cell, between `Description` and `Spec sections updated`) and STEP 1.2 gains a new rule requiring it be populated at authoring time — curated user-benefit copy for EPICs with a user-facing effect, `—` otherwise. `GET /changelog/latest` (`docs/specs/api_contracts/changelog_endpoints.md` v1.1) now sources the in-app "What's New" panel from this column exclusively, excluding blank/`—` rows; a changelog entry written without it populated leaves the panel empty for that whole release. Design source: `docs/design/2026-08-14__release-v8.8/whats-new-user-benefit-copy/decision_record.md`. Authority: Sprint Execution Engine (ST-13, `2026-08-14__release-v8.8` EPIC-03, 2026-08-16). |
| 2.26 | 2026-08-10 | STEP 7.3 TSG backlog reconciliation — replaced the hardcoded "§27 (Technical Specification Gaps)" reference (stale since `Specs_Index.md`'s Test Coverage Gap register is append-only/chronologically-numbered and had long since renumbered past §27) with an instruction to scan the full document for `**Status:** Open` fields on `TSG-*`-prefixed entries. Self-confirmed live during `2026-08-08__release-v8.5` closure's own STEP 7 run (the document's actual §27 is "Test Coverage Gaps — v5.0"; the one genuinely Open TSG entry, `TSG-v33-03`, sits at §19.3). Closes a deferred patch carried forward without a `prompt_change_log.md` entry for 2 consecutive cycles (`2026-08-07__release-v8.4` closure carry-forward #2), applied as an immediate action per STEP 8's non-deferrable rule rather than deferred a 3rd time. Authority: Head of Specs Team (post-ship closure `2026-08-08__release-v8.5`, STEP 8 immediate-action rule). |
| 2.25 | 2026-08-08 | AUD-2026-08-03-001 / AUD-2026-08-08-003 — §5 Write Scope Restriction gains a state-pointer sync rule for `closure_escalations.md`. Whenever an entry is appended to `closure_escalations.md`, the same STEP 8 write must also set `.claude_current_state.json.open_escalations.<ESC-ID> = {"summary": ..., "owner": ..., "sla_due_utc": ...}`, clearing the key when the escalation's `Disposition` moves to `Resolved`. Closes a governance-integrity gap first flagged at lifecycle audit `AUD-2026-08-03`: `.claude_current_state.json.open_escalations` is checked as a gate precondition by `release_planning_prompt.md`, `sprint_planning_prompt.md`, and `roadmap_prompt.md`, but no engine ever wrote it — only the separate, per-cycle `execution_state.json.open_escalations` was maintained. Confirmed still unpatched at `AUD-2026-08-08` (2nd consecutive audit finding this open) before being applied here. Authority: Head of Specs Team (lifecycle audit AUD-2026-08-08, resolved 2026-08-08). |
| 2.24 | 2026-08-04 | ST-11 (BLG-GOV-218, EPIC-03, v8.2): STEP 0 Rebalance Cadence Check strengthened — before emitting the unconditional "✅ REBALANCE SKIP" advisory for an odd `completed_cycle_count`, now reads `claude/roadmap/current_roadmap.md` §1's "Next planned release" line. If `next_release` is `[TBD]`/unscoped, or the roadmap's matching version already carries a non-`[TBD]` Status (already scoped/committed/complete — release planning already ran or is running for it), a corrected "⚠ REBALANCE SKIP WITHHELD" advisory is recorded instead, since "proceed directly to `plan release v<next_release>`" would be stale or not-yet-actionable advice. A genuinely fresh, unconsumed `next_release` still gets the standard skip advisory unchanged (no regression). Authority: Head of Specs Team (Sprint Execution Engine, agent-mediated, ST-11, 2026-08-04). |
| 2.23 | 2026-08-03 | ST-12 (BLG-QA-129, EPIC-04, v8.1): new STEP 5.1 Cross-Cycle Deviation Consolidation Review — periodic (every 3rd Post-Ship Closure invocation) consolidation of `DEV-*` records across recent cycles, producing `docs/governance/deviation_consolidation_review_<date>.md` with Director of Quality sign-off. First run (2026-08-03) catalogued 9 deviation records and found/fixed a real resolution-status drift between `docs/testing/slippage_scenarios.md` and `docs/specs/frontend/pages/trade_history.md`'s sibling `DEV-ST14-01` entries. Authority: Director of Quality (Sprint Execution Engine, agent-mediated, ST-12, 2026-08-03). |
| 2.22 | 2026-08-03 | AUD-2026-08-03-003 — STEP 6 Endpoint Coverage Drift Check: added a "Script-derived tracking-item handoff" rule. When the existing stale-tracking-item delta rule (v2.21) fires, also emit the fully re-derived current-gap endpoint list into the closure record's Advisory Summary in copy-paste-ready form, and instruct the next engine actioning the item (or the next `groom backlog` review) to apply that list verbatim rather than re-deriving the diff each time. Closes a 3-consecutive-cycle recurring drift pattern (`BLG-OPS-111`, v7.9→v7.10→v8.0) caused by the tracking item's list being manually maintained and only ever delta-noted, never corrected — `BLG-OPS-111` itself retired as superseded by this fix (see `backlog_archive.md`). Authority: Head of Specs Team (lifecycle audit AUD-2026-08-03, resolved 2026-08-03). |
| 2.21 | 2026-07-28 | (Backfilled — this row was missing from this changelog despite the header already carrying v2.21; restored per CLAUDE.md §6 drift check.) Post-ship closure `2026-07-27__release-v7.9` STEP 6 self-discovered friction, immediate action: when an existing open `BLG-OPS-*` endpoint-coverage-drift tracking item is referenced instead of filing a duplicate, and the current normalised gap has grown beyond that item's own recorded list, the routine must record the delta explicitly in the closure record and Advisory Summary (rather than silently letting the tracking item understate the true gap). Found this run: `BLG-OPS-111` (filed at v7.2, 21 endpoints) now understates the gap by 4 (25 found this run, including 1 new from this cycle's own `PATCH /watchlist/{entry_id}`). Authority: Head of Specs Team (post-ship closure `2026-07-27__release-v7.9`, STEP 8 — immediate lessons-learnt action rule). |
| 2.20 | 2026-07-27 | Post-ship closure `2026-07-24__release-v7.8` STEP 6 self-discovered friction, immediate action: Endpoint Coverage Drift Check advisory strengthened. Added an explicit path-parameter-normalisation instruction before diffing `openapi.yaml` against `api_performance_baseline.md` (the two documents use different placeholder names for the same parameter, e.g. `{position_id}` vs generic `{id}`, producing false-positive gaps under a naive literal diff) and a check for an existing open `BLG-OPS-*` tracking item covering the same gap class before filing a new one — this run's normalised gap turned out to be identical, pre-existing drift already tracked by the still-open `BLG-OPS-111` (filed `2026-07-15__release-v7.2`). Authority: Head of Specs Team (post-ship closure `2026-07-24__release-v7.8`, STEP 8 — immediate lessons-learnt action rule). |
| 2.19 | 2026-07-26 | ST-02 (EPIC-02, v7.8, BLG-FEAT-84): new STEP 1.5 (Telegram Changelog Digest) added between STEP 1 (Changelog Entry) and STEP 2 (Roadmap Update) — invokes `scripts/send_changelog_digest.py`, which sends the release's `### Changes shipped` entries via the existing Telegram notification infrastructure (POST+JSON+retry, shipped v2.4/v5.1 for the SI-05 weekly digest, `backend/services/si05_digest_service.py`). Hard rule: a failed send never blocks closure — `send_changelog_digest()` never raises; STEP 2 proceeds regardless of send outcome. New batch checkpoint 1.5 records the step as attempted (not delivery-conditional). Authority: Sprint Execution Engine (v7.8 sprint execution, ST-02). |
| 2.18 | 2026-07-20 | Post-ship closure `2026-07-20__release-v7.6` Phase 4 friction, immediate action (LL-v7.6-P4-01): STEP 10 (Global State Update) — new amendment field reset rule added. If `active_amendment` is non-empty and its originating cycle has already reached `Closed`/`Closed_with_actions`, clear `amended_backlog_slice_path`, `amendment_sealed_utc`, `active_amendment`, and `amendment_status` in the same write. Root cause: `.claude_current_state.json`'s `amended_backlog_slice_path` still pointed to the already-closed `2026-07-17__release-v7.4`/`AMD-20260717-01` amendment when this cycle's own delivery verification ran, requiring manual cross-referencing to dismiss as inapplicable (`lessons_learnt_cycle.md` `2026-07-20__release-v7.6` §Phase 4). Authority: Head of Specs Team (post-ship closure `2026-07-20__release-v7.6`, STEP 8 — immediate lessons-learnt action rule). |
*(Note: v2.14–v2.17 entries not backfilled here — those versions predate the 2026-07-17 companion-per-file-changelog rule, `shared_standards.md` v3.17 §11; see `prompt_change_log.md` for their history.)*
| 2.13 | 2026-06-03 | BLG-GOV-82 (v5.0 ST-05): STEP 0 Audit Cadence Check strengthened to dual-condition — fires if `completed_cycle_count % 3 == 0` OR `(completed_cycle_count - last_audit_cycle_count) >= 4` (null-safe: gap check skipped when null); STEP 10 global state update — `last_audit_cycle_count` write rule added: if audit ran this cycle, set to new completed_cycle_count, else leave unchanged. Resolves BLG-GOV-82 (AUD-2026-06-02 AUD-005). |
| 2.12 | 2026-05-28 | Branch Safety Check (Hard Gate) added at top of STEP -1 preflight: `git branch --show-current` must return `main`; halt with checkout instructions if not. Prevents post-ship artefacts from landing on exec branches (root cause of v4.1 governance commit mislanding). |
| 2.11 | 2026-05-27 | STEP 0 — Rebalance Cadence Check added (advisory, non-blocking): if `completed_cycle_count % 2 == 0` emit "⚠ REBALANCE DUE"; if odd emit "✅ REBALANCE SKIP". Advisory Summary Block updated. |
| 2.10 | 2026-05-21 | §5 Write Scope, §6 Required Authority Roles, §9 Governance Invariants: replaced verbose inline blocks with preamble references (governance_preamble.md). Compression pass — no logic change. |
| 2.9 | 2026-05-17 | Governance pattern consolidation. STEP -1 preflight: -1.4 (Required Files Present), -1.5 (Required Authority Roles Exist), and -1.6 (Write Permission Test) replaced with single combined reference to `shared/preflight_common.md` (all three sub-checks); --dry-run skip behaviour for the write test preserved via write_test_path parameter marker. Engine-specific checks -1.1 through -1.3 unchanged. Authority: Head of Specs Team (governance pattern consolidation 2026-05-17). |
| 2.7 | 2026-05-15 | Token efficiency + speed + output compression pass. (1) Parallel read instruction added to STEP -1 preflight. (2) All 5 STEP 0 context reads marked "read in parallel". (3) closure_state.json schema replaced with compact prose description. (4) Read targets added to STEPs 2, 3, 6, 7 (scoped section reads, not full documents). (5) STEP 8.5 and STEPs 11/12 scoped to relevant sections only. (6) 13 per-step closure_state.json writes consolidated to 3 batch checkpoints (after STEP 1, after STEP 8.5, after STEP 13). (7) §5 prohibited list compressed — sealed-file list preserved, prose rule simplified. (8) §7 completion condition compressed to 4 conditions. (9) Closure record §2 description column shortened. (10) Output suppression rule added to §2 — intermediate step output suppressed on clean runs; only §7 Confirmation + Advisory Summary block surfaced. (11) Three inline advisory outputs (STEP 0 audit cadence, STEP 6 endpoint drift, STEP 12 ideas pipeline) consolidated into a single Advisory Summary block output after STEP 13. No governance logic altered. |
| 2.5 | 2026-04-17 | STEP 12 — Ideas Pipeline Health Check advisory added. After groom backlog completes, if active backlog count ≤ 5: scan ideas_register.md for Parked-cycle-N ideas whose Park Rationale references a BLG- item that has since shipped; if any found, record advisory in closure record §6 Outstanding Actions directing PO to run `run ideas` before next roadmap run so gate-cleared ideas can be re-evaluated at STEP 4.0. Advisory-only, non-blocking. |
| 2.4 | 2026-04-16 | STEP 6 — Advisory endpoint coverage drift check added. After all PRs merged: compare openapi.yaml path count vs api_performance_baseline.md measured paths; raise backlog item for gaps. Check SystemStatus.js categorizeEndpoint() for unhandled new top-level prefixes. Advisory-only, non-blocking. |
| 2.3 | 2026-04-11 | STEP 6 — `claude/cycles/velocity_metrics.md` added to operational documents reconciliation list. Engine must append a velocity row for the completed cycle (Planned/Completed from execution_state.json, rolling 6-cycle average updated) before writing STEP 6 pass. Fixes gap where v2.4 and v2.5 rows were missed (discovered 2026-04-11). |
| 2.2 | 2026-03-24 | AUD-2026-03-21-002: STEP 0 — added Audit Cadence Check advisory block. Reads `completed_cycle_count` from `.claude_current_state.json`; if `% 3 == 0`, surfaces "⚠ AUDIT DUE" advisory and records in run manifest. Non-blocking. Eliminates manual audit tracking and prevents overdue audit accumulation. |
| 2.0 | 2026-03-16 | Post-ship closure v1.10 deferred patch applied. STEP 8.5: sequencing clarification note added — `closure_record.md` is produced in STEP 9 and is not available at STEP 8.5 execution time; `lessons_learnt_closure.md` must be produced from STEP 8 consolidated action summary context; §6 Outstanding Actions in `closure_record.md` is derived from the same deferred items list. Prevents incorrect sequencing where STEP 8.5 waits for STEP 9 output. |
| 1.9 | 2026-03-14 | AUD-2026-03-13-004: STEP 11 (Roadmap Document Management — mandatory) and STEP 12 (Backlog Document Management — mandatory) added. Both invoke their respective management prompts inline and pass through --dry-run. Closes Phase 1M skip gap — manage roadmap + groom backlog now run every cycle regardless of whether Phase 1 was executed. Former STEP 11 (Commit) renumbered STEP 13. closure_state.json schema updated: step_11_manage_roadmap, step_12_groom_backlog, step_13_commit. |
| 1.8 | 2026-03-11 | IMP-15: STEP 3.4 added — stale parked items disposition check; identifies `backlog.md` items parked in 3+ consecutive completed cycles; records mandatory PO disposition requirement in closure record §6 Outstanding Actions; adds `[STALE — PO disposition required]` note to backlog item; does not resolve item. |
| 1.7 | 2026-03-10 | IMP-54: §4 Source-of-Truth inputs — `lessons_learnt_execution.md` and `lessons_learnt_verification.md` rows replaced with single `lessons_learnt_cycle.md` row (Phase 3 + Phase 4 + Amendment sections). §5 must-not-modify — `lessons_learnt_execution.md` and `lessons_learnt_verification.md` lines replaced with `lessons_learnt_cycle.md` (read-only). STEP 8 lessons learnt table — two rows replaced with single `lessons_learnt_cycle.md` row (all phase sections, structured table read). |
| 1.6 | 2026-03-10 | IMP-27: STEP 0 — field-level read targets added for `verification_report.md` (§1 verification_status + §4 deviation register only), `execution_state.json` (epics outcome map only), `sprint_close.md` (verification readiness statement + deviations list only). STEP 8 — `lessons_learnt` files: read action items section / classification table only (not full prose). |
| 1.5 | 2026-03-10 | IMP-42: STEP -1.2A added — sprint_close.md verification readiness statement check (all three `Yes` fields required before proceeding). IMP-34: §5 Write Scope — `current_roadmap.md` entry expanded to list all STEP 2 write actions; canonical spec entry updated to add document owner notification requirement; `closure_escalations.md` added as permitted write. IMP-50: `closure_escalations.md` added to §5 write scope (escalations during closure use this file, not `closure_record.md §6`). IMP-59: STEP 10 — `completed_cycle_count` increment added to global state write; rule documented (default 0 if absent; used for meta-review cadence tracking). IMP-12: STEP 9 §6 Outstanding Actions template — required table format added with named columns (Description, Owner, Deadline, Escalation path, Resolution); "None" path documented. |
| 1.4 | 2026-03-07 | **IMP-01 — closure_state.json for reliable resumability.** Added `closure_state.json` to §4 Source-of-Truth inputs and §5 Write Scope. Added full STEP 0 initialization/resume logic with JSON schema (fresh run creates file; resume skips completed steps; already-Closed halts). Added `closure_state.json` update lines at the end of STEP 0 through STEP 11 (each step writes its completion flag and `last_updated_utc`). STEP 11 commit list: `closure_state.json` added. `next_cycle_unblocked` guard noted in STEP 10. Consistent with resumability model used by execution and release planning engines. |
| 1.3 | 2026-03-07 | **Lifecycle Guard added.** Apply Lifecycle Guard per `shared_standards.md §10` (valid from-states: `Verified`, `Verified_with_deviations`) at §2 Invocation Rule. |
| 1.2 | 2026-03-07 | **`amended_backlog_slice_path` handling added.** §4 backlog slice source-of-truth rule added. §5 must-not-modify: amended backlog slice added. STEP 0: `amended_backlog_slice_path` read from `.claude_current_state.json` as first action; cross-referenced against `execution_state.json.backlog_slice_source`; disagreement flagged before proceeding; authoritative path recorded in closure record §1. STEP 3 intro updated: reconciliation runs against the authoritative slice. STEP 3.3 failure condition expanded. `closure_record.md` §1 template: `Backlog slice source` field added. §9 invariant added. **`lessons_learnt_closure.md` creation formalised (STEP 8.5, new).** STEP 8.5 added: invokes `lessons_learnt_prompt.md §3.5` using STEP 8 consolidated action summary as input; produces `claude/cycles/<cycle_id>/lessons_learnt_closure.md`; hard gate before STEP 9. §5 write scope: `lessons_learnt_closure.md` creation entry updated to reference STEP 8.5 explicitly. §7 completion condition: `lessons_learnt_closure.md` condition updated to reference `lessons_learnt_prompt.md §3.5` structure. §9 closure record §2 table: STEP 8.5 row added. §6 outstanding actions: `lessons_learnt_prompt.md` invocation failure added as example. STEP 11 commit: order corrected (`lessons_learnt_closure.md` before `closure_record.md`). §1 Purpose updated to name STEP 8.5 explicitly. **Dry-run enforcement added throughout.** §2: dry-run definition tightened — closure plan is the deliverable; routine ends after producing it. §5: dry-run exception block added at top (no writes permitted). STEP -1.6: write permission test skipped in dry-run. STEP 0: dry-run exits here after producing closure plan. §7 completion condition: dry-run completion defined. §9 invariant added. |
| 1.1 | 2026-03-06 | Added `decisions_record` N/A condition (STEP 4.2). Added push-before-pull rule (STEP 11). Clarified AR record exemption from Superseded status. |
| 1.0 | 2026-03-03 | Initial version. |
