**Owner:** Head of Specs Team
**Status:** Active

# Change Log — Roadmap Rebalance Engine

This file contains the historical change log for `claude/system/roadmap_prompt.md`.
The prompt itself contains only the current version — full history is here.

---

| Version | Date | Change |
|---------|------|--------|
| 9.16 | 2026-08-18 | Sprint execution `2026-08-17__release-v8.9` EPIC-06/ST-21 (BLG-GOV-264): STEP 8's "Displacement candidate flag" instruction gains a paired write to `claude/roadmap/displacement_debt_register.md` — create-if-absent (format/seed content designed at ST-14, `2026-07-27__release-v7.9`, never physically placed), new-candidate row creation, re-flag counter increment, and Disposition resolution to "Displaced" when this same step's Kill/Replace decision resolves a prior cycle's open flag. **Write-scope self-correction:** this engine initially created the register file directly this session, which `execution_prompt.md` §7's write-scope hard gate does not permit for `claude/roadmap/*` — reverted before commit. The prompt-wiring change here stands (a legitimate Class 6 governance-prompt edit, per CLAUDE.md §6 and prior precedent, e.g. this file's own v9.14 row); physical file creation is deferred to the next live `run roadmap`/`manage roadmap` invocation. Does not close `ESC-EXEC-20260727-02` (sealed cycle, left untouched) — see `ESC-EXEC-20260818-02` in this cycle's own `execution_escalations.md` for the live cross-reference. Authority: Head of Specs Team (Sprint Execution Engine, agent-mediated, ST-21, 2026-08-18). |
| 9.15 | 2026-08-17 | Post-ship closure `2026-08-14__release-v8.8` STEP 12 (`groom backlog` ID Uniqueness Scan finding): §8.5.C.5 BLG-ID collision advisory now greps **both** `backlog.md` and `backlog_archive.md` for the highest existing ID in each series, not `backlog.md` alone. Closes the gap that let `BLG-FEAT-84` and `BLG-SEC-18` each get independently reissued to a second, unrelated item after the first holder had already been archived — an archived-only ID was invisible to the prior `backlog.md`-only grep. The `backlog-add` skill (`.claude/skills/backlog-add/SKILL.md` Step 1) already scanned both files; this was the one remaining ID-assignment path that did not. Authority: Head of Specs Team (post-ship closure `2026-08-14__release-v8.8`, STEP 12 finding, agent-mediated fix). |
| 9.14 | 2026-08-10 | **Backfilled** (`prompt_change_log.md` row dated 2026-08-10 found this version cut with no corresponding row in this file — gap closed 2026-08-17 at post-ship closure `2026-08-14__release-v8.8`). Sprint execution `2026-08-08__release-v8.5` EPIC-06/ST-22 (BLG-FEAT-72): STEP 2.4 Product Value Ratio Diagnostic gains a structured history record — new `claude/roadmap/product_value_ratio_history.md` (backfilled DL-057 through DL-077 from `decision_log.md` prose), appended to by STEP 2.4 every rebalance going forward; the sustained-Advisory-tier consecutive-readings check now reads this file instead of re-deriving from `decision_log.md` prose. Authority: Head of Specs Team (Sprint Execution Engine, ST-22, 2026-08-10). |
| 9.13 | 2026-08-06 | ST-26 (BLG-GOV-270, EPIC-05, v8.3): new §7.2 Cross-Role Workload Balance Check — tallies story ownership by named role (`sprint_backlog.md`'s `**Owner:**` field) over the same rolling 3-cycle window §7.1's Skill-Silo Alert uses, flagging >40% single-role concentration as advisory. Distinct dimension from §7.1 (role ownership vs story shape). Authority: Director of HR (Sprint Execution Engine, agent-mediated, ST-26, 2026-08-06). |
| 9.12 | 2026-08-04 | ST-15 (BLG-GOV-279, EPIC-03, v8.2): STEP 2.3 Credential-fallback guidance gains a Standing-behaviour decision — Product Owner formally accepted the fallback-citation pattern as permanent, intended behaviour (not an open gap), closing the recurring "next rebalance should attempt a genuine live re-check" carry-forward pattern. Future sessions must not re-file this as outstanding governance debt. Authority: Product Owner (Sprint Execution Engine, agent-mediated, ST-15, 2026-08-04). |
| 9.11 | 2026-08-04 | ST-17 (BLG-GOV-283, EPIC-03, v8.2): STEP 9 gains a Last Updated header-history retention instruction — when appending to a chained `**Last Updated:**` field (e.g. `current_roadmap.md`), apply `shared_standards.md §16.14`'s retain-3 rule instead of chaining indefinitely. Authority: Head of Specs Team (Sprint Execution Engine, agent-mediated, ST-17, 2026-08-04). |
| 9.10 | 2026-08-03 | Sprint execution `2026-08-03__release-v8.1` EPIC-03/ST-09 (BLG-GOV-240): STEP 8.1 cross-references the new `shared_standards.md` §17 companion provision — a resolved condition-1b un-versioned carry-forward heading can now be relabeled with its confirmed release version by Head of Specs Team standing authority, without a full `run roadmap` invocation. Authority: Product Owner (Sprint Execution Engine, agent-mediated, ST-09, 2026-08-03). |
| 9.9 | 2026-08-03 | Sprint execution `2026-08-03__release-v8.1` EPIC-03/ST-04 (BLG-GOV-268): STEP 2.4 Product Value Ratio Diagnostic gains a mandatory-pull-forward clause for sustained Advisory-tier readings, mirroring §7.1's Skill-Silo sustained-failure clause — if the ratio has stayed in the 0.30–0.49 Advisory band for 3+ consecutive readings without reaching Healthy or dropping into Alert, the Product Owner must commit at least 1 build-and-ship-shaped U-item at the next release. Reviewed with Head of Specs Team (agent-mediated, §5.3) before adoption. **Backfill note:** this entry also retroactively adds the v9.7 and v9.8 rows below, which were made to `roadmap_prompt.md` but never logged here — discovered via this story's own governance-file-edit checklist pre-check (CLAUDE.md §6 step 1); both are independently confirmed via `OPERATIONAL_GUIDE.md` §14 rows 4.119/4.121, which did capture them at the time. `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 updated same commit. Authority: Head of Specs Team (Sprint Execution Engine, agent-mediated, ST-04, 2026-08-03). |
| 9.8 | 2026-07-29 | ST-22 (EPIC-06, v7.10, BLG-GOV-216) — recent-rebalance recency advisory added: new STEP -1.5.5 — when a `--reason "scheduled"` invocation's `last_scheduled_rebalance_utc` is less than 24h old, surface a non-blocking confirmation advisory before proceeding. Sign-off review (Head of Specs Team, agent-mediated) caught that no step actually wrote `last_scheduled_rebalance_utc`; fixed in the same commit — STEP 12.1 now sets it whenever `--reason` is `"scheduled"`. `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 updated same commit (originally; row backfilled here 2026-08-03). Authority: Head of Specs Team (Sprint Execution Engine, ST-22, 2026-07-29). |
| 9.7 | 2026-07-28 | Post-ship closure `2026-07-27__release-v7.9` STEP 8 immediate action (Release Planning lessons_learnt.md Friction Item 1): candidate live-status cross-check added — before naming any pull-forward candidate, the engine must confirm the item is still open in `backlog.md` (not archived/shipped), including checking any `groom backlog`/post-ship-closure action already taken earlier in the same session. Closes the gap where `2026-07-27__scheduled` named already-archived `BLG-FE-128` as a pull-forward candidate, caught only downstream at `plan release v7.9` via an appended correction. `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 updated same commit (originally; row backfilled here 2026-08-03). Authority: Head of Specs Team (post-ship closure `2026-07-27__release-v7.9`, STEP 8 — immediate lessons-learnt action rule). |
| 9.6 | 2026-07-27 | Roadmap rebalance `2026-07-27__scheduled` STEP -1.5, resolving deferred patch (Friction Item 2, `2026-07-24__scheduled`, target "2026-07-25__scheduled or next scheduled cycle" — matched this cycle): STEP 2.3 SI-02 gate read instruction gains explicit credential-fallback guidance. When production API credentials are unavailable in the executing checkout or a live check returns an auth failure (e.g. `401 Unauthorized`), the engine must cite the existing `**Last formally confirmed:**` structured field unchanged and record in `run_manifest.md` whether a live check was attempted and why it did not succeed — never write a "live re-confirmed" claim without an actual successful authenticated response. `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 updated same commit. Authority: Head of Specs Team (roadmap rebalance `2026-07-27__scheduled`, STEP -1.5). |
| 9.5 | 2026-07-27 | Post-ship closure `2026-07-24__release-v7.8` §6 Outstanding Actions row 1 (`next_release` field ownership drift): STEP 8's `next_release` advisory note updated to explicitly state it is non-authoritative — `release_planning_prompt.md` STEP 9 now owns and unconditionally overwrites this field on every seal; this advisory remains only as a best-guess pre-fill for the window before Release Planning next runs, and must yield to the last STEP 9 write if the two ever disagree. Authority: Head of Specs Team (post-ship closure `2026-07-24__release-v7.8` §6 row 1, 2026-07-27). |
| 9.4 | 2026-07-24 | Roadmap rebalance `2026-07-24__scheduled` STEP 11 (Friction Item, self-discovered same cycle — the v9.3 due-date scan added earlier this run would not have matched the `## Recurrence Escalations` table structure that actually surfaced 3 due items at STEP 0): STEP -1.7 Outstanding Action Count scan widened to check both the standard `^## ESC-`/`SLA due-by` pattern and any `## Recurrence Escalations` table naming "next roadmap review" as a target. `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 updated same commit. Authority: Head of Specs Team (roadmap rebalance `2026-07-24__scheduled`, STEP 11). |
| 9.3 | 2026-07-24 | Roadmap rebalance `2026-07-24__scheduled` STEP -1.5, resolving deferred patch (Friction Item 2, `2026-07-17__scheduled`): STEP -1.7 Governance Health Score Outstanding Action Count extended to a due-date-aware scan of the last 3 completed cycles' `lessons_learnt_closure.md`/`lessons_learnt.md` across all five routines, surfacing any escalation whose deadline falls on or before the current cycle's date regardless of owning routine. `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 updated same commit. Authority: Head of Specs Team (roadmap rebalance `2026-07-24__scheduled`, STEP -1.5). |
| 9.2 | 2026-07-17 | Roadmap rebalance `2026-07-17__scheduled` STEP 11: STEP 8.1 Empty Now Horizon Gate condition 1 extended — now also fires when the Now horizon contains committed items sitting only under an un-versioned carry-forward heading, not just when fully empty. Closes `BLG-GOV-240`, carried from `2026-07-16__release-v7.3` `lessons_learnt_closure.md` Carry-Forward #1. `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 updated same commit. Authority: Head of Specs Team (roadmap rebalance `2026-07-17__scheduled`, STEP 11). |
| 9.1 | 2026-07-16 | Roadmap rebalance `2026-07-16__scheduled` STEP 11: STEP 3.1 Actionable Backlog Assessment — scale-appropriate methodology codified (grep-based structural heuristic at ≥150 active backlog items, manual read below that scale). Closes the deferred patch from `2026-07-15__scheduled` Friction Item 2 on its 2nd confirming occurrence (319-item backlog). Authority: Head of Specs Team. *(Backfilled 2026-07-17 — this row was recorded in `prompt_change_log.md` and `OPERATIONAL_GUIDE.md` §14 v4.99 at the time but omitted from this file; found and corrected during the `2026-07-17__scheduled` governance file edit checklist §9.1 pre-check.)* |
| 9.0 | 2026-07-15 | Roadmap rebalance `2026-07-15__scheduled` STEP 11: STEP 4.2 Idea Consolidation convention codified — when N idea submissions converge on the same feature/problem area, file one consolidated backlog item (Source field lists all contributing Idea IDs). Confirmed generalisable after a 2nd independent clustering event. Closes the deferred patch from `2026-07-13__scheduled`. Authority: Head of Specs Team. *(Backfilled 2026-07-17 — see note on v9.1 row above; same omission.)* |
| 8.9 | 2026-07-14 | Head of Specs Team direct action (companion to `shared_standards.md` §16.12): STEP 4.2 `📋 Backlog (gate-conditional)` disposition and STEP 9 Canonical Write now require the `**Effort:**` field to include a day range at time of write whenever `Provisional-Target` names a specific release. *(Backfilled 2026-07-17 — see note on v9.1 row above; same omission.)* |
| 8.8 | 2026-07-13 | Friction patch from `2026-07-13__scheduled` STEP 11: STEP -1.5 Prior Cycle Outstanding Actions — "condition-gated defer exemption" clause added. A deferred patch whose Target names a recurrence condition (not a cycle_id/date) is exempt from the "second consecutive cycle → OVERDUE" cycle-count mechanism; instead a non-blocking "Stale Condition-Gated Defer" advisory fires at 6+ consecutive carries, escalating to Head of Specs Team to assess whether the condition is realistic. Closes an ambiguity that had been implicitly (and correctly) treated as an exemption across 3 prior scheduled cycles (2026-07-08 through 2026-07-13) without ever being codified — confirmed live when the STEP 0.C abbreviated-manifest exception reached its 4th consecutive carry without triggering an OVERDUE halt. Authority: Head of Specs Team (STEP 11 action-now, `2026-07-13__scheduled`). |
| 8.7 | 2026-07-12 | Two friction patches from `2026-07-12__scheduled` STEP 11: (1) §6 Completion Event Definition — same-day `cycle_id` collision check added; before creating `claude/cycles/<cycle_id>/`, detect an existing folder for today's computed `cycle_id` and auto-suffix (`-2`, `-3`, …) rather than requiring ad hoc user escalation. Confirmed live this cycle (a same-day double-run collision was resolved manually before this rule existed). (2) STEP -1.5 Prior Cycle Outstanding Actions — "out-of-scope OVERDUE resolution" clause added: once a named authority holds a standing out-of-band write privilege for an OVERDUE patch's target file (e.g. `shared_standards.md` §17), "outside this engine's write scope" is no longer a valid carry-forward reason: the STEP -1.5 escalation must instruct direct application under that authority. Closes a gap where the `CLAUDE.md` §6 patch was correctly identified as OVERDUE for 6 consecutive cycles (2026-07-01 → 2026-07-10 first run) but kept being re-carried anyway. Authority: Head of Specs Team (STEP 11 action-now, `2026-07-12__scheduled`). |
| 8.3 | 2026-07-06 | Deferred patch from `lessons_learnt_closure.md` (`2026-07-04__release-v6.6`, Carry-Forward #2) resolved action-now at `2026-07-06__scheduled` via STEP 5 debate (`IDEA-challenger-20260702-01`). §7.1: added "Mandatory pull-forward on sustained failure" — if the rolling 3-cycle Skill-Silo average has worsened or remained unresolved for 3+ consecutive readings, the pull-forward recommendation becomes mandatory: PO must commit ≥2 build-and-ship-shaped U-items at the next release (audit/investigation-shaped stories, classified per the STEP 2.4 content-based test, do not count even if nominally labelled U at scoping). Closes the gap where v6.5 and v6.6 each bundled 2 nominal U-items but only 1 resolved to genuine U at ship in both cases. Authority: Head of Specs Team (STEP 5.2 PO Modify decision, Challenger counter-argument citing GCA-2026-06-17). |
| 8.2 | 2026-07-06 | LP-05 (post-ship closure 2026-07-04__release-v6.6): §7.1 Skill-Silo Alert pull-forward candidate selection — added explicit candidate gate verification. Before naming a pull-forward candidate, the engine must read the candidate's own backlog entry for a `**Gate criteria:**` line and confirm it is met or near-term-clearing; unmet-gate candidates must not be named without an explicit "gate status unverified/unmet" flag. Fixes the gap where `2026-07-03__scheduled` named BLG-FEAT-52 as a candidate without checking its own unmet PO-02 gate, requiring release planning to catch and reject it. Authority: Head of Specs Team (applied at post-ship closure per CLAUDE.md §2 immediate-fix rule). |
| 6.3 | 2026-05-20 | 3-cycle hard cap on parked ideas + Backlog (gate-conditional) classification. §4.1: new 📋 Backlog (gate-conditional) option added — idea exits the parked queue immediately and is added to backlog.md with a documented gate criteria block; park rationale validation gate added (Facilitator must challenge vague rationales; second vague park defaults to Reject not strong); stale ideas note updated to reference §4.5 hard cap. §4.2: Promoted-Backlog row added to document management table. §4.5: "no cap on re-parks with valid rationale" removed; replaced with 3-cycle hard cap — cycles 1–2 allow re-park with valid specific rationale (subject to Facilitator gate); cycle 3 forces terminal outcome (Advance, Reject, or Backlog gate-conditional only). Authority: Head of Specs Team (2026-05-20). |
| 5.0 | 2026-04-17 | Two pipeline patches applied by Product Owner (2026-04-17): (1) STEP 0.D Empty Horizon Advisory added — when the Now horizon is empty post-ship and active backlog items exist, surface advisory directing PO to run `plan release` directly rather than proceeding through a full rebalance debate; advisory-only, non-halting. (2) STEP 4.0 Gate-Condition Re-Check added — before per-idea classification, Facilitator must verify whether any `BLG-` backlog item referenced in a Park Rationale has shipped; gate-cleared ideas are surfaced as "mandatory re-evaluation" and cannot be silently re-parked; results recorded in cycle_record.md `### Gate-Condition Re-Check` subsection. |
| 4.9 | 2026-04-16 | ST-11 (BLG-GOV-14, v2.7): STEP -1.7 added — Governance Health Score advisory check. Computes three components per OPERATIONAL_GUIDE.md §15: (1) Header Compliance % for Class 4/5 docs in active cycle folder; (2) Deferred Patch Indicator by age band (Green/Amber/Red); (3) Outstanding Action Count across open_escalations, execution_state, and lessons_learnt. Output written to run_manifest.md. Advisory only — cannot halt run. Authority: Head of Specs Team (ST-11, 2026-04-16). |
| 4.3 | 2026-03-21 | Friction Item 1 (cycle 2026-03-21__item-3.5): STEP 4.4 Write Summary extended — `## STEP 5 Debate Queue` table added to cycle_record.md output template; lists all Advancing IDEA IDs with title and source (stale/new); mandatory count-match check before proceeding to STEP 5. STEP 5 preflight check added — reads queue, verifies one debate entry per queued IDEA ID before writing STEP 5 section as complete; detects omissions before they propagate to non-terminal register rows. |
| 4.2 | 2026-03-18 | STEP -1.6 corrected: count source changed from `claude/ideas/submissions/` file scan to `claude/ideas/ideas_register.md` row count (consistent with register model). STEP 0.C Lightweight criterion 2 updated to reference register rows. STEP 8.5.B item 4 language updated: "idea file" → "register row" throughout (LL-02-patch language now register-model consistent). |
| 4.1 | 2026-03-17 | ST-19 (EPIC-06): STEP 4 updated to read from `claude/ideas/ideas_register.md` (single register) instead of scanning per-file submissions. §4.2 Document Management updated: actions now update register rows (Status, Park Count, Park Rationale) instead of individual files. §4.3 window summary path updated. Schema: `shared_standards.md §16.5`. |
| 4.0 | 2026-03-17 | ST-18 (EPIC-06): Extended `cycle_record.md` single-file pattern to all three tiers (Lightweight/Standard/Extended). STEPS 2, 3, 4, 5, 8 write targets changed from separate stage files (`stage1_validation.md`, `stage2_backlog_health.md`, `stage3_ideas.md`, `stage4_debate.md`, `stage5_rebalance.md`) to labelled sections within a single `cycle_record.md`. Stage file references in §5 hard gates, §5.3 PoG issuance, §7 Skill-Silo / Capacity Floor rules, §10 Completion Condition, and STEP 0.C tier table all updated accordingly. Note in STEP 0.C explains mapping for any residual stage-file references. |
| 3.0 | 2026-03-17 | Five governance improvements applied at post-ship closure v1.10 (BLG-GOV-01, BLG-GOV-02 actions): (1) Tier table added to STEP 0.C classifying run weight; (2) Stage output format note established `cycle_record.md` as Lightweight output target; (3) STEP 2 and STEP 3 write targets standardised to `cycle_record.md` sections for Lightweight; (4) STEP -1.5 prior cycle actions check updated; (5) Minor structural clarifications. |
| 2.8 | 2026-03-16 | AUD-2026-03-13-005: §9.0 Net-Zero Displacement Gap inline halt block replaced with prose reference to `shared_standards.md §5` — saves ~80 tokens/cycle, SST improved. AUD-2026-03-13-006: §9 Invariants list replaced with reference to `claude/system/invariants.md` (new canonical file). |
| 2.7 | 2026-03-16 | Post-ship closure v1.10 deferred patch applied. STEP 8.5.B: item 4 added — idea file status verification (LL-02-patch): for each idea file set to `Advancing` in §4.2, verify STEP 9 write plan includes terminal status update (`Promoted-Added` or `Promoted-Rejected`); governance record gap if any `Advancing` idea file unaccounted for at end of run. |
| 2.6 | 2026-03-15 | STEP -1.6 corrected: trigger changed from open-window status check (unreachable in normal flow) to open idea count < 20 — counts Submitted + Parked-cycle-N files in claude/ideas/submissions/; invokes idea intake inline if below threshold, skips if 20+. |
| 2.5 | 2026-03-15 | AUD-2026-03-13-003: STEP -1.6 Idea Window Check (conditional) added — checks ideas_window.json status; invokes idea_intake_prompt.md inline if open; proceeds gracefully if absent or closed. |
| 2.4 | 2026-03-14 | AUD-2026-03-13-002: `--dry-run` flag added to both invocation forms (completion-triggered and scheduled). Dry-run exits after STEP 8 — no writes, no commit; output sufficient to validate before live run. CLAUDE.md command table updated to show `[--dry-run]`. |
| 2.3 | 2026-03-14 | AUD-2026-03-13-021 (PATCH 1): STEP 11 lessons_learnt.md output must end with machine-readable `// ARTEFACT_STATUS` JSON terminal block — enables post_ship_closure.md STEP 8 to grep for counts without full document read. |
| 2.2 | 2026-03-14 | AUD-2026-03-13 audit improvements: (1) STEP -1.5 extended — B7 auto-escalation rule added for deferred patches carried two consecutive cycles; prompt patch confirmation check; state age advisory (>30 days). (2) STEP 5 — Challenger failure halt instruction added per team_charter.md §3.2. (3) STEP 9 — Decision log append-only enforcement upgraded from assertion to structural (pre/post count check + corruption detection). (4) STEP 9 — Header formatting rule added (bold field labels required for Class 4 headers). |
| 2.1 | 2026-03-11 | IMP-13: STEP 9.0 net-zero displacement verification added — hard gate before any write; counts additions vs confirmed kills; halts if additions > kills with displacement gap report; mode-independent. IMP-33: STEP 5.0 displacement rule — mode-independence note added ("applies in both strict and standard mode; governance constraint, not relaxed by --mode standard"). |
| 2.0 | 2026-03-06 | **Six governance improvements plus continuous improvement loop.** (1) Added STEP -1.5: Prior Cycle Outstanding Actions Check. (2) Added Parked Idea Expiry Rule (§4.5). (3) Displacement candidate flag moved to initiative_register.md exclusively. (4) Added scheduled run trigger. (5) Added absolute CPS alert threshold (>2.5). (6) Added effort banding (S/M/L). **Continuous improvement additions:** Expanded STEP 11 into four sub-steps: 11.1 lessons learnt invocation (unchanged), 11.2 prompt change classification (action-now vs defer — Head of Specs Team sign-off required for action-now), 11.3 prompt change log (`claude/system/prompt_change_log.md` — append-only, traceable from every prompt version to its triggering friction item), 11.4 meta-review trigger (every third cycle — aggregates friction patterns across cycles and produces candidate prompt changes). Added `claude/system/*` and `claude/system/prompt_change_log.md` to write scope (§5). Added `prompt_change_log.md` to optional artifact list (§6). Added `last_meta_review_cycle` key to `.claude_current_state.json` (§12.1). Updated commit scope, write plan integrity checks, and completion condition accordingly. Also incorporated two tooling notes from cycle 2 lessons learnt: bash heredoc pattern for new files (§6); bash sed pattern for bulk idea updates (§4.2). Added hard gate marking rule to STEP 9 and §9 invariants. |
| 1.9 | 2026-03-04 | **Six governance improvements.** (1) Added Class 8 — Proof of Gate (PoG). (2) Added Strategy Proximity Score (1–5) in STEP 2.1. (3) Added Cycle Proximity Score aggregate and trend check in STEP 2.2. (4) Added Skill-Silo Alert in STEP 7.1. (5) Proximity score added to STEP 6 scoring matrix. (6) Added PoG validity and CPS to completion condition (§10). |
| 1.8 | 2026-03-03 | Removed displacement as an advancement gate in STEP 4.1. |
| 1.7 | 2026-03-03 | Rewrote STEP 4 — replaced "Idea Intake & Eligibility Gate" with "Idea Review and Document Management". |
| 1.6 | 2026-03-03 | Fixed header formatting. Added lessons_learnt_prompt.md to preflight. Added STEP 12.1 Global State Update. |
| 1.5 | 2026-03-01 | Prior version. |
