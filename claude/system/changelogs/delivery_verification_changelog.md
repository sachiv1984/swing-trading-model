**Owner:** Head of Specs Team
**Status:** Active

# Change Log — Delivery Verification Engine

This file contains the historical change log for `claude/system/delivery_verification_prompt.md`.
The prompt itself contains only the current version — full history is here.

---

| Version | Date | Change |
|---------|------|--------|
| 3.8 | 2026-08-12 | Post-ship closure `2026-08-11__release-v8.6` STEP 8 immediate action (LL-v8.6-P4-03): §7 Deviation Severity Policy — added the Resolved-deviation carve-out: a deviation record filed with `Status: Resolved` (retroactive record of an already-fixed defect) is entered in the Deviation Register for traceability but does not trigger the P0–P3 hard-block/PO+DoQ acceptance-recording requirement. Closes an interpretive gap this cycle's own verification had to infer rather than read directly (`DEV-NAV-ST06-01`, a retroactively-filed P1 record for an already-shipped v8.5 fix). Authority: Head of Specs Team (post-ship closure `2026-08-11__release-v8.6`, STEP 8 — immediate lessons-learnt action rule). |
| 3.7 | 2026-08-03 | Post-ship closure `2026-08-03__release-v8.1` STEP 8 immediate action (LL-v8.1-P4-01): STEP -1.3A PR Number Recovery — no longer hand-writes a recovered `pr_number` into the disposable/regenerate-on-read `execution_state.json`; now writes into the owning `execution_state/EPIC-xx.json` file (per `shared_standards.md §12.1`, added this same cycle by ST-19) and regenerates the summary via `generate_execution_summary.py`, with a fallback to the legacy direct write for pre-per-EPIC-mechanism cycles. Closes a friction item surfaced proactively by EPIC-07's agent-mediated reviewer during `2026-08-03__release-v8.1` delivery verification (not yet triggered in practice — no PR recovery was needed that run). Authority: Head of Specs Team (post-ship closure `2026-08-03__release-v8.1`, STEP 8 — immediate lessons-learnt action rule). |
| 3.6 | 2026-08-03 | ESC-CLOSE-20260731-01 (Option a): STEP -1.3 Tier 2 — added a fourth recognised sign-off format, the "named domain-authority class exception", accepting a signer naming a specific human or agent-mediated domain-authority role (e.g. `Infrastructure & Operations Owner`, `Head of Engineering`, compound `<Role A> ... with <Role B> concurrence` forms, or execution_prompt.md's Infrastructure co-sign format) as compliant provided the EPIC contains no `autonomous`-class story. Closes the gap where `execution_prompt.md` §5.3's Infrastructure co-sign class claimed acceptance by this gate that the gate did not actually implement (surfaced by EPIC-04/EPIC-06 requiring one-off DoQ counter-signs at `2026-07-30__release-v8.0` delivery verification). Authority: Head of Specs Team (post-ship closure `2026-07-30__release-v8.0` escalation, resolved 2026-08-03). |
| 3.5 | 2026-07-20 | Post-ship closure `2026-07-17__release-v7.5` STEP 8 immediate lessons-learnt action: STEP 2.1 — `Staging-deferred (per CLAUDE.md §2 / shared_standards.md §16.11)` added as an explicitly accepted `Result` value alongside `Pass`/`Pass with notes`/`Fail`, conditioned on a confirmed pre-PR backlog item. Authority: Head of Specs Team (post-ship closure `2026-07-17__release-v7.5`, STEP 8). **Note:** this file had fallen behind the prompt's actual version (top row was 2.3/2026-05-17 against a live version of 3.4) — full backfill of the missing 2.4–3.4 rows deferred per the same partial-backfill precedent applied to `roadmap_prompt_changelog.md` (`2026-07-17__scheduled` STEP 11); this entry restores sync going forward only. |
| 3.4 | 2026-07-09 | Companion patch to the `execution_prompt.md` v3.55 Case E change. STEP 1.3 — when `spec_references = []`, now checks `spec_reference_not_applicable` first (exempt, recorded with its reason in the traceability matrix) before falling back to the legacy `notes` "no prior spec applicable" string for pre-existing records. Authority: Head of Specs Team (direct action, user-invoked, 2026-07-09). |
| 3.3 | 2026-07-06 | v6.7 ST-07 (EPIC-02, BLG-GOV-170): STEP 6 (System Status Report Reconciliation) — added explicit sub-step documenting the `**Status:**` line transition (`Sprint_Complete — pending verification` → `Verified — <date>` / `Verified_with_deviations — <date>`) as routine, expected behaviour; must not be logged as friction in `lessons_learnt_cycle.md`. Authority: Head of Specs Team (v6.7 ST-07, BLG-GOV-170, 2026-07-06). |
| 3.2 | 2026-07-06 | v6.7 ST-05 (EPIC-02, BLG-GOV-168): escalation write step (`verification_escalations.md`) now references the Structural Append-Verification Procedure (`shared_standards.md §7.1`). Authority: Head of Specs Team (v6.7 ST-05, BLG-GOV-168, 2026-07-06). |
| 3.1 | 2026-06-22 | AUD-2026-06-22-007 (latent): STEP 5.1 — algorithm replacement advisory added; for stories replacing a core algorithm/model/scoring function, cross-check that all test_scenarios entries in execution_state.json were either confirmed run in qa_evidence, or declared superseded in DoQ sign-off naming the replacement test; otherwise flag as coverage gap and create a TSG entry in STEP 5.3. Authority: Head of Specs Team + PMO Lead (AUD-2026-06-22 latent, 2026-06-22). |
| 3.0 | 2026-06-21 | v5.1 ST-03 (EPIC-02, LL-RP-v5.0-D-2): STEP -1.3 Tier 2 — new agent-mediated class exception added: `"Sprint Execution Engine (agent-mediated, <Role Name> role — §X.Y)"` accepted for mixed-class EPICs as equivalent to agent-mediated sign-off with named role. Authority: Head of Specs Team (v5.1 ST-03, 2026-06-21). |
| 2.9 | 2026-05-30 | AUD-2026-05-30-006: Owner field corrected — `Director of Quality` → `Head of Specs Team`, matching prior `prompt_change_log.md` entries and OPERATIONAL_GUIDE §13. No functional behaviour change. Authority: Head of Specs Team (AUD-2026-05-30-006, 2026-05-30). |
| 2.8 | 2026-05-28 | Branch Safety Check (Hard Gate) added at STEP -1: `git branch --show-current` must return `main`; halt with checkout instructions if not. Paired with post_ship_closure.md v2.12 branch safety gate. Authority: Head of Specs Team (2026-05-28). |
| 2.7 | 2026-05-27 | STEP 9.0 — Artefact Presence Pre-Check added as sub-step of STEP 9 (Global State Update): before updating `.claude_current_state.json`, verify `verification_report.md`, `sprint_close.md`, and `lessons_learnt_cycle.md` (Phase 4) exist on disk; Required Class-3 artefacts trigger a soft halt if absent. Authority: Head of Specs Team (BLG-GOV-56, v4.1 ST-14, 2026-05-27). |
| 2.6 | 2026-05-27 | ST-03 (EPIC-01, v4.1) OA-04: STEP -1.3A (new sub-step) added to preflight — PR Number Recovery: for each EPIC in `epics_merged`, check if `pr_number` is null; if null, recover via `gh pr view exec/<cycle_id>/EPIC-xx --json number,state,mergedAt`; record recovered number; if no PR found, flag as process gap. Authority: Head of Specs Team (OA-04, v4.1 ST-03, 2026-05-27). |
| 2.5 | 2026-05-22 | ST-11 (EPIC-04, v3.9): dry-run detection block added at STEP -1 — when `--dry-run` present, output structured verification plan listing all STEP checks with precondition sources; no verification_report.md written, no state update. Authority: Head of Specs Team (ST-11, v3.9 sprint execution, 2026-05-22). |
| 2.4 | 2026-05-21 | §5 Write Scope: preamble reference replaces verbose inline block. §6 Required Authority Roles: preamble §Agent-Integrity reference replaces verbose block. §9 Governance Invariants: preamble reference + 3 phase-specific invariants preserved. Authority: Head of Specs Team (prompt compression 2026-05-21). |
| 2.3 | 2026-05-17 | Governance pattern consolidation. STEP -1 preflight: -1.4 (Required Files Present) replaced with reference to `shared/preflight_common.md` sub-check 1 (required_files: per Section 4). Engine-specific checks -1.1 through -1.3 unchanged. Authority: Head of Specs Team (governance pattern consolidation 2026-05-17). |
| 1.9 | 2026-04-11 | AUD-2026-04-11-005: STEP -1.3 sign-off check upgraded to two-tier STRUCTURAL check — Tier 1 (BLANK): empty or "pending" sign-off = HALT; Tier 2 (WRONG AUTHORITY): sign-off present but not Director of Quality = FLAG + require DoQ counter-sign before STEP 1 + compliance advisory in run_manifest. Resolves STALE 2-cycle deferred patch. Authority: Head of Specs Team (AUD-2026-04-11, 2026-04-11). |
| 1.8 | 2026-04-06 | ST-12 (CF-2b): Pre-seal gate added at STEP 8 — before proceeding to STEP 8.5, verify §9 DoQ Date and PO Date fields are both non-blank in verification_report.md; surface for completion if blank; do not seal until both filled. Authority: Head of Specs Team (ST-12, 2026-04-06). [Backfill entry — not present at time of apply.] |
| 1.7 | 2026-03-31 | LL-v2.3-CL-03: STEP 3 — canonical spec Known Deviations sync note added. After creating a backlog item for any P1–P3 deviation, verify the canonical spec has a Known Deviations section entry for this deviation; create if absent. Prevents post-ship closure STEP 5 from being the first propagation gate (recurred v2.2 and v2.3). Authority: Head of Specs Team (post-ship closure 2026-03-24__release-v2.3). |
| 1.6 | 2026-03-24 | LL-CL-v22-01: STEP 3 deviation register — backlog reference synchronisation note added. When a new backlog item is created for a deviation, the canonical spec deviation note `Backlog reference:` field must be updated to the new item ID in the same session. Prevents stale references at post-ship closure. Authority: Head of Specs Team (lessons learnt closure 2026-03-21__release-v2.2). |
| 1.4 | 2026-03-11 | IMP-14: STEP 5.3 added — `test_scenario_gaps` structured table in `verification_report.md §6`; fields: gap_id, EPIC, description, qualifying_reason, disposition (backlog_item_created | not_applicable | deferred); all gaps must have a disposition before report seals (Phase 4 exit criterion). §8 completion condition updated. IMP-15: STEP 4.3 added — stale parked items detection; items parked in 3+ consecutive cycle backlog slices surfaced for mandatory PO disposition; recorded in `verification_report.md §5`; detection only — does not block verification status. |
| 1.3 | 2026-03-10 | IMP-54: §5 Write Scope — `lessons_learnt_cycle.md` added (append-only, Phase 4 section; create if absent). STEP 8.5 added — lessons learnt Phase 4 append via `lessons_learnt_prompt.md §3.4`; output: `lessons_learnt_cycle.md` Phase 4 section; idempotency guard built into prompt §3.4; hard gate before STEP 9. STEP 10 commit: `lessons_learnt_cycle.md` added. §8 completion condition: Phase 4 section appended condition added. |
| 1.1 | 2026-03-07 | **`amended_backlog_slice_path` handling added.** §4 backlog slice source-of-truth rule added. STEP -1.1 extended: checks `amended_backlog_slice_path` in `.claude_current_state.json`; cross-references against `execution_state.json.backlog_slice_source`; flags disagreement before proceeding. STEP 1 updated: iterates over the authoritative slice (not hardcoded `stage4_backlog_slice.md`). §5 write scope: amended backlog slice added to must-not-modify list. `verification_report.md` §1 template: `Backlog slice source` field added. §9 invariant added. **`Verification_Failed` status corrected to `Not_Verified`.** STEP 9 `Not_Verified` path: `status` field in `.claude_current_state.json` changed from `Verification_Failed` to `Not_Verified`, consistent with guide §9.4 state machine and lifecycle table. **Deferred execution blockers acknowledged (STEP 4.2, new).** STEP 4 split into §4.1 (outstanding items, unchanged) and §4.2 (deferred execution blockers). §4.2 reads `deferred_execution_blockers` from `state.json`, dispositions each blocker, and records outcomes in `verification_report.md` §5. Informational only — does not block verification status. Sign-off blocks in `verification_report.md` §9 updated: DoQ checklist and PO checklist each add a deferred blocker acknowledgement line. §8 completion condition updated. §9 invariant updated. **Escalation subroutine added.** `verification_escalations.md` added to §5 write scope. Escalation subroutine added (callable, ID prefix `ESC-VER-YYYYMMDD-nn`). STEP 3: P0 deviation now files escalation record. STEP 9 Not_Verified path: references escalation records in halt report. STEP 10 commit: `verification_escalations.md` added. §8 completion condition updated. **Guide fix required:** §9 source prompt v1.0 → v1.1; §14 Verification Engine Source → v1.1; `Not_Verified` confirmed as the canonical status string (not `Verification_Failed`). |
| 1.0 | 2026-03-03 | Initial version. |
