Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-08-07
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-08-07

# Run Manifest — Release Planning — v8.4

Class: Operational Record (Class 3)
Cycle: 2026-08-07__release-v8.4
Invocation: `plan release --version v8.4` — user instruction: "Use full capacity and prioritise user features"

---

## STEP -1 — Preflight Gate

### -1.1 Required Files Present
All present: `team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md`. `gh_issue_template.md` present (not required this run — `--issues` not specified, defaults to none/no issue artifact requested; no `gh` automation invoked).

### -1.2 Release Exists on Roadmap
No formal `## v8.4` roadmap section exists. Cleared via STEP 8.1 Option (b) decision from `2026-07-28__scheduled` (same Option (b) equivalence already relied on by `2026-07-30__release-v8.0`, `2026-08-03__release-v8.1`, `2026-08-04__release-v8.2`, `2026-08-05__release-v8.3`). §-1.2 cleared via STEP 8.1 Option(b) decision from 2026-07-28__scheduled.

### -1.3/-1.4 Roles and Write Test
All 9 required agent-integrity roles confirmed present in `claude/agents/`. Write test at `claude/cycles/2026-08-07__release-v8.4/.write_test` succeeded and was removed.

### -1.5 Prior Cycle Lessons Learnt Closure Check (Advisory)
`prior_cycle` = `2026-08-05__release-v8.3`. `lessons_learnt_closure.md` exists. 0 `action-now` items recorded in that file (`action_now_count: 0` per its own ARTEFACT_STATUS block) — nothing to cross-check against `prompt_change_log.md`. No advisory fires.

### -1.6 Post-Ship Precondition Check (Hard Gate)
`.claude_current_state.json`: `post_ship_complete = true`, `next_cycle_unblocked = true`. Both conditions met. Proceed.

### -1.7 Prompt Change Log Integrity Check (Advisory)
Not independently re-run this session (would require enumerating every Class 6 prompt's current version against `prompt_change_log.md` — no drift-relevant change made this run). Deferred to next `governance-drift`/`prompt-sync` check; not a hard gate.

### -1.8 Amendment In Progress Guard (Hard Gate)
`.claude_current_state.json.status` = `Closed` at session start (not `Amendment_In_Progress`). Clear.

### -1.9 Stale Backlog Lock Preflight Check (Advisory)
`claude/backlog/.lock` does not exist. No stale-lock condition. Clear.

**Preflight: PASS.**

---

## STEP 0 — Run Manifest + State Init

**Carry-Forward Advisory (ST-15):** Most recently completed cycle with `post_ship_complete = true` is `2026-08-05__release-v8.3`. Its `lessons_learnt_closure.md` `## Carry-Forward` section has 3 items:
1. `BLG-OPS-13`/`BLG-OPS-133` overlapping problem space — for `groom backlog`, not this engine. Noted; `BLG-OPS-133` is included in this cycle's scope (see EPIC-05) and will itself close out the residual gap.
2. `execution_prompt.md` §7 backlog write-scope tension, 3 consecutive cycles unresolved — for Sprint Execution/PMO Lead, not this engine. Noted, no action here.
3. Cross-EPIC merge retrospective on commit `8a62cfc1` — for Post-Ship Closure/Head of Specs Team. Noted, no action here. (`BLG-GOV-212`, "Dry-run the cross-EPIC merge conflict runbook", is included in this cycle's scope — EPIC-07 — and is a reasonable vehicle to also close out this specific carry-forward observation when executed.)

Carry-forward items reviewed: 3 items from cycle `2026-08-05__release-v8.3`.

**scored_initiatives.md Load (ST-14):** Loaded. 0 matching rows for this cycle's scope items (all scope items this cycle are backlog-driven debt/hardening/single-feature stories, not roadmap-level scored initiatives). Falling back to inline STEP 4 estimates for all EPICs — no advisory required per the three-tier rule (no matching row = no advisory).

`run_manifest.md` created. `state.json` initialized: `status = Initialized`, `mode = standard`, `issues_mode = none`, `auto_escalate = true`.

---

## STEP 1 — Release Readiness Validation

### 1.1 Backlog Age Advisory
Scanned scope candidates for spec/documentation-debt items aged 2+ cycles without a story assignment. `BLG-SPEC-116` (structural `openapi.yaml` defect, P1) was only just filed 2026-08-07 (this session's own prior-session finding, 0 cycles aged) — no advisory. No other spec-debt candidate in this cycle's scope has aged 2+ cycles without assignment. No advisory fires.

### 1.2 Provisional-Target Advisory
Of this cycle's 30 scope candidates: 3 carry `Provisional-Target: v8.4` explicitly (`BLG-FE-141`, `BLG-BE-82`, `BLG-BE-83`) — horizon-planned for this exact release. 27 carry `TBD`/`Unscheduled`/no field — no Provisional-Target signal, selected on priority/readiness/user-instruction grounds instead.

### 1.3 Design-Gate Language Scan
Scanned all 30 candidates for design-gate language ("design decision required", "pending design", "requires UX decision"). None found. No design-dependency flag raised at this step (STEP 4.1's own UI-facing classification, run separately below, still applies).

### 1.4a Perennial-Return Check
`BLG-FEAT-73` (SI-02 frontend) and `BLG-FEAT-74` (PO-05 Replay Mode) are the only items with a history of PO Option (a) "keep conditional" dispositions (2 of 4, per the v8.3 STEP 1.4a.1 retroactive assessment). Both remain gate-blocked this cycle (gate conditions confirmed still `[unverified/unmet]` in their own backlog entries, no live re-check performed this session — see STEP 1.4 note below) and were not named as candidates in `2026-08-05__release-v8.3`'s `stage4_backlog_slice.md` (v8.3 parked them at PO disposition before candidate stage, per that cycle's roadmap annotation). Because they never entered `returned_to_backlog`/`deferred` status in v8.3's own slice, the "returned N consecutive cycles" counter used by this check does not increment this cycle — no active PO disposition is required at this gate. Both remain excluded on ordinary gate-blocked grounds.

### 1.4a.1 Sunset Criteria
No sunset trigger fires this cycle — see 1.4a above (counter did not increment).

### 1.4b Within-Sprint Date Gate Classification
Scanned all 30 scope candidates for a gate condition whose clearing date falls within the planned sprint execution window. None of the 30 selected items carry a calendar-date gate at all (all are either genuinely ungated or were excluded for cause — see 1.4 below). No item requires conditional classification under this rule.

### 1.4 Gate-Condition Proximity Scan (Advisory)

| Item | Gate condition | Current trajectory | Projected clear date |
|------|-----------------|---------------------|----------------------|
| `BLG-FEAT-73` (SI-02 frontend) | ≥20 closed trades with **linked** trade_plans; drift-detection p99 <2s over 7 days; non-trivial score variance | Last live re-check 2026-07-28 (EPIC-08/ST-08, v8.3 execution): NOT MET, 0/N linked. No new linkage-generating activity this session to re-derive a rate from. | Not calculable this session — trajectory unknown |
| `BLG-FEAT-74` (PO-05 Replay Mode) | §13 determinism pre-clearance not yet run; VH effort exceeds single-cycle sizing | No change since 2026-07-27 escalation note | Not calculable — pre-clearance review not yet scheduled |
| `BLG-OPS-51` (excluded from this cycle's scope) | `claude_audit_log` table 6+ months old (~Nov 2026, since v4.0 ship 2026-05-22) | Fixed calendar gate, ~3 months out from today (2026-08-07) | ~2026-11-22 (per its own stated ~Nov 2026 target) |

**Arc 4 data density sub-check:** No live production query performed this session (no data-density-changing activity occurred between the 2026-07-28 rebalance's last readings and now — no sprint has executed in that window). PO-02/PO-04/SI-02 projected dates carried forward unchanged from the 2026-07-28__scheduled rebalance: all "data not available for a fresh rate-based projection this session — Product Owner to confirm at next rebalance's live re-check."

Advisory only — no halt.

---

## STEP 2 — Scope Extraction

31 scope items identified across 7 EPICs. S2-01 through S2-07 map 1:1 to EPIC-01 through EPIC-07 (each EPIC groups a single coherent scope theme this cycle — no EPIC spans multiple S2 items). Scope document: `docs/product/scope/scope--2026-08-07__release-v8.4.md`.

**User-feature scope-priority finding (per explicit user instruction "prioritise user features"):** The ungated, ready backlog pool initially surfaced **1** genuinely user-facing feature-build item: `BLG-FE-141` (Add Avg P&L/Trade column to Monthly P&L Report, already carrying `Provisional-Target: v8.4` from an explicit 2026-08-06 PO acceptance decision). A 2nd was found via the self-caught gate correction below: `BLG-FEAT-78` (trigger-source column on the tax-year P&L CSV export), whose gate condition (`BLG-FE-116` ships) is now met. Both are placed in EPIC-01 (ST-01, ST-31) as this release's lead user-facing scope. All other `BLG-FEAT-*` items (the larger, roadmap-flagship user features — SI-02 frontend, PO-05 Replay Mode, SI-05 Phase 2, and the remaining Arc-4/Arc-5 feature set) remain gate-blocked on data-density or §13-clearance conditions not met as of this session (see STEP 1.4). Of the 8 originally-ungated `BLG-FE-*` items, only `BLG-FE-141` is feature-shaped in the "user gets new visible capability" sense — the remaining 7 are UX-research, design-exploration, accessibility-fix, or code-health/audit-shaped (not new user-facing capability). This is the same thin-user-feature-pool pattern documented at every release since `v8.0` — the backlog genuinely does not contain more than 2 ready, ungated, build-and-ship user features this cycle even after the gate-correction find; the "prioritise user features" instruction is honoured by leading with both that exist and by weighting the remaining 29 items toward execution-heavy (debt/hardening) rather than governance-process scope (see Skill-Silo note below), not by inventing feature scope that isn't there.

**Self-caught gate correction — `BLG-FEAT-78`:** This item's `Gate criteria` field reads "`BLG-FE-116` (custom price alerts) ships" — `BLG-FE-116` shipped in `v7.5` (retired 2026-07-20, confirmed via `backlog_archive.md`). The gate condition is met; the item's own field was never updated after `v7.5` shipped (same stale-gate-field failure mode `BLG-GOV-286` was filed to prevent recurring). Corrected in `backlog.md` this session (Gate criteria annotated as cleared, item promoted into this cycle's scope as EPIC-01/ST-31 — a genuine 2nd user-facing/reporting item this cycle, strengthening the "prioritise user features" honouring beyond the single `BLG-FE-141` item). Recorded here per the same self-caught-scan-miss precedent as `BLG-OPS-48` in prior cycles.

**Note on scan-window artifact (self-correction):** An earlier pass of this session's own ad hoc gate-verification script (a fixed-line-count window read from each candidate's header) mis-attributed `BLG-FEAT-78`'s neighbouring `Gate criteria` line to `BLG-QA-110` (which sits 3 entries above `BLG-FEAT-78` in `backlog.md`, with a body only 5 lines long — well inside the script's 25-30 line window). `BLG-QA-110` itself carries no `Gate criteria` field at all and was never gated; it is included in scope (EPIC-06/ST-27) on ordinary ungated grounds, not as a correction. This is exactly the class of failure `BLG-GOV-286` (also in this cycle's scope, EPIC-07/ST-29) exists to fix — its acceptance criteria have been extended (see `stage4_backlog_slice.md` ST-29) to explicitly cover this fixed-window body-bleed failure mode alongside the previously-known ones.

**Excluded, checked and confirmed still gated:** `BLG-OPS-51` (claude_audit_log 6-month gate, ~Nov 2026, not yet due).

---

## STEP 3 — Execution Plan + Decisions Record

See `release_plan.md §Execution Plan` for the EPIC table, risk register, and sequencing. Decisions record: `docs/product/decisions/decisions--2026-08-07__release-v8.4.md`.

**Skill-Silo mitigation — execution-heavy assignment rotation (§3 guideline):** The most recent rebalance (`2026-07-28__scheduled`) reported the Skill-Silo Alert as a 2nd consecutive worsening reading (65.8%, up from 64.5%). Per the rotation guideline, this release's EPIC table is weighted toward execution-heavy (debt/hardening) scope: of 30 scope items, only 2 are governance-process-shaped (`BLG-GOV-286`, `BLG-GOV-212`, both EPIC-07, ~7% by count / ~5% by estimated effort) — the remaining 28 are user-feature, backend engineering, frontend, operations, QA, or security hardening (execution/debt-shaped, not governance-process-shaped). This is a materially lower governance proportion than the ~50-90% seen in several recent rebalance/backlog-addition windows, consistent with the rotation guideline's intent.

---

## STEP 3.5 — Local Model Integrity Check
No local model/prompt-drift integrity issue identified for this cycle's scope (no AI-model-touching story in scope changes model routing or prompt templates). Pass.

---

## STEP 3.9 / STEP 4 — Backlog Lock, Slice, Capacity

See `stage4_backlog_slice.md`, `backlog_txn.json`, and `release_plan.md §Capacity Check`.

---

## STEP 4.1 — Design Gate Classification

Scanned all 30 ST items for UI-facing scope. UI-facing items with observable ACs: `ST-01` (`BLG-FE-141`, new visible table column), `ST-16` (`BLG-FE-140`, visible colour-token fix). Both are `autonomous`-class with observable UI acceptance criteria (visible rendering / colour). **`design_gate_required = true`.**

⚠ DESIGN GATE REQUIRED before plan sprint — 2 items classified as UI-facing. Run: `run design-gate --cycle 2026-08-07__release-v8.4`

---

## Outstanding / Advisory Notes Summary

- Design Gate required (2 UI-facing items) — must run before `plan sprint`.
- `BLG-QA-110` gate-field correction applied this session (see STEP 2).
- `BLG-FEAT-73`/`BLG-FEAT-74` remain gate-blocked; no fresh live gate re-check performed this session (last check 2026-07-28).
- Carry-forward items 1–3 from `2026-08-05__release-v8.3` closure reviewed — none required action within this engine's write scope.
