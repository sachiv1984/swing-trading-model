Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-09-14
Environment: Governance
Generated By: PMO Lead
Filed: 2026-09-14

# Lessons Learnt — Roadmap Rebalance

Feature / Trigger: N/A — scheduled rebalance (`run roadmap --reason "scheduled"`)
Run: 2026-09-14__scheduled
Reviewed by: PMO Lead
Date filed: 2026-09-14
Prior cycle checked: 2026-08-11__scheduled

---

## What worked well

- **The idea-intake backlog-scope overlap check (`idea_intake_prompt.md` v2.8, patched at `2026-07-27__scheduled`) caught a genuine duplicate live this window.** QA Lead's initially-planned "flaky-test triage SLA" topic was correctly identified as overlapping an existing gated backlog item and replaced with a materially different angle before submission — confirmed working exactly as designed, closing the loop on that Type A patch.
- **This cycle's idea intake independently surfaced a genuine, ungated pull-forward candidate (`BLG-FEAT-95`) at exactly the moment the Skill-Silo mandatory clause needed one**, without that being the window's stated purpose — a coincidental but useful convergence between routine idea generation and a governance-mandated need.
- **STEP 11.4 meta-review correctly resolved a 3-cycle-carried deferred patch (Six-Arc model vs. backlog-driven delivery) using both the standing patch and 2 independently-submitted Challenger ideas raising the same substantive question from a fresh angle** — the kind of convergent evidence the meta-review process is designed to wait for before acting, rather than acting on the first single data point.
- **STEP 8.1.5's §13-adjacent expiry review correctly kept surfacing a genuinely stale gate** (2 `rejected_but_strong.md` entries unresolved since 2026-03-04) rather than letting repeated soft-gate advisories substitute for an actual disposition — this cycle escalated it further by filing a backlog item (`BLG-GOV-329`) to force the question, rather than repeating the same non-blocking note a 5th time.

---

## Friction Log

---

### Friction Item 1

**Classification:** Type B — Semantic Mismatch: The same concept was named or interpreted differently across documents or roles

**Recurrence:** Not checkable (no prior cycle recorded a STEP 7.2 friction item — this is the first time STEP 7.2 has been recomputed after 2 cycles of being deliberately skipped as advisory-redundant, per `2026-08-11__scheduled`'s own carry-forward note).

**What happened:**
Recomputing STEP 7.2 (Cross-Role Workload Balance Check) required tallying `**Owner:**` fields across `sprint_backlog.md` for the last 3 shipped cycles (v9.1, v9.2, v9.3). The same underlying role is spelled inconsistently across these files — e.g. `QA Testing Owner`, `QA & Testing Owner`, and `QA Lead` all appear to refer to the same or overlapping roles; `Metrics Definitions & Analytics Owner` vs `Metrics Definitions & Analytics Canonical Owner`; `Backend Engineering Owner` vs `Backend Engineering Patterns Owner`. Additionally, raw per-owner counts summed to more than each changelog's own total story count for all 3 cycles checked (v9.1: 47 raw vs 41 stories; v9.2: 62 vs 56; v9.3: 33 vs 27), most likely because some stories list 2 owners and both were counted.

**Where in the routine:** STEP 7.2 — Cross-Role Workload Balance Check.

**Root cause:** naming inconsistency — no canonical enforced role-name enum exists for the `**Owner:**` field in `sprint_backlog.md` beyond "match a role defined in `claude/agents/`"; near-miss spellings and abbreviations are not caught at write time.

**Blast radius analysis:**
- What would have propagated: a workload-concentration reading that is directionally useful but not precisely reliable — this cycle's ~24% Head of Specs Team share is well clear of the 40% ceiling, so the imprecision did not change the outcome this time, but a borderline reading in a future cycle could tip a ceiling determination either way based on which spelling variant a story happened to use.
- When it would have surfaced: the next cycle where the rolling share sits close to 40% and the naming-inconsistency noise could change whether the advisory fires.
- Recovery cost if uncaught: low this cycle (finding not close to the threshold); potentially medium in a future close-call cycle (a wrongly-fired or wrongly-suppressed advisory).

**Process patch:**

→ Deferred patch (cannot apply this run):
  - File: `claude/system/shared_standards.md §16.11` (sprint_backlog.md Schema) or `sprint_planning_prompt.md`'s STEP that writes the `**Owner:**` field
  - Section: §16.11 Owner field definition
  - Change required: constrain the `**Owner:**` field to the exact role names enumerated in `claude/agents/*.md` (or a canonical alias table), and add a lint/check at sprint-backlog-write time flagging any non-matching spelling
  - Owner: Head of Specs Team
  - Target: next `sprint_planning_prompt.md` revision touching §16.11, or the next `2026-1[0-2]` scheduled rebalance if none occurs first

---

### Friction Item 2

**Classification:** Type C — Dependency Stall: A gate or pre-condition was invisible, ambiguous, or not enforced

**Recurrence:** Yes — appeared in 2026-08-11__scheduled

**What happened:**
The Skill-Silo mandatory-pull-forward clause (§7.1) requires naming ≥2 build-and-ship-shaped, ungated U-item candidates once the rolling 3-cycle average has worsened/remained unresolved for 3+ consecutive readings. This is the 2nd consecutive cycle the clause has fired (`2026-08-11__scheduled`, this cycle) and the 2nd consecutive cycle a full, exhaustive backlog search has found fewer than 2 genuinely qualifying candidates — 1 at the prior reading, **0** at this reading (that 1 candidate, `BLG-FEAT-32`, has since shipped and left the pool). The clause's own text provides an accept-with-rationale escape valve for exactly this situation, which the Product Owner used both times, but the clause has now been exercised at its designed limit twice running without the underlying condition (a genuinely near-empty ungated-U-item pool) changing.

**Where in the routine:** STEP 7.1 — Skill-Silo Alert, mandatory pull-forward response.

**Root cause:** dependency stall — the clause's remedy (name 2 ungated U-items) depends on a backlog condition (2+ such items existing) that this project's actual state (deep, multi-month debt-clearance mode, most `BLG-FEAT-*`/`BLG-FE-*` items gated on trade-volume thresholds far from being met) does not currently supply, and arguably cannot supply until the SI-02/Arc-5 data-density gates clear structurally.

**Blast radius analysis:**
- What would have propagated: if left unexamined, this could become a recurring pattern where the mandatory clause fires every cycle but is answered with the same accept-with-rationale response indefinitely, which risks the clause becoming pro forma rather than a genuine forcing function.
- When it would have surfaced: the next rebalance, if the same 0-candidate outcome recurs a 3rd consecutive time.
- Recovery cost if uncaught: low-medium — no incorrect decision was made (the PO's response is honest and well-reasoned both times), but a 3rd consecutive 0-candidate cycle without any structural discussion would be worth escalating harder than an accept-with-rationale note.

**Process patch:**

→ Deferred patch (cannot apply this run — this is a product-strategy question, not a mechanical file/section fix):
  - Escalated below under Escalations rather than filed as a deferred patch (no single file/section can resolve "should the ungated-U-item pool structurally change" — that is a scope/prioritisation decision, not a prompt-logic fix).

---

## Recurrence Escalations

| Friction item | First appeared | Prior outstanding action | Escalated to |
|---------------|-----------------|---------------------------|--------------|
| Friction Item 2 (Skill-Silo mandatory clause, 0-of-2 candidates) | 2026-08-11__scheduled (as 1-of-2) | PO named `BLG-FEAT-32` + funded `BLG-BE-91` (structural fix) — actioned and shipped (v8.6), but the alert itself persisted and the candidate shortfall worsened (1→0) rather than resolving | Head of Specs Team / Product Owner (see Escalations below) |

No deferred patch from a prior cycle has been carried 2+ cycles without a `prompt_change_log.md` entry — Friction Item 1 above is newly filed this cycle (not a carry-forward miss).

---

## Process improvements actioned this run

| File | Section | Change | Version | Prompt change log entry |
|------|---------|--------|---------|-------------------------|
| `claude/system/roadmap_prompt.md` | §2.3 | New standing operating-mode note resolving the Six-Arc-model-vs-backlog-driven-delivery divergence deferred patch (STEP 11.4 meta-review) | 9.19 → 9.20 | Yes — `prompt_change_log.md` 2026-09-14 |
| `claude/system/OPERATIONAL_GUIDE.md` | §6, §14 (×1), document header, Change Log | Source-prompt header and governance table synced for the bump above | 4.187 → 4.188 | Yes — `prompt_change_log.md` 2026-09-14 |

---

## New files created this run

- `claude/cycles/2026-09-14__scheduled/run_manifest.md`
- `claude/cycles/2026-09-14__scheduled/cycle_record.md`
- `claude/cycles/2026-09-14__scheduled/cycle_summary.md`
- `claude/cycles/2026-09-14__scheduled/meta_review.md`
- `claude/cycles/2026-09-14__scheduled/lessons_learnt.md` (this file)
- `claude/ideas/window_summary_IW-20260914-01.md`

---

## Outstanding deferred patches

| File | Section | Change required | Owner | Target | Carried since |
|------|---------|------------------|-------|--------|---------------|
| `shared_standards.md §16.11` / `sprint_planning_prompt.md` | Owner field definition | Constrain `sprint_backlog.md`'s `**Owner:**` field to an exact canonical role-name enum with a write-time lint | Head of Specs Team | Next `sprint_planning_prompt.md` revision touching §16.11 | 2026-09-14 (new) |

---

## Escalations

**Escalation 1 — Skill-Silo mandatory-pull-forward clause exercised at its designed limit for the 2nd consecutive cycle, with the underlying ungated-U-item pool worsening (1→0), not improving.** Not a governance-prompt defect — the clause is working exactly as designed (it does not mandate the impossible; it requires an honest accept-with-rationale response when no qualifying candidate exists, which the PO has correctly provided both times). Escalated as a **product-strategy** question, not a process-fix question: should the Product Owner/Head of Specs Team consider a more structural response than "accept and wait for `BLG-BE-91` to mature" if a 3rd consecutive cycle also finds 0 qualifying candidates? Owning authority: Head of Specs Team (process design) and Product Owner (scope decision). Target: re-assess at the next scheduled rebalance if the 0-candidate outcome recurs a 3rd time; otherwise no action needed (a recovery to ≥1 candidate resolves this on its own).

---

## Carry-Forward

Items: 2

| # | Observation | Implication | Engine |
|---|-------------|-------------|--------|
| 1 | The Skill-Silo mandatory-pull-forward clause has now found 0 qualifying ungated U-item candidates for the first time (down from 1 at the prior reading) — 2 consecutive cycles at or below the ≥2 requirement. | If a 3rd consecutive rebalance also finds 0-1 candidates, treat this as a structural product-strategy signal requiring more than an accept-with-rationale response, not merely a 3rd repetition of the same note. | Roadmap |
| 2 | `sprint_backlog.md`'s `**Owner:**` field spelling is inconsistent across cycles (confirmed across v9.1/v9.2/v9.3), which introduces imprecision into STEP 7.2's workload-share computation. | The next roadmap rebalance that recomputes STEP 7.2 should re-check whether the deferred canonicalisation patch (Friction Item 1) has landed; if not, apply the same consolidation-by-best-match method used this cycle and note it explicitly. | Roadmap |

```yaml
// ARTEFACT_STATUS
{
  "file": "lessons_learnt.md",
  "cycle_id": "2026-09-14__scheduled",
  "phase": "Roadmap",
  "filed_utc": "2026-09-14T12:30:00Z",
  "friction_item_count": 2,
  "action_now_count": 1,
  "deferred_count": 1,
  "escalation_count": 1,
  "overdue_patches": 0,
  "status": "Complete"
}
```
