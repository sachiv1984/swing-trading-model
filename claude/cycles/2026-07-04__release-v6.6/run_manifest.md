**Owner:** Infrastructure & Operations Documentation Owner
**Status:** Operational Record
**Deployment Version:** N/A
**Report Date:** 2026-07-04
**Environment:** Governance
**Generated By:** Claude Code (Release Planning Engine)
**Filed:** 2026-07-04

---

# Run Manifest — Release Planning v6.6

Cycle: `2026-07-04__release-v6.6`
Invocation: `plan release --version "v6.6"` (mode=standard [default], issues=none [default], auto-escalate=true [default], date=2026-07-04 [default: today])

**Note on cycle_id date:** Cycle folder was initially created as `2026-07-03__release-v6.6` during preflight (matching the date of the most recent `2026-07-03__scheduled` rebalance cycle). Corrected to `2026-07-04__release-v6.6` before any artefact was written, per §10: `date = --date or today (YYYY-MM-DD)`; no `--date` flag was supplied, so today's actual date (2026-07-04) governs. Folder was empty at time of rename — no drift.

---

## STEP -1 — Preflight Gate

| Sub-check | Result | Notes |
|-----------|--------|-------|
| -1.1 Required files present | PASS | team_charter.md, document_lifecycle_guide.md, strategy_rules.md, current_roadmap.md, backlog.md all present |
| -1.1 Issue creation prerequisite | N/A | `--issues` not specified (defaults to `none`); gh_issue_template.md check skipped |
| -1.2 Release exists on roadmap | PASS (via Option (b) record) | `current_roadmap.md` has no formal "v6.6" release section. §1 line 5 records: "v6.6 scoping deferred to plan release v6.6 (Option (b))" from rebalance cycle `2026-07-03__scheduled`. §-1.2 cleared via STEP 8.1 Option(b) decision from `2026-07-03__scheduled`. |
| -1.3/-1.4 Required roles + write test | PASS | All 9 required role agent files present in `claude/agents/` (23 total agent files); non-destructive write test at `claude/cycles/2026-07-04__release-v6.6/.write_test` succeeded and was removed |
| -1.5 Prior cycle lessons learnt closure (advisory) | Clear | `prior_cycle` (per `.claude_current_state.json`) = `2026-06-26__release-v6.3`; `lessons_learnt_closure.md` exists; 0 items marked action-now |
| -1.6 Post-ship precondition | PASS | `post_ship_complete=true`, `next_cycle_unblocked=true` on `.claude_current_state.json` |
| -1.7 Prompt change log integrity (advisory) | Clear | All Class 6 prompts' current versions confirmed present in `prompt_change_log.md`, including `release_planning_prompt.md` v2.40 (row dated 2026-07-03) — 1 non-blocking gap noted: `ideas_housekeeping_prompt.md` v1.0 has no change-log row (no prior version to log; not concerning, first-version file) |
| -1.8 Amendment in progress guard | PASS | `status = Closed`, not `Amendment_In_Progress` |
| -1.9 Stale backlog lock (advisory) | Clear | `claude/backlog/.lock` does not exist |

Lifecycle Guard: from-state `Closed` confirmed valid.

---

## STEP 0 — Run Manifest & State Initialization

**Carry-Forward Advisory (ST-15):** Most recently completed cycle = `2026-07-02__release-v6.5` (`post_ship_complete=true`, highest-dated). `lessons_learnt_closure.md` §Carry-Forward contains 3 items:

1. DF-16 (LP-01): STEP 4.1 / STEP 7 state-sync sequencing in `release_planning_prompt.md` remains unresolved — two alternative fixes proposed, needs a design decision. Head of Specs Team concern for a future prompt revision; not actionable within this release-planning run.
2. DF-17 (LP-04): v6.5 bundled 2 nominal U-items (BLG-FE-46, BLG-FEAT-41) to test correction of the rolling Skill-Silo Alert; only 1 of the 2 was actually classified U on review. The `2026-07-03__scheduled` rebalance already checked this (average worsened to 83.7%, 3rd consecutive worsening reading) and named pull-forward candidates **BLG-FE-82 + BLG-FEAT-52**, advising PO to commit **≥2 substantive U-items at v6.6**. Directly actionable — see STEP 2/3 scope rationale below.
3. DF-18: `/commit-check` pathspec-diff reinforcement patch still unapplied (`.claude/skills/` outside every routine's declared write scope). This is now a 1-cycle carry-forward (v6.5→v6.6); if still unapplied at v6.7 it becomes a 2-cycle carry-forward triggering an automatic recurrence escalation per `lessons_learnt_prompt.md` §3.7. Not in this engine's write scope — flagged for Head of Specs Team to apply directly, outside any single routine, before that threshold is reached.

Item 2 is directly actionable at this cycle (addressed in STEP 2/3 scope selection). Items 1 and 3 are out of this engine's write scope and are carried forward again in this cycle's own `lessons_learnt.md`.

**scored_initiatives.md Load (ST-14):** Loaded `claude/scoring/scored_initiatives.md` (Last Updated 2026-06-07, rebalance `2026-06-07__scheduled`). Checked for rows matching the named pull-forward candidates (BLG-FE-82, BLG-FEAT-52): 0 matching rows found. This file scores roadmap-level initiatives and pre-2026-06-07 backlog IDs only — no effort band data available for v6.6 candidate items at this stage. STEP 4.5 will use inline STEP 4 estimates for all v6.6 EPICs.

Artifacts created this step: `run_manifest.md`, `state.json` (status=Initialized).

---

## STEP 0 — Scope Rationale (Head of Specs Team / Product Owner, delegated authority)

`current_roadmap.md` §3 (Now horizon) reports Arc 1/Arc 2 fully complete; Arc 3–6 remainder items remain genuinely gated (per `2026-07-03__scheduled` rebalance outcome — SI-02 gate condition (1) trajectory estimate ~15–17/20 closed trades as of last official rebalance count, though user has separately reported 20 closed trades reached 2026-07-03; this requires formal PMO Lead re-verification via the production query before treating as cleared — see project SI-02 trade gate memory). No Now-horizon feature arc item is confirmed ready to enter firm scope this cycle without that re-verification.

v6.6 scope selection (finalized at STEP 2) will therefore prioritise:
- The two named pull-forward candidates **BLG-FE-82** and **BLG-FEAT-52** (per rebalance PO advisory: commit ≥2 substantive U-items to address the worsening Skill-Silo Alert).
- Any remaining audit/backlog debt items surfaced by `groom backlog` (2026-07-03 run: health=PASS, 0 orphans, 8 items archived as shipped in v6.5).
- SI-02 gate re-verification as a candidate story/task if Product Owner elects to action it formally this cycle (see gate proximity scan, STEP 1.4).

This continues to STEP 1 (Release Readiness Validation).

```yaml
# state.json update (STEP 0):
status: Initialized
artifacts.run_manifest: present
locks.backlog_lock.marker: "RP:v6.6:2026-07-04__release-v6.6"
locks.roadmap_lock.marker: "RA:v6.6:2026-07-04__release-v6.6"
```
