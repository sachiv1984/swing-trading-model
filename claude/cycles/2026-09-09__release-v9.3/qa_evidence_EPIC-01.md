Owner: Director of Quality
Class: Planning Document (Class 4)
Status: Active
Last Updated: 2026-09-09

---

## Consolidation Block

**EPIC:** EPIC-01 — Backend Reliability & Data Correctness Debt
**Cycle:** 2026-09-09__release-v9.3
**Sprint goal:** Clear a full-capacity, cross-category slate of 27 debt items — backend reliability/correctness, QA/test infrastructure, operations/cost monitoring, spec/documentation, and governance-process/security — exhausting the confirmed ~24–28 day capacity band at 27.50 days, with zero P1/P2 debt items deferred on capacity grounds this cycle.
**Test scenarios used:** `tests/test_screener_batch_service.py`, `tests/test_signal_write_path_consolidation.py`, `tests/test_signal_write_sanitization.py`, `tests/test_rebalance_exit_signal_numpy_regression.py`, `tests/test_correlation_id_propagation.py`, `tests/test_arc5_total_closed_trades_null_vs_zero.py`, `tests/e2e/arc5-compliance-section.spec.js`, `tests/test_system_status_endpoint_count.py`

| ST Item | Spec Reference | What was built | Acceptance criteria | Result | Deviations |
|---------|----------------|-----------------|---------------------|--------|------------|
| ST-01 | `docs/specs/api_contracts/screener_api_contract.md#GET /screener/history` | New `GET /screener/history` endpoint returning paginated screener run history, backed by the existing `screener_runs` table rather than a new duplicate table | `screener_run_history`-equivalent data created (run_id, run_timestamp, total_tickers, pass_count, regime_distribution JSON); `GET /screener/history` returns paginated run history; no backfill required | Pass | None |
| ST-02 | *(no prior canonical spec — `spec_reference_not_applicable: true`)* | Consolidated `create_signal()` and `create_rebalance_exit_signal()` into a single shared, validated `_signal_upsert()` helper, parameterised by each caller's distinct ON CONFLICT column set | The 3 signal write paths consolidated into a single validated path; regression coverage confirms no behavioural change across all 3 previously-separate paths | Pass | None |
| ST-03 | `docs/specs/api_contracts/backend_engineering_patterns.md#Correlation-ID request tracing` | Request-scoped correlation ID via `backend/utils/correlation_id.py` (contextvars) + a `logging.setLogRecordFactory()` override + `correlation_id_middleware` (registered outermost, covers 401s) | Correlation ID (middleware-generated or accepted via header) included in all log lines during a request; present for ≥2 representative multi-step endpoints; documented in `backend_engineering_patterns.md` | Pass | 2 Known Deviations filed in `structured_logging_standards.md` — see Known Deviations note below |
| ST-04 | `docs/specs/api_contracts/arc5_compliance_analytics.md#total_closed_trades` | `get_arc5_trade_plan_adherence_rate()` now returns `total_trades: None` on a `trade_history` schema error, `0` only for a genuine empty table | Missing/broken `trade_history` table no longer produces a `total_closed_trades` value indistinguishable from a genuine zero-trades portfolio; `arc5_compliance_analytics.md` updated; frontend low-trade-volume advisory updated to match if its rendering depends on this field | Pass | None |

**QA test coverage:**
- Scenarios run: `tests/test_screener_batch_service.py` (screener run history shaping, pagination, endpoint 200/400), `tests/test_signal_write_path_consolidation.py` (ON CONFLICT semantics preserved, parameter order unchanged), `tests/test_signal_write_sanitization.py` (pre-existing sanitisation regression, re-verified against the consolidated path), `tests/test_rebalance_exit_signal_numpy_regression.py` (updated for the new parameter shape, re-verified numpy-scalar leak protection), `tests/test_correlation_id_propagation.py` (context var, log record factory, 2 representative endpoints, 401-rejection coverage), `tests/test_arc5_total_closed_trades_null_vs_zero.py` (unit + endpoint-level null-vs-zero), `tests/e2e/arc5-compliance-section.spec.js` (SC-ARC5-10c, new), `tests/test_system_status_endpoint_count.py` (fallback-count/AST-derived-count consistency, both directions)
- Regression areas checked: signal write paths (all 3, including the previously-separate `update_signal` PATCH path), screener results/regime-distribution endpoints (unchanged, re-run), Arc5 compliance summary endpoint (all four other cards unaffected), backend app startup/middleware stack (full `test_api_contracts.py` suite — 57 tests — re-run clean), full backend suite (1383 passed, 10 skipped, 0 failed)
- Known deviations: None found — all stories' deviation checks completed with nothing to file, **except** ST-03, which filed 2 Known Deviations directly in `structured_logging_standards.md` per the Known Deviation Documentation Standard (`document_lifecycle_guide.md` §9):
  1. Backend log output remains plain-text, not the JSON Lines format that spec mandates — pre-existing gap, not introduced by ST-03. P3, backlog reference `BLG-BE-112`.
  2. The correlation-ID mechanism actually implemented (contextvars) differs from that spec's illustrative `request.state`-based sample, because service/database-layer log lines have no access to the `Request` object. P3, documentation-freshness note (no separate backlog item — folds into a future revision of that section).

**Agent-mediated Director of Quality review (execution_prompt.md §5.3):** A subagent reviewing on behalf of the Director of Quality against this role's charter (`claude/agents/director_of_quality.md`) examined the full diff (commit `e6845cbe0b53971b14c021cec141df7df812b218`), re-derived the SQL/parameter-order claims for ST-02 directly rather than trusting the commit message, cross-checked all 6 named documentation files for the claimed sections, and re-ran the full relevant test set (64 tests, all passing). Verdict: **Approved**, no blocking findings. Two informational notes only (this qa_evidence file did not yet exist at review time — expected, since it is created as the next step; and the commit message's "AST-accurate" phrasing for the endpoint-count fix is imprecise — the count was verified manually via a before/after diff of `test_cases`, not a scripted AST scan — the number itself, 118, is correct). The reviewer independently confirmed: ST-02's ON CONFLICT semantics are byte-for-byte preserved for both `create_signal` (never resets `status`) and `create_rebalance_exit_signal` (never overwrites sizing fields); the correlation-ID middleware ordering is correct (outermost, verified via the passing 401-rejection test); `Arc5ComplianceSection.js`'s existing `!= null` guard already covers the new null case with no frontend code change required; and the only `src/` file touched (`SystemStatus.js`) is a single-literal fallback-count change with no new rendering behaviour.

---

## Standard Sign-Off Block

- [x] All acceptance criteria verified against canonical spec
- [x] No unresolved P0 or P1 deviations
- [x] Regression areas checked
- [x] For any frontend component making direct URL construction (not via api.* wrapper): confirm the URL-base variable is exposed on the imported object — N/A, no frontend component touched beyond the SystemStatus.js literal
- Signed off by: Sprint Execution Engine (agent-mediated, Director of Quality role — §5.3)
- Date: 2026-09-09
- Comments: BLG-GOV-19 autonomous class is not available for this EPIC — `src/pages/SystemStatus.js` was touched (Criterion 3's detection rule per `execution_prompt.md` §3.2.A/BLG-GOV-135), even though the change is a single hardcoded-literal fallback correction with no new rendering behaviour and no story in this EPIC has an observable UI acceptance criterion. Agent-mediated Standard Sign-Off used instead, per §5.3. Full review findings recorded above. **This document's own DoQ block does not itself satisfy the STEP 4 merge gate's "QA sign-off" or "Product Owner acceptance" conditions** — those remain always-human per `execution_prompt.md` §5.3 and CLAUDE.md §2, and must be given by the Product Owner/QA reviewer directly on the pull request before merge. Items flagged by the reviewer for human double-check before merge: (1) `docs/ops/api_performance_baseline.md` §42's p50/p95 figures for `GET /screener/history` are estimates, explicitly flagged "Pending next baseline re-run"; (2) the two Known Deviations filed in `structured_logging_standards.md` are P3/backlog — worth a quick confirmation that priority is agreed, not because anything looked miscategorised.
