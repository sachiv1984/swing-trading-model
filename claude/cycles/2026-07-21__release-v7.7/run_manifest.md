Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-07-21
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-07-21

# Run Manifest — Release Planning — 2026-07-21__release-v7.7

## Invocation

`plan release --version "v7.7"` (user instruction: "ensure you use full capacity")

## STEP -1 Preflight

- -1.1 Required files present: `team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md` — all PASS.
- -1.2 Roadmap release exists: `current_roadmap.md` §3 carries a formal `### v7.7 — Strategy Intelligence Surfacing & Notification UX` section (formalized 2026-07-21, DL-074) with 7 named anchor items — PASS.
- -1.3/-1.4 Roles present (21 agent files in `claude/agents/`); write test at `claude/cycles/2026-07-21__release-v7.7/.write_test` — PASS, cleaned up.
- -1.5 Prior cycle lessons learnt closure check: most recently completed cycle = `2026-07-20__release-v7.6` (`post_ship_complete=true`). `lessons_learnt_closure.md` reviewed — 0 action-now items outstanding (2 action-now items this cycle both had matching `prompt_change_log.md` entries, confirmed at that closure's own STEP 8). No advisory gap.
- -1.6 Post-Ship Precondition: `post_ship_complete=true`, `next_cycle_unblocked=true` — PASS.
- -1.7 Prompt Change Log Integrity: not exhaustively re-audited this run (advisory only); no known gaps carried from v7.6 closure.
- -1.8 Amendment In Progress Guard: `.claude_current_state.json.active_amendment` is empty — PASS.
- -1.9 Stale Backlog Lock: `claude/backlog/.lock` and `claude/roadmap/.lock` both absent — no stale lock condition.

**Carry-Forward Advisory (ST-15):** 2 items reviewed from `2026-07-20__release-v7.6/lessons_learnt_closure.md`:
1. Release Planning should evaluate whether a bounded, governed "reopen with zero downstream consumption" path is worth adding, given 3 consecutive cycles needed an ad hoc PO bypass for same-session scope changes. Noted — no reopen is needed this cycle (fresh cycle, not touching a published one). Recommend Head of Specs Team pick this up as a process-improvement item independent of this release.
2. Empty-Now-horizon scope-selection direct-writes (DL-068/071/072/074) lack a distinct confirmation step from pure carry-forward relabels. Noted for Head of Specs Team awareness — out of this engine's write scope to action.

**scored_initiatives.md Load (ST-14):** File not present at `claude/scoring/scored_initiatives.md` — recorded "not present"; STEP 4 estimates used for all EPICs, no advisory required (Tier 3 per §16.7).

## STEP 1.4a — Perennial-Return Check

`BLG-FEAT-73` and `BLG-FEAT-74` are named on the roadmap's v7.7 anchor list but have never previously appeared in any `stage4_backlog_slice.md` (this is their first release-planning appearance) — the formal 2-consecutive-cycle Perennial-Return trigger does not technically fire. However, both carry an explicit `[gate status unverified/unmet]` LP-05 flag requiring live reconfirmation before entering scope. See STEP 1.4 Arc 4 sub-check below for the live re-check performed this session, and STEP 2 for the resulting PO disposition (both excluded from firm scope).

## STEP 1.4 — Gate-Condition Proximity Scan / Arc 4 Data Density Sub-Check

Live production API re-check performed 2026-07-21T08:49 UTC (`https://trading-assistant-api-c0f9.onrender.com`, authenticated via `RENDER_API_KEY`):

| Condition | Query | Result |
|---|---|---|
| SI-02 (1): ≥20 closed trades with linked trade_plans | `GET /trades` total_trades; `GET /trade-plans` position_id non-null count | `total_trades: 20`; trade-plans: 11 total, **0 linked** (position_id null on all 11) |
| SI-02 (2): behavioural-drift p99 < 2s stable | not re-spot-checked this session (no new evidence since 2026-07-12 spot-check of 1.1s) | Unconfirmed either way (unchanged) |
| SI-02 (3): drift scores meaningful | `GET /analytics/behavioural-drift` | `{"status":"insufficient_data","analysis_window_days":90,"trade_count_in_window":9,"metrics":[]}` — still failing |

**9th consecutive byte-identical reading** (2026-07-12 through 2026-07-21) — zero movement since `BLG-FE-109` ("Start Trade from Plan") shipped v7.3 (2026-07-16). `BLG-GOV-107` gate remains **NOT MET**. `BLG-FEAT-73` (SI-02 frontend build) cannot enter firm scope this cycle per its own LP-05 flag — see STEP 2 disposition.

`BLG-FEAT-74` (PO-05 Lightweight Replay Mode): no evidence found of a §13 determinism pre-clearance review having been run (grepped cycle records and `docs/specs/` — no PO-05-specific §13 clearance artefact exists). Effort (VH, >2 weeks) alone exceeds this release's realistic single-cycle capacity. Excluded from firm scope — see STEP 2 disposition.

## STEP 1.1 — Backlog Age Advisory

No spec/documentation debt items in this cycle's candidate scope have aged 2+ cycles without a story assignment among the items selected for firm scope. `BLG-GOV-28` (§13 compliance review) is itself an aged/overdue governance item (flagged overdue 2026-07-10, escalated P1 2026-07-12, un-actioned across 3 subsequent release cycles) — included in this cycle's firm scope specifically to close that aging gap.

## STEP 1.2 — Provisional-Target Advisory

7 items carry `Provisional-Target: v7.7` (the full roadmap anchor list). Of these, 5 proceed to firm scope this cycle (`BLG-FEAT-75`, `BLG-FE-114`, `BLG-FE-113`, `BLG-FE-120`, `BLG-FEAT-80`); 2 are excluded on gate grounds (`BLG-FEAT-73`, `BLG-FEAT-74` — remain `Provisional-Target: v7.7` in `backlog.md`, unresolved, pending gate clearance for a future cycle). An additional 6 items with `Provisional-Target: TBD` were pulled forward this cycle per explicit capacity-maximisation instruction (see Full-Capacity Fill Rationale below) and are now set to `v7.7`.

## Full-Capacity Fill Rationale (User Instruction: "ensure you use full capacity")

The 5 ready named anchor items total ~12.5 days midpoint effort against a confirmed capacity ceiling of ~24–28 working-day-equivalent per sprint (per `workforce_capacity.md`/DL-069 convention, consistent with v7.6's own capacity doc) — only ~45–50% utilisation, well short of "full capacity." Per standing feedback (fill remaining capacity with ready, well-scoped backlog items rather than accepting minimal scope, favouring genuine product/bug-fix/correctness value over pure process debt where a Product Value signal permits), the backlog was scanned for additional `Gate criteria: None`, ungated, S/M/XS-effort P1/P2 items with no blocking dependency. Current Product Value Ratio (0.39, v6.9–v7.3 window, last measured 2026-07-17) is Advisory, not Alert — moderate headroom exists for correctness/ops/QA-value fill items without materially worsening that signal.

Selected fill items (6):
- `BLG-OPS-108` (P1) — CI curl calls silently mask endpoint failures (the same defect class that let the numpy/signals crash run undetected for 3 weeks); stale since `Provisional-Target: v7.1` (3 releases overdue with no pickup).
- `BLG-GOV-28` (P1) — PT-04 §13 compliance review, overdue since 2026-07-10, escalated P1 2026-07-12, explicitly flagged in its own backlog text as "should be picked up as a priority item at the next `plan release` invocation" — direct match for this exact routine.
- `BLG-QA-104` (P2) — numpy-scalar regression coverage gap identified during the same PR #971 incident that produced `BLG-OPS-108`'s root cause; currently inspection-only, no automated guard.
- `BLG-BE-63` (P2) — nightly backtest job idempotency check, companion correctness item to the shipped `BLG-BE-59`/`BLG-BE-60` v7.1 fixes.
- `BLG-OPS-110` (P2) — nightly backtest job monitoring/alerting, same lineage as `BLG-BE-63`; closes the observability gap that let the v7.1 bug run undetected.
- `BLG-QA-102` (P2) — automates the `CLAUDE.md` §2 endpoint-count drift check, removing a standing manual-discipline process-deviation risk.

Not selected: pure governance/tooling process-debt items with no correctness or user-facing lineage (e.g. `BLG-GOV-240/241/243/246`, `BLG-SEC-*` cadence items, `BLG-SPEC-*` documentation-only items) — deprioritised in favour of the above correctness/QA cluster, consistent with the standing guidance to avoid padding scope with process debt when better-value ready items are available.

**Resulting scope: 11 EPICs, ~19.5 days midpoint effort (range ~16–24 days) against ~24–28 day capacity ≈ 70–81% utilisation.** See STEP 4.5 Capacity Check for the full breakdown.

## Escalations

None raised this cycle. `open_escalations = {}`.
