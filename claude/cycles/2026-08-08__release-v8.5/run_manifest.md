Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-08-08
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-08-08

# Run Manifest — Release Planning v8.5

Invocation: `plan release --version "v8.5"` (user instruction, translated: "Use fully capacity, focus on user features first" → `--capacity "full capacity"`, scope prioritisation instruction). `--mode` defaulted to `standard`; `--issues` defaulted to `none`; `--auto-escalate` defaulted to `true`.

---

## STEP -1 — Preflight Gate

### -1.1 Common Preflight — Required Files Present
All present: `team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md`. `gh_issue_template.md` not required this run (`--issues` not specified, defaults to `none`).

### -1.2 Verify Release Exists on the Roadmap
No formal `## v8.5` roadmap section exists. Checked `2026-07-28__scheduled` rebalance record: STEP 8.1 fired (Now horizon fully empty) — Option (b): defer again, no newly-ready anchor content that cycle. This same Option (b) equivalence has been relied on by every release since `v8.0` (`v8.0`, `v8.1`, `v8.2`, `v8.3`, `v8.4`, now `v8.5`). §-1.2 cleared via STEP 8.1 Option (b) decision from `2026-07-28__scheduled`.

### -1.3/-1.4 Common Preflight — Roles and Write Test
23 agent files present in `claude/agents/` covering all Section 6 required roles. Write test (`claude/cycles/2026-08-08__release-v8.5/.write_test`) created and removed successfully.

### -1.5 Prior Cycle Lessons Learnt Closure Check
`prior_cycle` (per `.claude_current_state.json`) = `2026-08-05__release-v8.3`. `claude/cycles/2026-08-05__release-v8.3/lessons_learnt_closure.md` exists; `action_now_count: 0`. No advisory required.

### -1.6 Post-Ship Precondition Check
`post_ship_complete = true`, `next_cycle_unblocked = true` (per `.claude_current_state.json`, both set at `2026-08-07__release-v8.4`'s post-ship closure, `last_post_ship_utc: 2026-08-08T14:15:00Z`). Pass.

### -1.7 Prompt Change Log Integrity Check
`release_planning_prompt.md` current version `v2.48` — confirmed present in `prompt_change_log.md` (entry dated 2026-08-08, ST-29/BLG-GOV-286). No advisory required.

### -1.8 Amendment In Progress Guard
`.claude_current_state.json.status = "Closed"` — not `Amendment_In_Progress`. Pass.

### -1.9 Stale Backlog Lock Preflight Check
`claude/backlog/.lock` does not exist. No action required.

---

## STEP 0 — Run Manifest + State Init

**Carry-Forward Advisory (ST-15):** Most recently completed cycle with `post_ship_complete = true` is `2026-08-07__release-v8.4`. Its `lessons_learnt_closure.md` has no `## Carry-Forward` section with open items relevant to Release Planning (the one Carry-Forward item recorded — ST-29 gate-detection-script coverage — is a Sprint Execution-scoped confirmation, already closed by ST-29 shipping in `v8.4`). No carry-forward items apply to this cycle.

**scored_initiatives.md Load (ST-14):** Loaded — 0 active roadmap initiatives (CPS=N/A, unchanged since 2026-07-01__scheduled); no matching rows for this cycle's 6 EPICs (all backlog-driven). Falling back to inline STEP 4 estimates throughout; no advisory required.

`run_manifest.md` created. `state.json` initialized: `status = Initialized`, `mode = standard`, `issues_mode = none`, `auto_escalate = true`.

---

## STEP 1 — Release Readiness Validation

### 1.1 Backlog Age Advisory
Spec/documentation debt items (`BLG-SPEC-*`) appearing 2+ cycles without a story assignment: not scanned as blocking — this cycle's scope contains no `BLG-SPEC-*` items (none made the ready/priority cut this cycle; the largest ready `BLG-SPEC-*` items remain P3 governance-tooling documentation, deferred in favour of the frontend-first selection). No hard finding; advisory not triggered against in-scope items.

### 1.2 Provisional-Target Advisory
1 item (`BLG-FE-143`) carries `Provisional-Target: v8.5` — horizon-planned for this release, included in scope (`ST-07`). 1 item (`BLG-QA-105`) carries a stale `Provisional-Target: v7.1` (long since passed) — included on priority merit (P2, test-pollution correctness fix), stale field noted but not corrected here (out of Release Planning's write scope to touch fields outside the release-slice mechanism). All other 23 in-scope items carry `TBD`/`Unscheduled` — no matching horizon signal.

### 1.3 Design-Gate Language Scan
Multiple in-scope items carry observable UI acceptance criteria (visible rendering, colour, layout) — see STEP 4.1 below for the full design-gate classification.

### 1.3a Gate-Detection Procedure (Scripted)
Ran `python3 scripts/scan_backlog_gate_conditions.py`: 269 backlog items scanned, 191 items carry a formal Gate field (of which a substantial subset are `None`-valued or explicitly cleared, and therefore not actually blocking), 5 data-quality warnings (embedded gate language in `Provisional-Target` with no formal Gate field: `BLG-FEAT-74`, `BLG-FEAT-76`, `BLG-SPEC-56`, `BLG-SPEC-57`, `BLG-QA-59`).

**Self-caught 5th failure mode this cycle:** `BLG-FEAT-73` carries gate-blocking language ("gate status unverified/unmet", "NOT MET") inside its `Provisional-Target` field, inside square brackets rather than the parentheses the script's `EMBEDDED_GATE_SIGNAL_RE` pattern requires (`\(.*(gate|gated|...)*\)`) — so it is neither caught as formally gated (no `Gate criteria`/`Gate`/`Gate date` field present) nor flagged as a data-quality warning by the script. Confirmed gate-blocked by manual reading of the full item (consistent with every prior release's treatment of this item since `v7.7`) and excluded from scope on that basis, not the script's output alone. Filed as a follow-up backlog item (`BLG-GOV-292`, see STEP 2 below) to extend `EMBEDDED_GATE_SIGNAL_RE` to also match bracket-delimited gate language, per this repo's standing self-caught-scan-gap pattern (`BLG-GOV-286` precedent).

A locally-built candidate-ranking script (parsing Priority/Type/Effort/Provisional-Target across all 269 items, cross-referenced against the scripted gate scan above) was used to build the ready-item pool for STEP 2 below — see that section for the resulting selection.

### 1.4a Perennial-Return Check
`BLG-FEAT-73`/`BLG-FEAT-74` remain excluded (gate-blocked). Both were formally parked at `2026-08-05__release-v8.3` (STEP 1.4a.1 mandatory sunset trigger fired that cycle; PO chose Option (b) — remove from horizon). Neither is in `returned_to_backlog` status from `v8.4` (they were not selected into `v8.4` scope to begin with, consistent with the park disposition), so no fresh Perennial-Return Check disposition is required this cycle — same treatment as `v8.4`.

### 1.4a.1 Sunset Criteria
No new sunset trigger this cycle — both items already parked (see above). No action required.

### 1.4b Within-Sprint Date Gate Classification
No in-scope item carries a within-sprint date gate. `BLG-SEC-10` and `BLG-FEAT-29` both had gate conditions that cleared *before* this cycle began (2026-08-08, confirmed by the backlog's own strikethrough "Gate cleared" annotations, dated 2026-08-08 during `v8.4`'s post-ship/groom activity) — both are firm, not conditional, in this cycle's scope.

### 1.4 Gate-Condition Proximity Scan
Not run in full detail this cycle — no PO-02/PO-04/SI-02 data-density-relevant item is in this cycle's scope. Arc 4/Arc 5 gate proximity unchanged from `v8.4`'s last recorded figures (SI-02: 0/11 linked trade plans, NOT MET; PO-02/PO-04: no material change expected over a single-cycle window).

```yaml
# state.json update (STEP 1):
artifacts.stage1_readiness: pass
```

---

## STEP 2 — Scope Extraction

**Method:** Built a full ready-item pool from `claude/backlog/backlog.md` (269 items) by combining the STEP 1.3a scripted gate scan with a field-parser script (Priority/Type/Effort/Provisional-Target extraction, handling both the multi-line and condensed pipe-delimited backlog entry formats). Excluded genuinely gate-blocked items (`BLG-FEAT-73`/`74`/`76`, `BLG-SPEC-56`/`57`, `BLG-QA-59` — all carrying unmet gate language even though 3 of these 6 lack a formal `Gate criteria` field, per the self-caught finding in §1.3a above) and 1 stale-closed duplicate (`BLG-GOV-105`, marked "✅ CLOSED (confirmed duplicate)" in its own body — not re-selected). Resulting ready pool: 144 items.

**User-feature-priority instruction:** The ready pool's `Product Feature`-type items are `BLG-FEAT-29` (regime distribution metric — small analytics view) and `BLG-FEAT-72` (Product Value Ratio trend chart — internal governance tooling, not user-facing). **0 genuinely new user-facing feature-build items exist in the ready pool this cycle.** All roadmap-flagship features (`BLG-FEAT-73`/`74`/`76`) remain gate-blocked. Given this, scope selection led with the full ready `BLG-FE-*` (frontend) pool — 15 items covering correctness fixes, design-consistency audits, and UX review/documentation — as the closest available honouring of "focus on user features first": these are the items most directly touching what users see and interact with, even though none is a net-new capability. `BLG-FEAT-29`/`72` included alongside as the pool's only `Product Feature`-typed items.

**Capacity-priority instruction ("use fully capacity"):** After the frontend-first tranche (17 items, 19.9 days) and 2 P1 production-correctness bugs (2.0 days) — 21.9 days — filled the remaining ~5.25 days of the ~24-28 day band with the highest-priority remaining P2 items (security hardening: `BLG-SEC-10`/`15`/`16`; test-pollution correctness: `BLG-QA-105`; 2 cheap self-caught governance-process fixes: `BLG-GOV-288`/`289`, XS effort, 0.5 days combined). Final scope: 25 items, ~27.15 days — top of the confirmed ~24-28 day band (~97-113% utilisation depending on which band edge is used as denominator), consistent with the explicit "full capacity" instruction.

**Deliberately not selected:** ~119 remaining ready P3 items (mostly `BLG-GOV-*` governance-process/checklist items, 58 of the 144-item ready pool) — capacity reached; this cycle already carries 2 governance-process items (8% of scope), consistent with the Skill-Silo rotation guideline's execution-heavy preference (see STEP 3 below).

**Follow-up filed this cycle:** `BLG-GOV-292` — extend `scan_backlog_gate_conditions.py`'s `EMBEDDED_GATE_SIGNAL_RE` to also match bracket-delimited gate language (`[gate status unverified/unmet]`), per the `BLG-FEAT-73` finding in §1.3a above. Filed via the `backlog-add` skill after this release plan's publish (see repo's standing self-caught-scan-gap pattern, `BLG-GOV-286` precedent) — not itself part of this cycle's scope (found too late in the session to size/sequence into an already-capacity-full plan; candidate for `v8.6`).

```yaml
# state.json update (STEP 2):
artifacts.stage2_scope_extraction: pass
artifacts.stage2_scope_document: present
```

---

## STEP 3 — Execution Plan + Decisions Record

6 EPICs, 25 stories. See `release_plan.md §Execution Plan` for the full table and Risk Register.

**Skill-Silo mitigation note:** Most recent rebalance (`2026-07-28__scheduled`) reported a 2nd consecutive worsening Skill-Silo reading (rolling-3-cycle avg 65.8%, v7.7/v7.8/v7.9). `v8.4` already applied the execution-heavy rotation guideline (2 of 31 stories governance-process-shaped). This cycle continues that weighting: 2 of 25 stories (`BLG-GOV-288`/`289`, 8%) are governance-process-shaped; the remainder is frontend/security/backend correctness and analytics scope. No mandatory pull-forward triggered (alert has not reached 3 consecutive worsening readings as of the last rebalance).

```yaml
# state.json update (STEP 3):
artifacts.stage3_execution_plan: pass
artifacts.stage3_decisions_record: present
attributes.plan_structured: true
status: Planning
```

---

## STEP 3.5 — Local Model Integrity Check
Pass — see `release_plan.md`. No story touches AI model routing or prompt templates.

## STEP 3.9 — Shared Write Lock Preflight
`claude/backlog/.lock` did not exist — created fresh: `{cycle_id: "2026-08-08__release-v8.5", release: "v8.5", acquired_utc: "2026-08-08T18:00:00Z", acquired_by: "Release Planning Engine"}`.

```yaml
locks.backlog_lock: { owned: true, owner_cycle_id: "2026-08-08__release-v8.5", owner_release: "v8.5", acquired_utc: "2026-08-08T18:00:00Z", status: acquired, marker: "RP:v8.5:2026-08-08__release-v8.5" }
artifacts.backlog_lock: acquired
```

---

## STEP 4 — Backlog Slice
`stage4_backlog_slice.md` and `stage4_issue_manifest.json` written (25 stories, ST-01–ST-25, no gaps). `claude/backlog/backlog.md` updated: 25 items' `Provisional-Target` fields set `TBD`/`Unscheduled` → `v8.5`; `## v8.5 Release Slice` section appended with the release-plan-marker.

```yaml
artifacts.stage4_backlog_slice: pass
artifacts.stage4_issue_manifest: pass
attributes.backlog_committed: true
attributes.design_gate_required: true
status: Committed
```

### STEP 4.1 — Design Gate Classification
`design_gate_required = true`. Items with observable UI acceptance criteria (visible rendering, colour, layout, or explicit CLAUDE.md frontend-visible-change-rule reference): `ST-06` (muted-colour class fix — visible text/background rendering), `ST-08` (P&L colour convention reconciliation — visible colour), `ST-10` (empty-state pattern applied to 3+ pages — visible), `ST-12` (mobile responsive fixes — visible layout), `ST-13` (contrast audit follow-up — visible colour), `ST-19` (chart CSP rework, conditional on adoption — visible if triggered). At minimum `ST-06` and `ST-08` unconditionally require Playwright coverage or staging sign-off per their own ACs.

⚠ **DESIGN GATE REQUIRED before `plan sprint`** — run: `run design-gate --cycle 2026-08-08__release-v8.5`

**Note (LP-01 fix, v2.41):** `design_gate_required`/`design_gate_status` not written to `.claude_current_state.json` at this step — deferred to STEP 7's intermediate sync, atomic with `active_cycle` advancement.

### STEP 4 Postcondition
`claude/backlog/.lock` removed after transaction commit.

```yaml
locks.backlog_lock.status: released
artifacts.backlog_lock: released
```

---

## STEP 4.5 — Capacity Feasibility Sense Check
Pass — ~27.15 days against the ~24-28 day band, top of band. See `release_plan.md §Capacity Check`.

## STEP 5 — Roadmap Annotation
`claude/roadmap/current_roadmap.md` §1 (Current Version) annotated with execution notes under `**Next planned release:**` (no formal `## v8.5` section exists to annotate under, per the Option (b) equivalence). `claude/roadmap/.lock` acquired, committed, released — no conflicting owner found.

```yaml
artifacts.roadmap_txn: committed
locks.roadmap_lock.status: released
```

## STEP 5.5 — Integrity Validation
Pass — see `release_plan.md §Integrity Validation`.

## STEP 5.7 — Decision Record Integrity
`not_applicable` — 0 escalations raised this cycle.

---

## STEP 7 — Cycle Summary
See `cycle_summary.md`. No High-priority risk carries a "must resolve before sprint planning seal" disposition — no `## Pre-sprint Planning Required Decisions` section required.

## STEP 8 — Lessons Learnt
See `lessons_learnt.md`.

## STEP 9 — Global State Synchronization
`.claude_current_state.json` terminal update: `active_cycle = "2026-08-08__release-v8.5"`, `status = "Release_Planning_Complete"`, `next_release = "v8.5"` (authoritative write, OA-1).

## STEP 10 — Stage, Commit & Push
`--issues` defaulted to `none` — no issue import/gh automation this cycle.

---

## Escalations
None raised this cycle — 0 hard/conditional gate blockers encountered.

## Publish Gate
All conditions met — see `release_plan.md`. `status = Validated`, `publish_eligible = true`.
