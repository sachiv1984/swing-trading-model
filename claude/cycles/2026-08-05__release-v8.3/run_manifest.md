Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-08-05
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-08-05

# Run Manifest — 2026-08-05__release-v8.3

Invocation: `plan release --v8.3` — treated as `plan release --version "v8.3"` (no `--date` given, defaults to today 2026-08-05; no `--timebox`/`--capacity` given, defaults empty — standing ~24-28 day/sprint band from `workforce_capacity.md` used as the reference ceiling), `--mode standard` (unspecified, default), `--issues none` (unspecified, default — `sync gh` remains a separate, later command per CLAUDE.md §4), `--auto-escalate true` (default). No explicit user scope-priority instruction this session (unlike `v8.1`/`v8.2`'s "full capacity, user features first") — scope selection made under Product Owner delegated authority per §2.

## STEP -1 — Preflight

**-1.1 Required files present:** PASS — `team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md` all present. `gh_issue_template.md` present (not required this run — `--issues none`).

**Required roles present:** PASS — 23 agent files found in `claude/agents/`, covering all 9 required roles (Product Owner, Head of Specs Team, PMO Lead, Director of Quality, Infrastructure & Operations Owner, Strategy Rules & System Intent Owner, FinOps & Resource Architect, Facilitator, Challenger).

**Write permission test:** PASS — `.write_test` created and removed in cycle folder.

**-1.2 Release exists on roadmap:** No formal `## v8.3` roadmap section exists (`current_roadmap.md` §1: "Next planned release: [TBD]"). Checked most recent rebalance (`2026-07-28__scheduled`) for a documented STEP 8.1 Option (b) decision: found — "STEP 8.1 fired (condition 1a — Now horizon fully empty) — Option (b): defer again, no newly-ready anchor content this cycle." This is the same Option (b) record already relied on by `2026-07-30__release-v8.0`, `2026-08-03__release-v8.1`, and `2026-08-04__release-v8.2`. No `run roadmap` invocation has occurred since. §-1.2 cleared via STEP 8.1 Option (b) decision from `2026-07-28__scheduled` — this is the 4th consecutive release plan to rely on this same record.

**-1.3/-1.4 Roles + write test:** PASS (see above).

**-1.5 Prior cycle lessons learnt closure check (advisory):** `.claude_current_state.json.prior_cycle` = `2026-08-03__release-v8.1`. `claude/cycles/2026-08-03__release-v8.1/lessons_learnt_closure.md` exists. Its one "Process improvements actioned this run" row (`delivery_verification_prompt.md` v3.6→v3.7, LL-v8.1-P4-01) shows `prompt_change_log.md` entry = Yes, confirmed present. No Friction Item in that file carries an explicit `action: now` disposition without a matching change-log row (Friction Item 1 there is an explicitly *deferred* patch, not action-now). No gap found.

**-1.6 Post-ship precondition (hard gate):** PASS — `post_ship_complete = true`, `next_cycle_unblocked = true` in `.claude_current_state.json` (both set at `2026-08-04__release-v8.2` closure, `last_post_ship_utc = 2026-08-05T08:35:00Z`).

**-1.7 Prompt change log integrity (advisory):** Checked current version of all 11 Class 6 prompts against `prompt_change_log.md` — all 11 have a matching `vOLD→vCURRENT` row present (`release_planning_prompt.md` v2.46, `sprint_planning_prompt.md` v3.15, `execution_prompt.md` v3.62, `delivery_verification_prompt.md` v3.7, `post_ship_closure.md` v2.24, `design_gate_prompt.md` v1.9, `amendment_cycle_prompt.md` v1.9, `roadmap_management_prompt.md` v1.4, `backlog_management_prompt.md` v1.13, `roadmap_prompt.md` v9.12, `gh_issue_template.md` v1.0). No gap.

**-1.8 Amendment in-progress guard:** PASS — `active_amendment = ""`, `amendment_status = ""`.

**-1.9 Stale backlog lock check:** PASS — `claude/backlog/.lock` does not exist. `claude/roadmap/.lock` does not exist.

## Carry-Forward Advisory (ST-15)

Most recently completed cycle: `2026-08-04__release-v8.2` (`post_ship_complete = true`, `last_post_ship_cycle`). `lessons_learnt_closure.md` for that cycle carries no `## Carry-Forward` section header as such, but records a "Recurrence Escalation 1" (Release Planning's ungated-candidate scan gap, 3 consecutive self-caught misses) which was actioned directly in the very next commit before this session began (`4254e8c0`, Head of Specs Team review): `BLG-GOV-286` filed (P1, canonical scripted gate-detection procedure, scoped for a future sprint story, not yet implemented) and `BLG-OPS-48`'s malformed duplicate `Provisional-Target` field fixed directly. Noted for this session's own STEP 1/STEP 2 scan: `BLG-GOV-286`'s prompt patch has not shipped yet, so the same manual-scan risk it targets is still live this cycle — see Scope Selection Rationale below for a self-caught instance of exactly this class of miss, found during this session's own full-text verification pass.

## scored_initiatives.md Load (ST-14)

`claude/scoring/scored_initiatives.md` last updated `2026-07-27__scheduled`; 0 populated initiative rows (Debate Queue was empty that cycle). No rows match any item selected into this cycle's scope (all 27 selected items are backlog-driven `BLG-*` items, not roadmap-scored initiatives). Tier 3 applies uniformly (§16.7) — STEP 4 inline estimates used for all 27 items, no advisory required.

## Scope Selection Rationale (Product Owner judgment, this session)

No explicit user scope-priority instruction was given this session (contrast `v8.1`/`v8.2`, which both had explicit "use full capacity" / "focus on user features" direction). Scope selected under Product Owner delegated authority per §2, using the same full-text ungated-candidate methodology as recent cycles, applied to the current 297-item backlog.

**Ungated-candidate scan:** Full-text scan (not field-label-only) of all 297 backlog headings for `**Gate criteria:**`, `**Gate date:**`, `**Gate:**`, `gate-conditional` type tags, and gate language embedded in `**Provisional-Target:**` field text (the exact class of miss named in `BLG-GOV-286`). 100 items initially scored ungated by an automated field-label pass; two known malformed-gate items (`BLG-FEAT-73`, `BLG-FEAT-74` — gate status embedded in `Provisional-Target` text) were correctly excluded by cross-referencing the still-open Recurrence Escalation context rather than the scan alone.

**Self-caught scan miss this session (same class as `BLG-OPS-48` at `v8.0`–`v8.2`):** `BLG-GOV-74` (AI feature usage quarterly review) was initially included in a draft P2 candidate list — the automated pass found no `**Gate criteria:**` field and no `gate-conditional` tag. A subsequent full-text read (specifically grepping every candidate's block for the substring "gate", prompted by awareness that `BLG-GOV-286` exists precisely because this class of miss keeps recurring) found a standalone `**Gate date:** First review due 2026-08-29` field — a variant field name the automated pass did not check for. `2026-08-29` (~24 days from this cycle's date) is well outside this release's expected execution window (recent cycles have shipped within 1-4 days of their `plan release` date). `BLG-GOV-74` was removed from scope before the backlog write was finalised — not committed to `stage4_backlog_slice.md` or `backlog.md`. This is the 4th consecutive Release Planning cycle (`v8.0`, `v8.1`, `v8.2`, `v8.3`) with a related self-caught ungated-candidate scan miss, each a distinct field-name/embedding variant. `BLG-GOV-286` (filed 2026-08-05, prior commit) already exists as the tracking item for the systemic fix and has not yet been implemented (scoped for a future sprint) — no new tracking item is filed here; this instance is additional evidence supporting `BLG-GOV-286`'s priority, recorded in `lessons_learnt.md` below rather than re-filed.

**STEP 1.4a Perennial-Return Check + STEP 1.4a.1 Sunset Criteria — mandatory disposition this cycle:** `BLG-FEAT-73` (SI-02 Behavioural Drift Detection frontend) and `BLG-FEAT-74` (PO-05 Lightweight Replay Mode) have now been excluded under Option (a) "keep as conditional" at 4 consecutive cycles: `v8.0` (1st), `v8.1` (2nd), `v8.2` (3rd, explicitly flagged there as "one cycle short of the mandatory sunset trigger... forced disposition at v8.3"), and this cycle would be the 4th if Option (a) were chosen again. Per §1.4a.1, the disposition escalates from advisory to **mandatory** at exactly this count. No materially new gate-clearance path exists for either item: `BLG-FEAT-73`'s SI-02 gate (≥20 linked trade plans) remains unchanged since the last live re-check (`2026-07-28__release-v7.9` EPIC-08: 20 total / 0 linked); `BLG-FEAT-74`'s §13 pre-clearance has still not been run and its VH effort (>2 weeks) still exceeds single-cycle sizing. Neither condition has moved since `v8.1`'s or `v8.2`'s assessment — no concrete forthcoming event or date-bound trigger exists to cite as "materially new" per §1.4a.1's definition. **Product Owner disposition: Option (b) — Remove from horizon, park until gate permanently cleared.** Both items remain in `claude/backlog/backlog.md` (not deleted — Release Planning's write scope does not extend to editing their entry text) but are formally out of active-horizon consideration; they will not be treated as recurring Perennial-Return candidates at future cycles unless a genuinely new gate-clearance path is documented at that time. The Arc 5 UX-prep cluster (`BLG-FEAT-44/56`, `BLG-FE-43/45/54/58/59/62/63/68/69/70/71`) — dependent on `BLG-FEAT-73`'s SI-02 UX surface — is excluded on the same basis; the Sunset Criteria retroactive assessment (§1.4a.1, v2.46) only tracked `BLG-FEAT-73`/`BLG-FEAT-74` explicitly, so no separate 4-consecutive count is claimed for the cluster itself, but its own dependency on the now-parked item means it remains correctly excluded regardless.

**STEP 1.4b Within-sprint date gate check:** `BLG-FEAT-45` (Monthly P&L report format review — 3-month usage retrospective) carries `**Gate criteria:** ≥ 2026-08-05`. Today's date (2026-08-05) meets this condition exactly. Per the promotion path ("gate owner explicitly confirms the gate condition has been met — dated confirmation recorded"): the gate is a simple objective date comparison (3+ months since the 2026-05-05 ship date), confirmed met as of this session (2026-08-05) under Product Owner delegated authority, recorded here as the dated confirmation. Promoted from conditional → firm scope, included as EPIC-06's sole item. No other candidate carries a within-sprint date gate.

**Full-capacity fill:** Remaining scope filled with the highest-priority ready (ungated) backlog items, led by the two P1 operational items (`BLG-OPS-129`, `BLG-OPS-130` — SI-05 digest pipeline root-cause fix and alerting, both filed as direct outcomes of `v8.2`'s own EPIC-03/ST-08 effectiveness review) and one P2 (`BLG-OPS-131`, also a `v8.2` EPIC-02/ST-06 finding), then a curated, thematically-grouped selection of P3 backend-reliability, frontend/design-system, QA/spec-debt, and governance-process items — deliberately avoiding a repeat of `v8.2`'s own self-flagged `RISK-03` (governance cluster at ~44% of scope); this cycle's governance EPIC (EPIC-05) is 5 of 27 items (~19% by count, ~22% by effort), balanced against comparably-sized Backend (EPIC-02), Frontend (EPIC-03), and QA/Spec (EPIC-04) clusters. No ungated P1/P2 U-shaped (build-and-ship product feature) item exists in the backlog this cycle — the only P3-or-better user-facing item found, `BLG-FEAT-45`, is a lightweight retrospective-review item (S effort), not new build scope; this is unchanged from the "no ungated P1/P2 U-item" finding at every rebalance since `2026-07-27__scheduled`. See `release_plan.md §Scope` for the full item table.

## Preflight Outcome

All hard gates PASS. Proceeding to STEP 0 state initialisation.
