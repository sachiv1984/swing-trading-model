Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-08-04
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-08-04

# Run Manifest — 2026-08-04__release-v8.2

Invocation: `plan release --v8.2. Use full sprint capacity. Focus on user features first.` — treated as `plan release --version "v8.2" --capacity "full sprint capacity"`, `--mode standard` (unspecified, default), `--issues none` (unspecified, default — `sync gh` remains a separate, later command per CLAUDE.md §4), `--auto-escalate true` (default).

## STEP -1 — Preflight

**-1.1 Required files present:** PASS — `team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md` all present. `gh_issue_template.md` present (not required this run — `--issues none`).

**Required roles present:** PASS — all 9 required agent files found in `claude/agents/`: Product Owner, Head of Specs Team, PMO Lead, Director of Quality, Infrastructure & Operations Owner, Strategy Rules & System Intent Owner, FinOps & Resource Architect, Facilitator, Challenger.

**Write permission test:** PASS — `.write_test` created and removed in cycle folder.

**-1.2 Release exists on roadmap:** No formal `## v8.2` roadmap section exists (`current_roadmap.md` §1: "Next planned release: [TBD]"). Checked most recent rebalance (`2026-07-28__scheduled`) for a documented STEP 8.1 Option (b) decision: found — "STEP 8.1 fired (condition 1a — Now horizon fully empty) — Option (b): defer again, no newly-ready anchor content this cycle." This is the same Option (b) record already relied on by `2026-07-30__release-v8.0` and `2026-08-03__release-v8.1` planning. No `run roadmap` invocation has occurred since (no newer rebalance exists to supersede it), and its "Now horizon empty, no anchor scope" finding is still accurate (§3 unchanged since 2026-07-27 manage-roadmap retirement). §-1.2 cleared via STEP 8.1 Option (b) decision from `2026-07-28__scheduled` — this is the 3rd consecutive release plan to rely on this same record.

**-1.3/-1.4 Roles + write test:** PASS (see above).

**-1.5 Prior cycle lessons learnt closure check (advisory):** `.claude_current_state.json.prior_cycle` = `2026-07-21__release-v7.7` — this field remains stale (4 releases behind the actual chain: v7.8, v7.9, v7.10, v8.0, v8.1 have all shipped and closed since). This is the same staleness already flagged as an advisory at `2026-08-03__release-v8.1`'s own `run_manifest.md`; the field was not corrected by that cycle's post-ship closure either. ⚠ Advisory recorded again: `prior_cycle` field is stale and should be corrected by the owning engine (Post-Ship Closure) at next opportunity — not in this engine's write scope to fix. This is now a 2nd consecutive Release Planning cycle to flag the same unfixed staleness — worth a `groom backlog`/`run roadmap` session filing a tracking item if it recurs a 3rd time.

**-1.6 Post-ship precondition (hard gate):** PASS — `post_ship_complete = true`, `next_cycle_unblocked = true` in `.claude_current_state.json` (both set at `2026-08-03__release-v8.1` closure).

**-1.7 Prompt change log integrity (advisory):** `release_planning_prompt.md` current version v2.46 confirmed present in `prompt_change_log.md` (2026-08-03 entry, ST-03/ST-07). No gap for this engine's own prompt.

**-1.8 Amendment in-progress guard:** PASS — `active_amendment = ""`, `amendment_status = ""`.

**-1.9 Stale backlog lock check:** PASS — `claude/backlog/.lock` does not exist. `claude/roadmap/.lock` does not exist.

## Carry-Forward Advisory (ST-15)

Most recently completed cycle: `2026-08-03__release-v8.1` (`post_ship_complete = true`). `lessons_learnt_closure.md` for that cycle was reviewed for a `## Carry-Forward` section; both items on record there (the ungated-candidate scan miss recurrence-escalation, and the 1-of-19 user-facing-item scarcity finding) are informational/monitoring items for other engines (`groom backlog`/`run roadmap`), not action items for this engine to execute. No action required here.

## scored_initiatives.md Load (ST-14)

`claude/scoring/scored_initiatives.md` exists (16 lines, no populated initiative rows). No rows match any item selected into this cycle's scope (all 25 selected items are backlog-driven `BLG-*` items, not roadmap-scored initiatives). Tier 3 applies uniformly (§16.7) — STEP 4 inline estimates used for all 25 items, no advisory required.

## Scope Selection Rationale (Product Owner judgment, this session)

User instruction: "Use full sprint capacity. Focus on user features first." Both instructions were applied literally:

**User-features-first scan:** Reviewed all `BLG-FEAT-*` (27 items) and `BLG-FE-*` (38 items) candidates for ungated, ready, genuinely user-facing scope. Result — materially better than the prior two cycles' near-total scarcity:
- **`BLG-FEAT-88`** (P&L/tax record reconciliation report, P3, M effort, ungated) — the flagship pull-forward candidate already named across the last two `workforce_capacity.md` rebalance entries. Included, leading EPIC-01.
- **`BLG-FE-105`** (Compliance Recheck Modal all-pass empty-state design, P3, S), **`BLG-FE-67`** (RFJ event colour-palette accessibility refinement, P3, XS), **`BLG-FE-138`** (Trade Plan focus-indicator accessibility fix, P3, S) — all genuinely user-visible, ungated, no dependency on unmet data-density gates. Included.
- **`BLG-FEAT-86`** (drift-detection metric for the `insufficient_data` streak, P3, S) — a metrics/definitions item tied to the user-facing SI-02 gate-status display; included as the closest available thing to forward progress on the SI-02 gate itself, which is the structural blocker named in both prior cycles' lessons learnt.
- `BLG-FEAT-73`/`BLG-FEAT-74` (Arc 5 SI-02 frontend, PO-05 Replay Mode) — both P1 but gate-blocked (SI-02: still `insufficient_data`/unlinked trade-plans per the last live re-check on record, `2026-07-28__release-v7.9` EPIC-08; FEAT-74: §13 pre-clearance not run, VH effort exceeds single-cycle capacity even alone). **Perennial-Return Check (STEP 1.4a) + Sunset Criteria (STEP 1.4a.1) apply** — see below.
- Arc 5 pre-entry/compliance-gateway UX cluster (`BLG-FEAT-44/56`, `BLG-FE-43/45/54/58/59/62/63/68/69/70/71`) — every item's own Problem statement still names a substantive unmet precondition (Arc 6 unscoped; SI-02/SI-04/SI-05 dependencies unshipped; Phase 2 channel undecided). No change since `v8.1`'s assessment. Deferred again.
- `BLG-FEAT-45` (Monthly P&L 3-month usage retrospective) — gate clears 2026-08-05, i.e. **tomorrow**, squarely inside this cycle's execution window. Per STEP 1.4b this may not be classified firm regardless of proximity; excluded again rather than admitted as conditional scope this cycle (no conditional-scope machinery was otherwise needed this cycle, and the item is low enough priority/effort to simply pick up next cycle once the gate owner can confirm live).
- **Result: 5 ready, ungated, genuinely user-facing/user-adjacent items found and included** (`BLG-FEAT-88`, `BLG-FE-105`, `BLG-FE-67`, `BLG-FE-138`, `BLG-FEAT-86`) — a materially larger pool than the single item found at `v8.1` and the single item found at `v8.0`, though still modest relative to the 25-item full-capacity slice. Leads EPIC-01 per the "focus on user features first" instruction.

**Perennial-Return Check (STEP 1.4a) — explicit PO disposition, 3rd consecutive cycle:** `BLG-FEAT-73`/`BLG-FEAT-74` and the Arc 5 UX cluster have now been deferred at 3 consecutive cycles (`v8.0`, `v8.1`, `v8.2`) under materially the same gate-unmet rationale. Per STEP 1.4a.1 (Sunset Criteria, added this cycle's predecessor via `BLG-GOV-280`), the mandatory sunset trigger fires at **4 consecutive** Option (a) dispositions — this is the 3rd, still below the mandatory threshold. Disposition: **(a) Keep as conditional** — no materially new gate-clearance path has emerged since `v8.1`'s assessment (SI-02 live re-check unchanged since `2026-07-28__release-v7.9`; §13 pre-clearance for FEAT-74 still not run). Recorded per Product Owner delegated authority. **Flag for next cycle:** if `v8.3` also defers both items under an unchanged rationale, that would be the 4th consecutive Option (a) and the sunset trigger becomes mandatory — the next Release Planning session must either force Option (b) or document a materially new gate-clearance path per §1.4a.1.

**Full-capacity fill:** Remaining scope filled with the highest-priority ready (ungated) backlog items across Security/Operations, Governance Process, and QA/Spec/Backend debt categories — prioritising items already carrying `Provisional-Target: v8.2` from their filing session (`BLG-SEC-27`, `BLG-OPS-128`, `BLG-GOV-285`, all filed during `v8.1` execution with this release named as their intended target) and P1/P2 ungated items generally, then rounding out with a small, curated set of high-value P3 debt items (not padding indiscriminately — see `release_plan.md §Capacity Check` for the full effort table and the explicit note on why capacity was not force-filled to the exact ceiling). See `release_plan.md §Scope`.

**Self-caught scan miss (`BLG-OPS-48`):** An initial pass flagged `BLG-OPS-48` (ANTHROPIC_API_KEY 6-month scope audit) as ungated P2 and included it in a draft scope/backlog edit. On a second full-text read before commit, its gate condition was found expressed only inside its `**Provisional-Target:**` field text ("Gate date: 2026-11-01") rather than as a standalone `**Gate criteria:**` field — invisible to a scan that only checks for the standalone field label. The item is genuinely date-gated to 2026-11-01 (~3 months out) and was removed from scope before the backlog write was finalised; the (already-applied) `backlog.md` edit was reverted to its original two-line `Provisional-Target` text. This is the same class of miss self-caught at `v8.1` (Friction Item 1: ad hoc scan-methodology gap, no canonical gate-detection procedure exists in `release_planning_prompt.md`) — now a 3rd consecutive cycle with a related self-caught miss. See `lessons_learnt.md` Friction Item 1 for the recurrence escalation.

## Preflight Outcome

All hard gates PASS. Proceeding to STEP 0 state initialisation.
