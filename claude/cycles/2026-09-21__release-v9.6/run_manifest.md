Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-09-21
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-09-21

Class: Operational Record (Class 3)

---

# Run Manifest — v9.6

Invocation: `plan release --version "v9.6" --capacity "full capacity"`

Session start (UTC): 2026-09-21T08:57:42Z
Session end (UTC): 2026-09-21T09:06:42Z — elapsed 9m00s (computed, not typed)

## STEP -1 — Preflight Gate

**-1.1 Required files present:** `claude/charter/team_charter.md`, `claude/charter/document_lifecycle_guide.md`, `claude/strategy/strategy_rules.md`, `claude/roadmap/current_roadmap.md`, `claude/backlog/backlog.md` — all present. No `--issues gh`/`import` requested this invocation, so the `gh_issue_template.md` prerequisite (IMP-48) is not applicable (the file exists regardless).

**-1.2 Release exists on roadmap:** No formal `## v9.6` roadmap section exists (`current_roadmap.md` §1 "Next planned release: [TBD]"). Cleared via the Option(b)-equivalence rule: the most recent rebalance (`2026-09-19__scheduled`) recorded at its own STEP 8.1 Empty Now Horizon Gate "PO decision (STEP 8.1): Option (b) — defer. Now horizon intentionally empty for this cycle… this rebalance immediately precedes `plan release v9.6`" (`claude/cycles/2026-09-19__scheduled/run_manifest.md` line 118; 6th consecutive firing). §-1.2 cleared via STEP 8.1 Option(b) decision from rebalance `2026-09-19__scheduled` — the 14th consecutive release cycle relying on this equivalence (extending the v8.5–v9.5 pattern).

**-1.3/-1.4 Roles and write test:** All 9 required role agent files confirmed present in `claude/agents/` (Product Owner, Head of Specs Team, PMO Lead, Director of Quality, Infrastructure & Operations Owner, Strategy Rules & System Intent Owner, FinOps & Resource Architect, Facilitator, Challenger). Write test passed (`.write_test` created and removed cleanly in the new cycle folder).

**-1.5 Prior cycle lessons learnt closure check (advisory):** `prior_cycle` = `2026-09-15__release-v9.5`. `lessons_learnt_closure.md` exists; no `action: now`/`action-now`-marked items in its body (its two process improvements were already applied and logged at that closure: `release_planning_prompt.md` v2.52→v2.53, `OPERATIONAL_GUIDE.md` v4.197). No advisory raised.

**-1.6 Post-Ship Precondition Check (hard gate):** `post_ship_complete: true`, `next_cycle_unblocked: true` for prior cycle `2026-09-15__release-v9.5` — both confirmed in `.claude_current_state.json`. **SLA-breach carry-forward check (AUD-2026-09-14-001):** `open_escalations` = `{}` (empty). `deferred_escalations` holds `ESC-EXEC-20260910-01` at `disposition: Deferred` (not `Open`) — the check only scans `open_escalations` entries at `disposition: Open`. No breach found. Gate clears.

**-1.7 Prompt Change Log Integrity Check (advisory):** `release_planning_prompt.md` v2.53 and `sprint_planning_prompt.md` v3.18 both present in `claude/system/prompt_change_log.md`. No further gap scan beyond the two prompts this routine's own hand-off touches; no advisory raised.

**-1.8 Amendment In Progress Guard (hard gate):** `.claude_current_state.json.status` = `Closed`, not `Amendment_In_Progress`. Passes.

**-1.9 Stale Backlog Lock Preflight Check (advisory):** `claude/backlog/.lock` does not exist; `claude/roadmap/.lock` does not exist. Nothing to check.

**Branch:** `main`, working tree clean at session start.

```yaml
artifacts.run_manifest: present
```

---

## STEP 0 — Run Manifest + State Initialization

**Carry-Forward Advisory (ST-15):** Most recently completed cycle = `2026-09-15__release-v9.5` (`post_ship_complete: true`). `lessons_learnt_closure.md` §Carry-Forward holds 2 items: (1) two open decision-required items (`BLG-GOV-337`, `BLG-GOV-335`) that risked crossing 2+ cycles without a ruling; (2) the `governance-drift` skill checking only the `Version` cell of a §14 row (`BLG-GOV-336`). **Both items' underlying backlog entries were resolved directly on 2026-09-19** (`BLG-GOV-335`/`336`/`337`, marked ✅ COMPLETE in `backlog.md`, commit `90d77131`), so neither carry-forward now requires scope — see the Ready Pool section below for how this was caught. Reviewed: 2 items from cycle `2026-09-15__release-v9.5`.

**scored_initiatives.md Load (ST-14):** Loaded `claude/scoring/scored_initiatives.md` — 0 active roadmap initiatives (unchanged since 2026-08-11). No pre-assigned Effort Bands exist for any candidate. scored_initiatives.md: 0 matching items — not present for this cycle's fully backlog-driven scope.

Cycle: `2026-09-21__release-v9.6`
Mode: standard (default — not specified in invocation)
Issues mode: none (not specified in invocation)
Auto-escalate: true (default)

```yaml
artifacts.run_manifest: present
status: Initialized
```

---

## Scope Construction — Ready Pool & Selection Method

### Gate-Detection Procedure (§1.3a, scripted)

`python3 scripts/scan_backlog_gate_conditions.py` run against `claude/backlog/backlog.md`: **212 items scanned** (175 active post-groom at v9.5 close + 37 filed by the 2026-09-19 rebalance's idea intake, `IW-20260919-01`). Result: **131 gated/conditional items; 4 data-quality warnings** (`BLG-FEAT-73`, `BLG-FEAT-74`, `BLG-FEAT-76`, `BLG-SPEC-156`). So 212 − 131 = **81 items the scan leaves ungated.**

### Manual dispositions applied to the scan's output (each item read individually, per §1.3a's LL-v9.5-Release-01 note)

**Data-quality warnings (4):**
- `BLG-FEAT-73` (own text: "may not enter sprint planning until independently reconfirmed met"; re-check not due before 2026-11-09), `BLG-FEAT-74` ("§13 determinism pre-clearance not yet run"), `BLG-FEAT-76` (own `Provisional-Target`: "Unscheduled (gated)") — substantively gate-blocked despite carrying no formal `Gate` field. **Excluded (3).** Same basis as v9.3–v9.5. (`BLG-FEAT-73`/`74` are the only two P1 items the scan leaves ungated — they are gate-blocked, so the cycle's true ready-P1 count is **0**.)
- `BLG-SPEC-156` (`Provisional-Target`: "Unscheduled (pre-work before PO-04 gate)"; `Type`: "Governance / §13 Compliance") — describes a dedicated §13 pre-assessment performable *ahead of* the PO-04 gate, not work blocked by it. **Retained in the ready pool** (not selected on capacity grounds).

**Date-lapsed gates (BLG-GOV-345 / rebalance `2026-09-19__scheduled` Friction Item 1) — read individually, since the presence-based scan cannot see them:**

| Item | Lapsed gate | Verification performed this session | Disposition |
|------|-------------|--------------------------------------|-------------|
| `BLG-GOV-90` | First `BLG-GOV-74` quarterly AI review (due 2026-08-29) | `docs/governance/ai_feature_usage_quarterly_review_2026-09-07.md` exists (v1.1, filed by v9.1 ST-23 / `BLG-GOV-74`, with a same-day correction note) — the named event has occurred. Remaining scope is real: `ai_model_version_pinning_policy.md` §6 reviews annually/on SDK upgrade only, not quarterly, so integration with the `BLG-GOV-74` cadence is still open. | **Cleared → ready pool** (selected, ST-31) |
| `BLG-GOV-188` | Gate line reads "None — revival condition … confirmed Met 2026-07-08" | Gate is explicitly stated met; the scan flagged only because a `Gate criteria` field is present. | **Cleared → ready pool** (selected, ST-32) |
| `BLG-FEAT-59`, `BLG-FEAT-60`, `BLG-FEAT-63`, `BLG-FE-84` | "AI adoption window clears ~2026-07-25" | Date has lapsed, but each item's own AC additionally requires *named-owner verification before sprint planning* (`BLG-FEAT-59`: Financial Reporting & Records Owner; others "verified before sprint planning"). No verification is recorded. The rebalance's own hand-off routes that evidence to the **2026-09-24 AI review** (after today). A lapsed date is not proof the gate is met. `BLG-FEAT-63` additionally depends on `BLG-FEAT-59`. | **Not selected — classified conditional** (§1.4b; see below). Re-eligible at v9.7 planning once the review lands. |

Additional date-scan finding beyond the rebalance's list: `BLG-GOV-121` (P2) carries a lapsed date (2026-07-04) but its gate is `… AND Phase 2 activation decision made` — no such decision is evidenced (`BLG-GOV-113`/`BLG-GOV-92` shipped v5.3/v5.4, but the Phase 2 activation depends on SI-02, which is unmet). **Remains gated.** `BLG-FEAT-55`/`BLG-SPEC-65` carry a lapsed date with a still-open §13 review condition and `BLG-OPS-53` names ~Nov 2026 — all remain gated.

**Already-complete / already-satisfied items the scan does not detect (found this session — see Friction Item 1):**
- `BLG-GOV-335`, `BLG-GOV-336`, `BLG-GOV-337` — each carries a `✅ COMPLETE — 2026-09-19 — resolved directly in post-ship closure 2026-09-15__release-v9.5 follow-up` banner in `backlog.md` (archive pending the next `groom backlog`). The gate scan treats them as ungated; a first-pass selection seated `BLG-GOV-336`/`337` as P2 items ahead of everything else. **Excluded (3).**
- `BLG-GOV-326` — its sole AC is a merge decision ("expected: yes, satisfied by `BLG-GOV-323`") and `workforce_capacity.md` line 66 already records "**Decision: yes, merged** — one rollup table serves both consumers… `BLG-GOV-326` requires no separate implementation" (v9.5 ST-38). **Excluded (1)** as satisfied by shipped work; recommend closing at the next `groom backlog`.

### Ready pool

81 (scan-ungated) − 3 (`BLG-FEAT-73`/`74`/`76`) − 3 (`BLG-GOV-335`/`336`/`337`, complete) − 1 (`BLG-GOV-326`, satisfied) + 2 (`BLG-GOV-90`/`188`, cleared) = **76 items, 61.75 estimated days.**

Priority distribution of the ready pool: **P1: 0**, P2: 8 (`BLG-BE-119`, `BLG-FEAT-96`, `BLG-FEAT-97`, `BLG-GOV-329`, `BLG-GOV-345`, `BLG-OPS-166`, `BLG-SPEC-148`, `BLG-SPEC-160`), P3: 61, P4: 7.

### Effort-to-days rule (stated explicitly this cycle — see Friction Item 3)

Applied per item from its own `**Effort:**` field: an explicit single-point day figure (`~0.5d`, `~2d`) → that figure; an explicit day range (`~0.5–1d`, `~1.5–2d`) → the range midpoint; an hour-denominated figure (`<1h`, `~0.5–1h`) or a bare `XS` → 0.15d; a bare `S`/`M`/`L` → the canonical `workforce_capacity.md` midpoint (0.5 / 2.5 / 3.5d). **Sensitivity:** the same 32 items sum to 27.90d under a band-letter-only reading, so the selection sits within 28.00–27.90d under either rule — inside the confirmed ~24–28d band either way.

### Selection method applied (§1.4c)

P1-then-P2-first (0 ready P1; all 8 ready P2 seated, ascending ID), then category-balanced round-robin over the remaining ready P3/P4, oldest-filed-first within each category's turn, stopping at the top of the confirmed ~24–28 day band (28.00d, per the "use full capacity" instruction). Categories (§1.4c's named set plus one): Backend, Financial Reporting, Frontend/UX/Product (`FE`/`FEAT`/`UX`), Governance/Process (`GOV`/`AI`), Ops/Security (`OPS`/`SEC`), QA, Spec/Documentation (`SPEC`/`API`); queue order alphabetical, as at v9.5. Oldest-first key = latest date in the item's `Source` field (an `IDEA-…-YYYYMMDD` stamp or ISO date), ties broken by ID number. An item that would overshoot 28.00d is skipped and the pass continues with the next item that fits.

**Result:** category-balanced, oldest-first, P2-first selection from a **76-item / 61.75-day ready pool** → **32 items, 28.00 estimated days** (100.0% of the 28-day ceiling; exactly at the top of the band). Selected by category: Frontend/UX/Product 6, Governance/Process 6, Backend 5, Spec/Documentation 5, Ops/Security 4, QA 4, Financial Reporting 2. **44 ready items (33.75 days) left unselected** on capacity grounds — available for v9.7.

No override of §1.4c was applied (no explicit Product Owner override instruction was given this invocation). See Friction Item 2 for a consequence of applying it strictly.

### Advisory checks

**Backlog Age Advisory (§1.1):** No spec/documentation debt item in the selected pool has appeared in 2+ prior release cycles' ready pools without a story assignment. The items filed by idea-intake window `IW-20260914-01` (`BLG-SPEC-144`/`145`/`146`, selected) were in v9.5's ready pool once and not selected — 1 prior cycle, below the threshold. Nothing to promote.

**Provisional-Target Advisory (§1.2):** 4 selected items carry `Provisional-Target: v9.6` (`BLG-BE-118`, `BLG-BE-119`, `BLG-BE-120`, `BLG-SPEC-148`) and 1 carries `v9.5` (`BLG-QA-178`); the other 27 carry `TBD`/`Unscheduled`. **The ready pool holds 16 items tagged `v9.6` — 12 of them (4.80 days) are not selected** (see Friction Item 2).

**Design-Gate Language Scan (§1.3) / STEP 4.1 classification, including the v2.51 Scope-text scan:** All 6 EPIC-01 items ship or change visible UI (`BLG-FEAT-96` Clone action; `BLG-FEAT-97` CSV-export control; `BLG-FEAT-98` in-app reminder notification; `BLG-FE-180` stale marker; `BLG-FE-181` next-action links in empty states; `BLG-FE-182` shared formatter migrated across 3 tables) and each carries an explicit Playwright-covers-the-observable-AC criterion. Both EPIC-02 items carry an observable UI AC (`BLG-FR-04`: NULL-fee count visible in Monthly P&L; `BLG-FR-05`: restatement diff surfaces). Scope text of the other 24 items scanned for UI-shipping verbs/nouns: `BLG-GOV-188` ("Chart of velocity trend") is a generated governance artefact from `velocity_metrics.md`, not product UI — noted, classified non-UI; `BLG-SPEC-144` documents a palette and explicitly excludes any visual re-skin ("documentation only this cycle"); no other match. → **`design_gate_required: true`.**

**Gate-Condition Proximity Scan (§1.4):**

| Item | Gate condition | Current trajectory | Projected clear date |
|------|----------------|--------------------|----------------------|
| `BLG-GOV-140` / `141` / `142`, `BLG-OPS-88` | 90-day AI review (90 days post-v6.2 ship) | Date-driven | 2026-09-24 |
| `BLG-FEAT-59` / `60` / `63`, `BLG-FE-84` | Named-owner verification of the lapsed 2026-07-25 AI-adoption window | Evidence due at the 2026-09-24 AI review | ~2026-09-24 (verification), not the gate itself |
| `BLG-OPS-92` | New CVE/deprecation, or quarterly hygiene cadence | Date-driven | ~2026-10-06 |
| `BLG-FEAT-58` | Arc 4 PO-02 data model (6+ months AI-summarised journal data) | Date-driven | ~2026-10-20 |
| `BLG-FEAT-73` | Not re-checked before 2026-11-09 or 10 new linked `trade_plans` | 0 linked at 2026-09-19 (read-only staging query, context only) | ≥ 2026-11-09 |
| `BLG-OPS-53` | `claude_audit_log` 6+ months old | Date-driven | ~2026-11 |
| SI-02 (20+ trades with plans) / PO-04 (50+ trades with plans) | Data density | Trajectory unknown — 0 linked `trade_plans` at the 2026-09-19 rebalance | Unknown |

**Arc 4 data density sub-check:** data not available — no production credential is used by this routine (a `DATABASE_URL` is set in this environment but its target could not be confirmed as staging vs production from here, so it was deliberately not queried); the 2026-09-19 rebalance's read (0 linked `trade_plans`; SI-02/PO-02/PO-04 gates NOT MET) is carried forward unchanged. Product Owner to surface at readiness review. Not a halt condition.

**Perennial-Return Check / Within-Sprint Date Gate (§1.4a/§1.4b):** Perennial-Return — not triggered: none of the 32 selected IDs appear in the v9.5 or v9.4 `stage4_backlog_slice.md`, and neither slice carries a `returned_to_backlog`/`deferred_at_planning` marker. **§1.4b within-sprint date gates — classified conditional, none entering the slice:** planning is 2026-09-21 and the most recent sprint (v9.5) ran ~2 calendar days from seal (2026-09-16) to verification (2026-09-18) after a 1-day planning-to-seal gap, so 2026-09-24 plausibly falls inside the planned execution window — classified conservatively as within-sprint. `BLG-GOV-140`, `BLG-GOV-141`, `BLG-GOV-142`, `BLG-OPS-88` (gate 2026-09-24) and `BLG-FEAT-59`, `BLG-FEAT-60`, `BLG-FEAT-63`, `BLG-FE-84` (owner verification at the 2026-09-24 review) are therefore **classified: conditional** and are not firm capacity; promotion path is dated gate-owner confirmation. They were not added as conditional stories because the firm pool already exceeds capacity 2.2×, so conditional slots would only add promotion overhead (an amendment cycle per sprint-planning §1.4) with no capacity benefit. They re-enter v9.7 planning as ordinary ready items if the review clears them.

**Skill-Silo / build-and-ship rotation (roadmap_prompt.md §7.1; release_planning_prompt.md STEP 3 rotation guideline):** the 2026-09-19 rebalance reported the Skill-Silo Alert at 98.8% (5th consecutive worsening) and satisfied the ≥2 build-and-ship pull-forward with `BLG-FEAT-96` and `BLG-FEAT-97` (P2, ungated). Both are seated (ST-01, ST-03), plus `BLG-FEAT-98` (ST-04), and the EPIC table **leads with the execution-heavy EPIC-01** as this cycle's rotation slot. The mix is still debt-heavy (the ready pool has only 3 ungated `BLG-FEAT-*` items, all seated); `BLG-FEAT-59`, the rebalance's named *secondary* candidate, cannot be seated until its 2026-09-24 owner verification lands (see above). Release planning may not manufacture build-and-ship scope that does not exist.

```yaml
artifacts.stage1_readiness: pass
```

---

## Friction Items (carried into `lessons_learnt.md`)

1. **The scripted gate scan cannot see already-completed or already-satisfied backlog items, so a machine-built ready pool contains stale work.** 4 of the 80 items the scan-plus-manual-exclusion pass first produced were not actually open: `BLG-GOV-335`/`336`/`337` (✅ COMPLETE banners, resolved directly 2026-09-19) and `BLG-GOV-326` (sole AC already satisfied by v9.5 ST-38). A first-pass selection seated two of them as P2 items ahead of all others. Caught only by reading selected items' bodies. `sprint_planning_prompt.md` §3.1 has a stale-feature-target check, but Release Planning — where the pool is built — has none. This is the same class of blind spot as the lapsed-date gap (`BLG-GOV-345`): the scan classifies by field presence, not by item state.
2. **§1.4c ignores `Provisional-Target: v<current release>` tagging.** Strict application seated 4 of the 16 items already horizon-tagged `v9.6` and left 12 (4.80 days) behind — including the follow-ups filed against v9.5's own shipped work (`BLG-SPEC-149`–`155`, `BLG-FE-178`/`179`, `BLG-QA-179`/`180`/`181`). Oldest-filed-first ranks the 2026-09-14 idea-intake items above 2026-09-16/18 follow-ups by construction. §1.4c permits a Product Owner override for a specific cycle; none was given, so none was applied. Whether a horizon-tag tier belongs in the method (or whether such tags should simply be dropped when unselected) is a Product Owner / Head of Specs Team question.
3. **The effort-to-days conversion is not reproducible.** v9.5's published total (27.99d) matches neither a band-letter-only reading (26.60d) nor an explicit-range-midpoint reading (28.35d) of its own slice; `workforce_capacity.md`'s table says items may cite a more specific range but does not say which wins. This cycle states its rule (above) and a sensitivity figure (27.90d band-only). Recommend the table state the precedence rule.

```yaml
artifacts.friction_items_drafted: true
```
