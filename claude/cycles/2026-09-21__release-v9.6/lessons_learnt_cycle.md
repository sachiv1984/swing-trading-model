Owner: PMO Lead
Class: Operational Record (Class 3)
Status: Active
Last Updated: 2026-09-23
Cycle: 2026-09-21__release-v9.6

---

## Phase 3

**Phase:** Sprint Execution
**Cycle:** 2026-09-21__release-v9.6
**Section anchor:** `## Phase 3` (stable — cycle_id in field above, not in header)
**Filed:** 2026-09-23
**Reviewed by:** PMO Lead
**Prior cycle checked:** 2026-09-15__release-v9.5 (`lessons_learnt_cycle.md` `## Phase 3`) — 4 friction items filed: (1) `deviations_filed` write-timing gap, action-now, resolved same cycle; (2) `OPERATIONAL_GUIDE.md` §14 `Last Updated` cell drift, deferred to `BLG-GOV-336`; (3) same-EPIC sibling testing-gap disclosure inconsistency, deferred to next `execution_prompt.md` §3.2.A revision; (4) `workforce_capacity.md` write-scope authority gap, deferred to `BLG-GOV-337` decision. Checked each for recurrence this cycle: (1) not recurring — no `deviations_filed=false` write-timing gaps found this cycle, all 32 stories had the flag set correctly at the time of their own commit. (2) Not directly re-triggered this cycle, but this cycle's own STEP 5.1.B/ST-30 work independently caught and fixed a near-identical `OPERATIONAL_GUIDE.md` §14 self-row drift from the ST-27 commit — see friction item 1 below; related pattern, not the same instance. (3) Not assessed — no equivalent sibling-story testing-gap disclosure question arose this cycle. (4) **Recurrence, resolved successfully**: `BLG-GOV-337`'s ruling (the deferred decision from v9.5) was used as the direct precedent for this cycle's own `ESC-EXEC-20260921-08` (backlog.md gate-line write-scope exception for ST-23/ST-27) — confirms the v9.5 deferral was closed and the resulting standing exception is now in active, working use, not still open.

| friction_item | phase | type | classification | action | owner | target_date |
|---------------|-------|------|----------------|--------|-------|-------------|
| `OPERATIONAL_GUIDE.md` §14 self-row `Version`/`Last Updated` was left stale after the ST-27 (EPIC-07) commit despite that commit's own message claiming it was updated — a direct instance of the same defect class as v9.5's own Phase 3 friction item 2, on a different pair of fields (this cycle: the self-row itself, not the `Last Updated` cell specifically). ST-30's own new mandatory version-table audit cadence (this cycle's own deliverable) caught it on its very first run, same session. | Phase 3 | Type A — Governance Drift: A documented rule or header requirement was ignored or missed | action-now | Corrected same session (OPERATIONAL_GUIDE.md v4.201→v4.202→v4.203); `prompt_change_log.md` carries a same-session correction note on the original ST-27 row rather than silently rewriting history. The underlying systemic fix — ST-30's new §5.5 mandatory audit cadence in `roadmap_management_prompt.md` — is itself this cycle's shipped remediation for this exact class of drift recurring. | Sprint Execution Engine | — |
| `.claude_current_state.json.open_escalations` entries for `ESC-EXEC-20260921-02` (ST-16) and `-03` (ST-18) were left `disposition: Open` for the remainder of the sprint despite both being genuinely resolved mid-sprint (2026-09-22) — the resolution was correctly recorded in `delegation_log.md` and `execution_state.json.process_notes` at the time, but nothing in the routine propagates a mid-sprint delegation resolution back to the canonical `open_escalations` dict. Found and corrected only at sprint close (STEP 5.1), not when each was actually resolved. Same defect class also produced a stale top-level `blocked_items` array (listed 4 items already resolved) and an incomplete top-level `completed_items` array (missing all 11 EPIC-06/EPIC-07 story IDs — the "Persist merged state" commits for those two EPICs updated per-story/per-EPIC fields but never touched this summary array). | Phase 3 | Type A — Governance Drift: A documented rule or header requirement was ignored or missed | action-now | All three corrected at sprint close (STEP 5.1/pre-seal): `open_escalations` dispositions corrected with real resolution timestamps already on record; `blocked_items` cleared; `completed_items` union-corrected (LL-v7.10-P4-01 check run early rather than only at STEP 7). No new backlog item filed — recommend `execution_prompt.md` STEP 3.1 (per-story completion write) gain an explicit "also update `.claude_current_state.json.open_escalations` disposition and the top-level `completed_items`/`blocked_items` arrays in the same commit that resolves a delegated item," rather than leaving this to the sprint-close pre-seal checks alone to catch. | Head of Specs Team | Next `execution_prompt.md` revision touching STEP 3.1/3.1.D |

**Recurrence Notes:**
The cross-EPIC-branch `execution_state.json`/`decisions--*.md`/`.claude_current_state.json` merge-conflict pattern recurred again this cycle (4th+ consecutive cycle per v9.5's own count) — twice, in fact: EPIC-06→EPIC-07 (2 real conflicts, both resolved via the established combine-rather-than-choose / identical-text-collision rules) and EPIC-07→governance-backlog-additions (clean auto-merge, no conflict). Both resolved cleanly using the already-established `CLAUDE.md` §8 rules with no new gap found — continuing to log this as a stable, working control per v9.5's own precedent, not a recurring problem requiring a new fix. `BLG-GOV-337`'s standing write-scope-exception pattern (confirmed above as a successful v9.5 recurrence closure) was itself extended once more this cycle via `ESC-EXEC-20260921-08` for a structurally identical class of finding (a determination already published elsewhere, needing only its already-agreed text applied to a write-scope-restricted file) — worth Head of Specs Team's attention as a candidate for generalising into a standing rule rather than a fresh one-off ruling each time this shape of question recurs.
