Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-09-15
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-09-15

Class: Operational Record (Class 3)

---

# Run Manifest — v9.5

Invocation: `plan release --version "v9.5" --capacity "full capacity"`

## STEP -1 — Preflight Gate

**-1.1 Required files present:** `claude/charter/team_charter.md`, `claude/charter/document_lifecycle_guide.md`, `claude/strategy/strategy_rules.md`, `claude/roadmap/current_roadmap.md`, `claude/backlog/backlog.md` — all present. No `--issues gh`/`import` requested this invocation, so `gh_issue_template.md` prerequisite check (IMP-48) not applicable.

**-1.2 Release exists on roadmap:** No formal `## v9.5` roadmap section exists (`current_roadmap.md` §1 "Next planned release: [TBD]"). Cleared via the Option(b)-equivalence rule: the most recent rebalance (`2026-09-14__scheduled`) recorded, at its own STEP 8.1 Empty Now Horizon Gate, "PO decision (STEP 8.1): Option (b) — defer... scoping decision belongs to the next `plan release` invocation, not this rebalance" (`claude/cycles/2026-09-14__scheduled/run_manifest.md`, line 61; 5th consecutive occurrence at that rebalance). §-1.2 cleared via STEP 8.1 Option (b) decision from rebalance `2026-09-14__scheduled` — this is the 13th consecutive release cycle relying on this equivalence chain (extending the v8.5–v9.4 pattern).

**-1.3/-1.4 Roles and write test:** All 9 required role agent files confirmed present in `claude/agents/` (Product Owner, Head of Specs Team, PMO Lead, Director of Quality, Infrastructure & Operations Owner, Strategy Rules & System Intent Owner, FinOps & Resource Architect, Facilitator, Challenger). Write test passed (`.write_test` created and removed cleanly in the new cycle folder).

**-1.5 Prior cycle lessons learnt closure check (advisory):** `prior_cycle` = `2026-09-14__release-v9.4`. `lessons_learnt_closure.md` exists; `action_now` scan found no `action: now`/`action-now`-marked items in its body — no advisory raised.

**-1.6 Post-Ship Precondition Check (hard gate):** `post_ship_complete: true`, `next_cycle_unblocked: true` for prior cycle `2026-09-14__release-v9.4` — both confirmed in `.claude_current_state.json`. **SLA-breach carry-forward check (AUD-2026-09-14-001):** `open_escalations` = `{}` (empty). `deferred_escalations` holds `ESC-EXEC-20260910-01` at `disposition: Deferred` (not `Open`) — the SLA-breach carry-forward check only scans `open_escalations` entries at `disposition: Open`; a `Deferred` entry does not trigger this gate. No breach found. Gate clears.

**-1.7 Prompt Change Log Integrity Check (advisory):** Spot-checked `release_planning_prompt.md` (v2.52) against `claude/system/prompt_change_log.md` — present. No further gap scan performed this session beyond the file this cycle itself touches; no advisory raised.

**-1.8 Amendment In Progress Guard (hard gate):** `.claude_current_state.json.status` = `Closed`, not `Amendment_In_Progress`. Passes.

**-1.9 Stale Backlog Lock Preflight Check (advisory):** `claude/backlog/.lock` does not exist. Nothing to check. `claude/roadmap/.lock` also does not exist.

```yaml
artifacts.run_manifest: present
```

---

## STEP 0 — Run Manifest + State Initialization

**Carry-Forward Advisory (ST-15):** Most recently completed cycle = `2026-09-14__release-v9.4` (`post_ship_complete: true`). `lessons_learnt_closure.md` §Carry-Forward holds 4 items (deferred-patch-recurrence watch, a stale-claim self-catch confirmation, a deviation-scan-method watch, and a same-cycle-application-pattern confirmation) — none require action from this release-planning session; none block or alter scope selection. Reviewed: 4 items from cycle `2026-09-14__release-v9.4`.

**scored_initiatives.md Load (ST-14):** Loaded `claude/scoring/scored_initiatives.md` — 0 active roadmap initiatives (unchanged since 2026-08-11). No pre-assigned Effort Bands exist for any candidate. scored_initiatives.md: 0 matching items — not present for this cycle's fully backlog-driven scope.

Cycle: `2026-09-15__release-v9.5`
Mode: standard (default — not specified in invocation)
Issues mode: none (not specified in invocation)
Auto-escalate: true (default)

```yaml
artifacts.run_manifest: present
status: Initialized
```

---

## Scope Construction — Ready Pool & Selection Method

**Gate-Detection Procedure (§1.3a, scripted):** `python3 scripts/scan_backlog_gate_conditions.py` run against `claude/backlog/backlog.md` (195 items scanned — down from 210 at v9.4, reflecting the 28 v9.4 items archived out at `groom backlog` post-ship, +1 new item filed this session, `BLG-FE-177`). Result: 130 gated/conditional items; 6 data-quality warning(s) (embedded gate-like free text in `Provisional-Target`, no formal `Gate` field — `BLG-FEAT-73`, `BLG-FEAT-74`, `BLG-FEAT-76`, `BLG-SPEC-56`, `BLG-SPEC-57`, `BLG-QA-59`).

**Manual exclusion of 3 of the 6 data-quality-flagged items:** `BLG-FEAT-73` (own text: "may not enter sprint planning until independently reconfirmed met"; re-check not due before 2026-11-09), `BLG-FEAT-74` (own text: "§13 determinism pre-clearance not yet run"), `BLG-FEAT-76` (`Type` field itself states "gate-conditional") are each substantively gate-blocked by their own body text despite carrying no formal `Gate` field — excluded from the ready pool on the same basis v9.3/v9.4 applied. The remaining 3 (`BLG-SPEC-56`, `BLG-SPEC-57`, `BLG-QA-59`) are **retained** in the ready pool this cycle: on direct re-read, their `Provisional-Target` text ("pre-work before PO-02 gate ~2026-10") describes legitimate pre-authoring work performable *ahead of* the PO-02 gate, not work blocked *by* it — their own `Type` fields read "Spec / Pre-authoring" and "Quality Assurance / Pre-design", consistent with being doable now. (Note: v9.3/v9.4's own run manifests excluded these same 3 alongside the genuinely gate-blocked trio without drawing this distinction; this session re-reads the underlying text rather than repeating that grouping, since the scripted scan's own docstring is explicit that a data-quality warning "does not mark the item gated" and is a flag for a future `groom backlog` fix, not an instruction to exclude. Flagged as a Friction Item below.)

**Ready pool:** 195 − 130 (gated) − 3 (`BLG-FEAT-73`/`74`/`76`, substantively gate-blocked) = **62 items, ~43.79 estimated days**.

**Priority distribution of ready pool:** 2 ready P1 items (`BLG-BE-117` — CI-blocking test failure on every PR; `BLG-OPS-160` — possible live risk-management scheduling gap) — the **first cycle with genuine ungated P1 items** since this method's current form stabilised (every prior cycle's P1/P0 pool was 0, per each rebalance's own STEP 8.0 Production Correctness Fast-Track finding). 2 ready P2 items (`BLG-FE-177`, `BLG-SPEC-D18`). 1 P4 item (`BLG-SPEC-133`). Remainder P3.

**Selection method applied (§1.4c, first citable application of the now-codified rule):** P1 items selected first (ascending ID: `BLG-BE-117`, `BLG-OPS-160`), then P2 items (ascending ID: `BLG-FE-177`, `BLG-SPEC-D18`) — extending §1.4c's documented "P2-first" step to also seat P1 ahead of P2, since §1.4c's text did not anticipate a ready P1 item existing (every prior cycle's ready pool held none); logical priority ordering (P1 > P2 > P3 > P4) is applied rather than treating §1.4c's silence on P1 as an omission to seat P1 lower. Then category-balanced round-robin (BE, FE, GOV, OPS, QA, SPEC, UX — alphabetical queue order), oldest-filed-first within each category's turn, for the remaining P3/P4 items, stopping once cumulative estimated days reached the top of the confirmed ~24–28 day band.

**Result:** 43 items selected, **27.99 estimated days** — within 0.01 days of the exact 28-day ceiling, the tightest full-capacity fit on record (prior closest: v9.2/v9.4 at 27.55d). The remaining 19 ready-pool items (~16.80 days) are left unselected on capacity grounds — available for a future cycle.

**Backlog Age Advisory (§1.1):** No spec/documentation debt item in the selected pool has been carried 2+ prior cycles without a story assignment. `BLG-SPEC-56`/`BLG-SPEC-57`/`BLG-QA-59` (source 2026-06-19) are the oldest-filed selected items and are entering `stage4_backlog_slice.md` for the first time — no prior-cycle story assignment exists for any of the three, so the 2+ cycle threshold is not met.

**Provisional-Target Advisory (§1.2):** 7 selected items carry `Provisional-Target: v9.5` (`BLG-BE-117`, `BLG-FE-175`, `BLG-FE-176`, `BLG-FE-177`, `BLG-OPS-161`, `BLG-OPS-162`, `BLG-UX-05`) — horizon-planned for this release ahead of time (filed during v9.4 sprint execution/PR review with this release already anticipated). The remaining 36 selected items carry `TBD`/`Unscheduled`.

**Design-Gate Language Scan (§1.3) / STEP 4.1 classification, including the v2.51 Scope-text scan:** All 4 EPIC-06 items carry an observable UI acceptance criterion or Scope-text UI-shipping description — `BLG-FE-177` (visual text-formatting change on trade entry), `BLG-FE-175` (motion-timing fix across 4 existing components — a timing/interaction AC), `BLG-FE-176` (toast-notification timing fix across 9 call sites — a timing AC), `BLG-UX-05` (Arc 5 compliance-section copy/placement change — a visible layout AC). → **`design_gate_required: true`**. No item outside EPIC-06 carries an observable UI AC or UI-shipping Scope text (checked `BLG-SPEC-142`'s "diagram exists in `data_model.md`" — documentation-only, no shipped UI; `BLG-QA-*` items are test/doc-only).

**Gate-Condition Proximity Scan (§1.4):** Arc 4/Arc 5 data-density metrics unchanged since the last rebalance (0 new linked trade_plans confirmed this session; SI-02/PO-02/PO-04 gates NOT MET). No item in this cycle's scope touches that gate family.

**Perennial-Return Check / Within-Sprint Date Gate (§1.4a/§1.4b):** Not triggered — all 43 selected items are ungated and none appear in any prior cycle's `stage4_backlog_slice.md` with a `returned_to_backlog` status. `BLG-FEAT-73`/`74`/`76` (excluded above) are tracked under the existing Perennial-Return disposition chain from `v8.3` onward (`BLG-FEAT-73`/`74`) — no new disposition required this cycle (re-check dates not yet reached); `BLG-FEAT-76` has not previously triggered the 2-consecutive-cycle Perennial-Return threshold and is not yet subject to it.

**Skill-Silo / build-and-ship rotation (roadmap_prompt.md §7.1):** No ungated `BLG-FEAT-*` item exists in the ready pool this cycle (all 3 FEAT items — `73`/`74`/`76` — are excluded as substantively gate-blocked; `BLG-FEAT-95`, the prior cycle's named mandatory-pull-forward candidate, already shipped at v9.4/ST-28). Consistent with the rolling Skill-Silo Alert reading at the last rebalance (94.1%, 4th consecutive worsening) — this cycle's scope is again entirely debt/governance/ops/QA/spec-shaped, not by choice but because the ready pool contains no ungated build-and-ship candidate. Flagged for the next rebalance's own review, not actioned here (release planning may not manufacture scope that does not exist in the ready pool).

```yaml
artifacts.stage1_readiness: pass
```

---

## Friction Items (carried into `lessons_learnt.md`)

1. **`BLG-SPEC-56`/`BLG-SPEC-57`/`BLG-QA-59` re-classification.** v9.3 and v9.4 both excluded these 3 items from their ready pool alongside the genuinely gate-blocked `BLG-FEAT-73`/`74`/`76`, without recording a distinct rationale. This session re-read each item's own text and found they describe legitimate pre-authoring work performable ahead of (not blocked by) the PO-02 gate, and included them in scope (`ST-15`, `ST-23`, `ST-24`). See `lessons_learnt.md` for the full write-up and recommendation.
2. **First-ever ready P1 items required extending §1.4c's selection order.** §1.4c's canonical text specifies "P2-first" because no prior cycle had a ready P1 item to consider. This session applied P1-before-P2 as the evident intended priority ordering; recommend a small `release_planning_prompt.md` wording fix so future sessions don't need to re-derive this.

```yaml
artifacts.friction_items_drafted: true
```
