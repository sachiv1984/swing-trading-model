Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-07-30
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-07-30

# Run Manifest — Release Planning v8.0

Cycle: 2026-07-30__release-v8.0
Invocation: `plan release v8.0` (interpreted as `plan release --version "v8.0"` — user typed the shorthand form; no `--date`/`--timebox`/`--capacity`/`--mode`/`--issues`/`--auto-escalate` flags given, so all defaults apply per §3)
Mode: standard (default — not specified)
Issues mode: none (default — not specified)
Auto-escalate: true (default)

---

## STEP -1 Preflight

**-1.1 Common preflight (required files):** All 5 required files present (`team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md`). `--issues` not specified — `gh_issue_template.md` prerequisite check (IMP-48) not applicable.

**-1.1 Common preflight (required roles):** All 9 required agent files present under `claude/agents/`: Product Owner, Head of Specs Team, PMO Lead, Director of Quality, Infrastructure & Operations Owner, Strategy Rules & System Intent Owner, FinOps & Resource Architect, Facilitator, Challenger.

**-1.1 Common preflight (write test):** `claude/cycles/2026-07-30__release-v8.0/.write_test` created and removed successfully.

**-1.2 Release exists on roadmap:** No formal `## v8.0` roadmap section exists (`current_roadmap.md` §1 "Next planned release" is `[TBD]`). Cleared via STEP -1.2 Option (b) equivalence: the most recent rebalance (`2026-07-28__scheduled`) has a documented STEP 8.1 Option (b) decision ("Now horizon fully empty... defer again, no newly-ready anchor content this cycle") recorded in `current_roadmap.md`'s own header history. Same equivalence pattern used for v7.8/v7.9/v7.10. Cleared.

**-1.3/-1.4 Roles/write test:** See above — pass.

**-1.5 Prior cycle lessons learnt closure check (Advisory):** `.claude_current_state.json.prior_cycle` = `2026-07-21__release-v7.7`. That cycle's `lessons_learnt_closure.md` records `action_now_count: 1` — the single immediate action (new `docs/specs/Specs_Index.md` §37) is explicitly marked "Prompt change log entry: Not applicable (not a Class 6 governance prompt)", so no `prompt_change_log.md` cross-check applies. No advisory required.

Note: `prior_cycle` in `.claude_current_state.json` still points to `2026-07-21__release-v7.7` rather than the more recently closed `2026-07-28__release-v7.10` — `prior_release_plan` (a separate field) correctly names `2026-07-27__release-v7.9`. This looks like a field-ownership gap (no engine appears to bump `prior_cycle` on each closure) but is outside Release Planning's write scope to fix; surfaced here for visibility only, not actioned.

Carry-Forward items reviewed: 1 item from cycle `2026-07-28__release-v7.10` (most recently completed, `post_ship_complete = true`):
1. `BLG-OPS-111`'s endpoint list has drifted for a second consecutive cycle (v7.9/v7.10) — actionable by Post-Ship Closure's STEP 6 reconciliation escalation rule, not Release Planning. Noted, no action here; none of this release's 20 selected items touch that item.

**scored_initiatives.md load (ST-14):** `claude/scoring/scored_initiatives.md` (`2026-07-27__scheduled` rebalance) — 0 active roadmap initiatives scored this cycle (Debate Queue empty). 0 of this release's 20 backlog-driven items have a matching row. Tier 3 applies uniformly: STEP 4 inline estimates used for all 20; no advisory required.

**-1.6 Post-ship precondition (Hard Gate):** `post_ship_complete = true`, `next_cycle_unblocked = true` in `.claude_current_state.json`. Pass.

**-1.7 Prompt change log integrity (Advisory):** All 15 Class 6 prompts' current versions confirmed present in `prompt_change_log.md` with an exact `vOLD→vNEW` (or equivalent unquoted-filename) entry: release_planning_prompt v2.44, roadmap_prompt v9.8, execution_prompt v3.61, shared_standards v3.19, sprint_planning_prompt v3.13, post_ship_closure v2.21, delivery_verification_prompt v3.5, backlog_management_prompt v1.13, design_gate_prompt v1.6, ideas_housekeeping_prompt v1.1, amendment_cycle_prompt v1.9, idea_intake_prompt v2.8, roadmap_management_prompt v1.4, lessons_learnt_prompt v1.10, OPERATIONAL_GUIDE v4.123 (found via unquoted "OPERATIONAL_GUIDE v4.122→v4.123" reference, not the backtick-quoted-filename pattern used elsewhere — false-negative on first grep pass, confirmed present on manual check). No advisory required.

**-1.8 Amendment in progress guard:** `.claude_current_state.json.status = Closed`, `active_amendment = ""`. Not applicable — pass.

**-1.9 Stale backlog lock preflight (Advisory):** `claude/backlog/.lock` does not exist. No stale-lock condition. Pass.

Preflight outcome: **PASS** — no hard gates fired, no blockers raised, no escalations required.

---

## STEP 1 — Readiness Advisories

**1.1 Backlog age advisory:** Spec/documentation debt items in scope — `BLG-SPEC-78` and `BLG-SPEC-79` (both sourced `IDEA-*-20260712-*`, filed at the `2026-07-12__scheduled` rebalance) have carried through 8 subsequent release cycles (v7.3–v7.10) without a story assignment — well past the 2+-cycle threshold. ⚠ Advisory triggered. This session's own scope selection resolves it: both are included in EPIC-01 below. `BLG-SPEC-107` was freshly filed this cycle (`IDEA-financial-reporting-20260728-01`, filed 2026-07-28) — 0 cycles aged, no warning applicable to it.

**1.2 Provisional-Target advisory:** 0 of the 20 in-scope items carry `Provisional-Target: v8.0` (none were horizon-planned — the roadmap Now horizon has been empty since `2026-07-24__scheduled`). All 20 carry `TBD`. This release is entirely backlog-driven, consistent with the v7.8/v7.9/v7.10 pattern. `Provisional-Target` updated TBD → v8.0 on all 20 at STEP 4.

**1.3 Design-gate language scan (Advisory):** `BLG-FE-135` ("reachable via Tab and toggleable via Space/Enter... `aria-checked` reflects state") and `BLG-FE-136` ("Tab cannot move focus outside the modal... Escape closes it... focus returns to the triggering button") both carry observable UI interaction/timing acceptance criteria. Flagged for the Pre-sprint Required Decisions checklist / Design Gate.

**1.4a Perennial-return check (Advisory):** `BLG-FEAT-73`/`BLG-FEAT-74` remain the only items with a 2+-consecutive-cycle-return pattern (returned at v7.8/v7.9/v7.10 closes). No new gate evidence presented this session; Product Owner's active disposition remains (b) — keep parked, unchanged from the disposition already recorded in `manage_roadmap_log_20260730.md`. Neither item is in scope for v8.0. No other candidate in this session's selected pool has a prior-cycle `returned_to_backlog` status (all 20 selected items are drawn fresh from the ungated pool, none appeared in `2026-07-28__release-v7.10`'s `stage4_backlog_slice.md` as returned/deferred).

**1.4b Within-sprint date gate classification:** No in-scope item carries a within-sprint calendar-date gate condition (all 20 selected items are ungated — no `**Gate criteria:**`/`**Gate:**` field, and none carry a prose-embedded gate-date hint in `Provisional-Target`). Not applicable.

**1.4 Gate-condition proximity scan (Advisory):** SI-02 gate (PO-04/SI-02 trade-count thresholds): still NOT MET per the 2026-07-28 structured field (0/11 linked trade plans; drift `insufficient_data` last confirmed 2026-07-17). No fresh live check performed this session — no production API credentials available in this checkout, consistent with the `roadmap_prompt.md` v9.6 credential-fallback guidance (Release Planning does not perform a fresh live check regardless; that is Roadmap Rebalance's responsibility). PO-02/PO-04 data-density trajectories: "data not available this session — Product Owner to surface at next roadmap review." No SI-02-gated item is in this release's scope regardless (`BLG-FEAT-73`/`BLG-FEAT-74` remain excluded per the standing disposition above).

```yaml
artifacts.stage1_readiness: pass
```

---

## Scope Selection Rationale (STEP 2 support)

Backlog scanned in full (367 items, `claude/backlog/backlog.md`). Field-and-prose gate scan: 197 items carry a `**Gate criteria:**`/`**Gate:**` field; a further 2 (`BLG-FEAT-73`, `BLG-FEAT-74`) are gated via prose in `Provisional-Target` rather than the standard field (consistent with the standing PO exclusion). 168 items are genuinely ungated.

Of the ungated pool: 20 items carry Priority P1 (4) or P2 (48, before selection). No user instruction on capacity/timebox/grouping was given this session (unlike the explicit "use full capacity" / "group into thematic EPICs" instructions given at v7.10) — this session defaults to the same established pattern (full confirmed ~24-28 day capacity band, thematic EPIC grouping) as the precedent set at v7.8/v7.9/v7.10, since no contrary instruction was given and the Product Owner has not signalled a change of approach.

**Self-caught correction:** an initial pass selected `BLG-OPS-48` (ANTHROPIC_API_KEY 6-month scope audit) as a 20th item. On closer read, its own entry carries `**Gate date:** 2026-11-01 (~6 months after BLG-OPS-36 scope review)` — a real future-dated gate that the automated ungated-pool scan missed because it only pattern-matched the `**Gate criteria:**`/`**Gate:**` field names, not `**Gate date:**`. `Provisional-Target` on this item already read `~v4.9 (date-gated: no earlier than 2026-11-01)`, consistent with the gate. This item is **not** ready — 2026-11-01 is ~3 months past this cycle's date (2026-07-30) — and was removed from scope before any backlog write occurred (the `**Provisional-Target:**` substitution step failed with a "no TBD found" warning, which is what surfaced the discrepancy; no backlog.md content for this item was altered). Final selection is 19 items, not 20.

Selection: all 4 ungated P1 items (`BLG-FE-135`, `BLG-FE-136`, `BLG-SEC-24`, `BLG-SEC-26` — all frontend-accessibility/security follow-through findings surfaced directly by v7.10's own EPIC-02/EPIC-05 work) plus 15 ungated P2 items, sized to ~26.25 days midpoint effort — within the confirmed ~24-28 day band. Selection favoured genuine engineering/security/QA/operations correctness and hardening debt over pure-process governance items, consistent with the Product Value Ratio advisory (0.38, last measured `2026-07-28__scheduled`) — with one deliberate exception: `BLG-GOV-263` (structural fix for the recurring cross-EPIC `execution_state.json` merge-conflict pattern) was included despite being governance-classified, because it is a genuine engineering fix (not a process-analysis item) that has recurred and compounded across 3 consecutive multi-EPIC cycles (v7.6/v7.7/v7.8, 10/11 → 11/12 branches affected) per its own backlog entry's escalation history.

See `release_plan.md` §Scope for the full item table and `stage4_backlog_slice.md` for full acceptance criteria per story.

---

## Outstanding Actions Recorded This Session

None requiring action within Release Planning's write scope. The `prior_cycle` field staleness noted at STEP -1.5 above is surfaced for visibility only — outside this engine's write scope (root state pointer field ownership is not assigned to Release Planning).
