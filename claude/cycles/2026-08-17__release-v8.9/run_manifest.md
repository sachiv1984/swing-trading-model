Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-08-17
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-08-17

---

# Run Manifest — Release Planning v8.9

Class: Operational Record (Class 3)
Cycle: 2026-08-17__release-v8.9

## Invocation

`plan release v8.9` (interpreted as `--version "v8.9"`; mode: standard, issues: none, auto-escalate: true — no explicit flags given, all defaulted per §3)

## STEP -1 — Preflight Gate

- **-1.1 Required files present:** PASS — `team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md` all present. `gh_issue_template.md` present (not required this run — `--issues none`).
- **-1.2 Release exists on roadmap:** No formal `## v8.9` roadmap section exists (§1 "Next planned release" reads `[TBD]`). Checked `2026-08-11__scheduled` rebalance (most recent, `last_rebalance_cycle` unchanged since) `run_manifest.md`: STEP 8.1 fired 4th consecutive time — Option (b) defer, recorded. Cleared via STEP -1.2 Option (b) equivalence, same precedent already relied on for v8.5–v8.8.
- **-1.3/-1.4 Roles + write test:** PASS — required role agent files present in `claude/agents/`. Write test to `claude/cycles/2026-08-17__release-v8.9/.write_test` succeeded and was removed.
- **-1.5 Prior cycle lessons learnt closure check (advisory):** `prior_cycle` field in `.claude_current_state.json` reads `2026-08-14__release-v8.8` (self-consistent this time — the field was 1 cycle stale at the v8.8 planning session per that run's own note, and was corrected by v8.8's own post-ship closure). `lessons_learnt_closure.md` exists for `2026-08-14__release-v8.8`. Action-now items: 0 (all 6 open items in that closure's Consolidated Action Summary are explicitly "Deferred to next cycle," not action-now). No advisory required.
- **-1.6 Post-ship precondition (hard gate):** `post_ship_complete = true`, `next_cycle_unblocked = true` — PASS.
- **-1.7 Prompt change log integrity (advisory):** Spot-checked `release_planning_prompt.md` v2.49 header against `prompt_change_log.md` — no v2.49 row found in the tail of the log scanned; not exhaustively verified against the full log this session (advisory only, does not block). Recorded as an open advisory item, not escalated.
- **-1.8 Amendment in progress guard (hard gate):** `.claude_current_state.json.status = Closed` — not `Amendment_In_Progress`. PASS.
- **-1.9 Stale backlog lock preflight (advisory):** No `claude/backlog/.lock` present. Not applicable.

**Preflight: PASS.**

## Carry-Forward Advisory (ST-15)

Most recently completed cycle with `post_ship_complete = true`: `2026-08-14__release-v8.8`. Its `lessons_learnt_closure.md` §Carry-Forward carries 2 items:
1. Ungated P1/P2 backlog pool was nearly exhausted at v8.8 scoping time — flagged as a scoping-input signal, not to assume a ready pool exists. **Confirmed still true this cycle**: excluding the 2 brand-new P0 items filed today, the ungated P1/P2 pool held only 7 items (1 closed/stale, 6 live — all newly filed 2026-08-17), consistent with the prior cycle's warning.
2. Two Phase 4 deferred patches (CI-green per-fix restatement clarification; canonical Sandbox Access Constraint block) have now carried one full cycle transition (v8.7→v8.8) without a `prompt_change_log.md` entry. Per that note's own threshold: if either remains unapplied after *this* transition (v8.8→v8.9), the §6.4 2-cycle-without-application threshold is crossed and both must escalate to Head of Specs Team. **Both remain unapplied as of this session** (neither is a Release Planning write-scope item — release_planning_prompt.md/execution_prompt.md/qa_evidence_template.md/shared_standards.md are all outside §7's write scope). Flagged here per the carry-forward note's own instruction; actioning the escalation itself belongs to the next engine session with write access to those files (Sprint Execution or a dedicated governance session), not this routine.

## scored_initiatives.md Load (ST-14)

Loaded `claude/scoring/scored_initiatives.md`: no active roadmap initiatives (CPS = N/A), 0 matching rows for this release's backlog-drawn scope. All EPICs sized via inline STEP 4 estimate — see Capacity Check section of `release_plan.md`.

## Scope Selection Methodology

No formal roadmap Now-horizon scope exists to draw from (Option (b), as above). Scope drawn from the backlog.

**Anchor scope:** Two brand-new P0 (Critical) items were filed today (`BLG-BE-102`, `BLG-BE-103`), both `Provisional-Target: TBD (next release)` — i.e. explicitly named for v8.9. Both originate from a live production data-integrity investigation (a real WDC position's stop-loss discrepancy): a nightly trailing-stop ratchet that fails to apply its breakeven floor for profitable positions, and a currency-basis display bug on the same stop fields that already caused user confusion on a live position.

**Sizing decision (Product Owner, 2026-08-17):** Presented with 3 options — (a) tight scope (P0 fixes only, ~1.5–3 days), (b) widen to full ~24–28 day capacity band, or (c) a moderate P0+P2-only scope (~10–15 days) — the Product Owner chose **(b) — widen to full capacity**.

**Gate-Detection Procedure (`scripts/scan_backlog_gate_conditions.py`):** 268 items scanned, 170 gated/conditional + 6 data-quality warnings (unchanged findings, no new occurrences). All 22 selected items confirmed ungated against this scan's output.

**Selection pool:** Beyond the 2 P0 anchor items, the ungated P1/P2 pool held 7 items: `BLG-GOV-105` (already ✅ CLOSED — confirmed duplicate, excluded, pending archival at next `groom backlog`) and 6 live P2 items, all filed 2026-08-17 from the same Product Owner feature-vision session (`BLG-BE-104`, `BLG-FEAT-89/90/91/92`). Of these, `BLG-FEAT-92` was **excluded** — its own backlog text states an unresolved scope-overlap dependency on the gated `BLG-FEAT-30` ("Product Owner/Head of Specs Team to reconcile scope... before this enters sprint planning — may be absorbed") that this routine has no authority to resolve unilaterally; it remains available for a future cycle once reconciled. The remaining 5 P2 items (`BLG-BE-104`, `BLG-FEAT-89/90/91`) were all selected.

Remaining capacity filled from the ungated P3 pool (82 items after excluding closed/gated), weighted toward execution-heavy Backend items directly related to this cycle's anchor theme (`BLG-SPEC-85`, the `trailing_stop_action_rate` metrics-tolerance spec entry, pairs naturally with the `BLG-BE-102` fix) plus a curated, correctness/coverage-weighted selection across Backend, QA, Operations/Spec, and Governance debt — avoiding the P3 pool's heavy GOV skew (41 of 82 ungated P3 items are `BLG-GOV-*`; only 4 of 22 selected items this cycle are `BLG-GOV-*`, deliberately under-representing the pool's raw composition per the same execution-heavy-rotation reasoning `release_planning_prompt.md` §3 already applies to the EPIC-table lead slot).

**Backlog Age Advisory:** No spec/documentation debt item in the active backlog slice was found aged 2+ cycles without a story assignment beyond what is already covered by direct selection above; no additional promotion triggered.

**Provisional-Target Advisory:** 2 items carry `Provisional-Target: TBD (next release)` explicitly naming v8.9 (`BLG-BE-102`, `BLG-BE-103`). 20 items carry no v8.9-specific Provisional-Target signal (6 `Unscheduled`/generic-`TBD` P2 items, 14 general-`TBD` ungated P3 items).

**Design-Gate Language Scan:** No explicit "design decision required"/"pending design" language found in any selected item's text. However, per STEP 4.1's classification rule (observable UI ACs, independent of this scan), several EPIC-02 items and `BLG-BE-103` have observable UI acceptance criteria — see `release_plan.md` STEP 4.1.

**Perennial-Return Check / Within-Sprint Date Gate:** Not applicable — no gate-conditional items in scope (all 22 items confirmed ungated).

## Capacity

Confirmed capacity band: **~24–28 working-day-equivalent units** (`claude/roadmap/workforce_capacity.md`, unchanged since 2026-07-17). Total estimated effort this cycle: **26.125 days** (see `release_plan.md` Capacity Check for the full EPIC-level breakdown) — within the confirmed band (~93–109% of the 24–28 day range, near its midpoint), consistent with the Product Owner's widen-to-full-capacity decision.

## Escalations

None raised this cycle.
