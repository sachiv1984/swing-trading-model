Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-07-08
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-07-08

---

# Run Manifest — Release Planning v6.8

**Invocation:** `plan release try to pull through as much as possible. act with no constraints` — interpreted as `plan release --version "v6.8" --date "2026-07-08" --mode "standard"` (no `--issues`/`--auto-escalate` specified; defaults `none`/`true` apply). The literal invocation omitted the required `--version` flag; resolved to v6.8 as the only candidate release (see §-1.2). "Act with no constraints" is read as an instruction to maximise legitimate scope pull-through, not to bypass hard gates — CLAUDE.md §2 and this engine's own hard-gate design (§13) are non-negotiable regardless of delivery-pressure framing.

---

## STEP -1 — Preflight

### -1.1 Required Files
PASS — `team_charter.md`, `document_lifecycle_guide.md`, `strategy_rules.md`, `current_roadmap.md`, `backlog.md` all present.

### -1.2 Release Exists on Roadmap
No formal `## v6.8` roadmap section exists. Found documented STEP 8.1 Option (b) decision at `claude/cycles/2026-07-08__scheduled/cycle_record.md` and mirrored in `current_roadmap.md`'s header line: *"v6.8 scoping deferred to plan release (Option (b))"*. **§-1.2 cleared via STEP 8.1 Option(b) decision from `2026-07-08__scheduled`.**

### -1.3/-1.4 Roles and Write Test
Required roles per §6 assumed present (delegated authority model, solo-developer context per `team_charter.md`). Write test path `claude/cycles/2026-07-08__release-v6.8/.write_test` — writable, cleaned up.

### -1.5 Prior Cycle Lessons Learnt Closure Check (Advisory)
`prior_cycle` field in `.claude_current_state.json` reads `2026-06-26__release-v6.3` — a stale pointer several releases old (same staleness noted in v6.7's own run_manifest). Most recently closed release is `2026-07-06__release-v6.7` (Verified, Closed_with_actions, post_ship_complete=true). Checked `claude/cycles/2026-07-06__release-v6.7/lessons_learnt_closure.md` action-now items: none marked `action: now` this cycle (2 carry-forward items only, both advisory-monitoring, not action-now). No advisory triggered.

### -1.6 Post-Ship Precondition Check (Hard Gate)
`post_ship_complete = true`, `next_cycle_unblocked = true`. PASS.

### -1.7 Prompt Change Log Integrity (Advisory)
Not re-checked this session (unchanged since v6.7's own pass — no governed prompt edited since). Skipped per no-change basis.

### -1.8 Amendment In Progress Guard
`status = "Closed"`, not `Amendment_In_Progress`. PASS.

### -1.9 Stale Backlog Lock Preflight
`claude/backlog/.lock` does not exist. PASS — no stale lock.

**Lifecycle Guard:** `status = Closed` — valid from-state. PASS.

---

## STEP 0 — Carry-Forward Advisory / Scoring Load

**Carry-Forward Advisory (ST-15):** Most recently completed cycle is `2026-07-06__release-v6.7`. Its `lessons_learnt_closure.md` §Carry-Forward has 2 items: (1) SI-02 gate remains unresolved — treat as still gated, do not accept self-reported trade counts; (2) `post_ship_closure.md` §7.3 cross-reference correction (documentation, not scope-relevant). Both reviewed; item (1) directly informs this cycle's exclusion of SI-02 from scope (already excluded on independent grounds — condition (1) itself is now known to be 0 linked trades, worse than "unresolved").

**scored_initiatives.md Load (ST-14):** 2 matching rows found for this cycle's scope candidates: `BLG-FEAT-52` (Effort Band: S) and `BLG-FEAT-71` (Effort Band: S), both from the 2026-07-08 rebalance's Product Value Alert entry. No other scope item has a `scored_initiatives.md` row — falls to tier 3 (inline STEP 4 estimate) for the remaining 15 items.

---

## Readiness (STEP 1)

**Roadmap status:** `current_roadmap.md` §3 (Now horizon) is empty by deliberate STEP 8.1 Option (b) choice at the 2026-07-08 rebalance — scoping delegated to this engine. Arc 1/Arc 2 fully complete. Arc 3 fully complete. SI-02 (Arc 5) gate **NOT MET** — worse than previously estimated: `BLG-BE-46` confirmed 20 total closed trades but 0 linked trade-plans (`position_id` is `NULL` on all 11 trade-plan rows) — a linkage bug, not a proximity-to-threshold situation. Arc 4/Arc 6 remainder data-density-gated, no change.

**Readiness determination: READY.** With the Now horizon deliberately empty, scope for this release is drawn entirely from `backlog.md` per this engine's own readiness scan (STEP 1.1–1.4) plus the two mandatory pull-forward items named by the 2026-07-08 rebalance's Product Value Alert response.

### 1.1 Backlog Age Advisory
4 spec-debt items found aged 4+ release cycles without a story assignment: `BLG-SPEC-58/59/60/61` (all `Provisional-Target: v6.3`; v6.4, v6.5, v6.6, v6.7 have since shipped without touching them — confirmed via the `groom backlog --dry-run` session immediately prior to this invocation). Per the advisory recommendation, **all 4 are promoted to firm sprint stories this cycle** (S2-06 through S2-09) rather than left to age further.

### 1.2 Provisional-Target Advisory
Only 1 item carries `Provisional-Target: v6.8` explicitly: `BLG-BE-46`. 2 items carry `Provisional-Target: Next release` (mandatory pull-forward): `BLG-FEAT-52`, `BLG-FEAT-71`. 1 item carries `Provisional-Target: Next release` for infrastructure reasons: `BLG-OPS-99`. The remaining 13 scope items carry stale or `Unscheduled` targets, surfaced instead via the 1.1 advisory and the informal deferral-age finding from the immediately-prior `groom backlog --dry-run` session (see Scope section for full per-item aging).

### 1.3 Design-Gate Language Scan
No scope candidate contains the literal design-gate trigger phrases. However `BLG-FEAT-52` (tag input + filter UI on PerformanceAnalytics) and `BLG-FEAT-71` (status panel/badge on Reports page) both carry explicit observable-UI acceptance criteria — flagged for STEP 4.1 design-gate classification.

### 1.4a Perennial-Return Check
No scope candidate appears in `2026-07-06__release-v6.7`'s `stage4_backlog_slice.md` with `returned_to_backlog`/`deferred` status (v6.7 had 0 returns) — the mechanical trigger for this check does not fire for any item this cycle. **Informal parallel finding (from the `groom backlog --dry-run` run immediately preceding this invocation, not yet formalised as backlog flags):** 16 backlog items carry a `Provisional-Target` version that has already shipped, with zero `PO re-deferral` notes anywhere in `backlog.md`. Of those 16, this release resolves 13 directly (see Scope) rather than deferring them a further cycle. The remaining 3 (`BLG-GOV-74`, `BLG-GOV-140`, `BLG-GOV-141`) carry genuine calendar gate dates in the future (2026-08-29, 2026-09-24, 2026-09-24 respectively) that had simply never been reconciled with their stale version-label — corrected disposition recorded under Scope → Items explicitly deferred.

### 1.4b Within-Sprint Date Gate Classification
No scope candidate carries a gate-clearing date falling within the planned sprint window (2026-07-08 → sprint close). `BLG-GOV-74`/`BLG-GOV-140`/`BLG-GOV-141`'s gate dates (Aug/Sept 2026) are well outside any plausible sprint window for this cycle — correctly excluded entirely rather than classified conditional-in-scope.

### 1.4 Gate-Condition Proximity Scan

| Item | Gate condition | Current trajectory | Projected clear date |
|------|-----------------|---------------------|------------------------|
| SI-02 (Behavioural Drift Detection) | ≥20 closed trades with linked trade_plans (+ p99 latency + drift-score meaningfulness) | 20 closed trades confirmed; 0 linked (`position_id` NULL on all 11 plans) — `BLG-BE-46` is the blocking bug, not trade-count proximity | Unresolvable by trade accumulation alone; clears only once `BLG-BE-46` fixes/backfills linkage — this release includes `BLG-BE-46` as S2-01 |
| PO-02 (Journal Pattern Recognition) | 6+ months of AI-summarised journal entries | trajectory unknown — not queried this session | data not available |
| PO-04 (Reflection ↔ Outcome Correlation) | 50+ trades with plans | long-horizon at ~1–2 trades/month | ≈2026-Q4/2027 (indicative) |
| BLG-GOV-74 | Quarterly AI-usage review — first due 2026-08-29 | Not due | 2026-08-29 |
| BLG-GOV-140/141 | Quarterly §13/logging audits — first due 2026-09-24 | Not due | 2026-09-24 |

**Do not halt.** Advisory only. Note: **`BLG-BE-46` is included in this release's firm scope** — if its root-cause fix lands, it directly unblocks future SI-02 re-verification (does not itself clear the 20-trade/linked-trade condition, since that requires either backfill or new linked closes after the fix).

```yaml
# state.json update (STEP 1):
artifacts.stage1_readiness: pass
```

---

## Notes carried into Scope/Execution Plan

- Both UI-facing items (`BLG-FEAT-52`, `BLG-FEAT-71`) trigger `design_gate_required = true` at STEP 4.1.
- `BLG-FEAT-52` introduces 2 new endpoints (`POST/DELETE /trades/{id}/tags`, `GET /analytics/tag-performance`) — CLAUDE.md's same-commit `openapi.yaml` + `docs/specs/api_contracts/` + `backend/routers/test.py` rules apply at execution time; flagged in Execution Plan sequencing note.
- `BLG-GOV-123` edits `execution_prompt.md` (a governed prompt) — CLAUDE.md §6 Governance File Edit Checklist (version bump, OPERATIONAL_GUIDE §14 sync, phase-section header sync, prompt_change_log.md entry) applies at execution time; flagged in Execution Plan.
