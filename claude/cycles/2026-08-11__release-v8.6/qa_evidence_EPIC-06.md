Owner: Director of Quality
Class: Planning Document (Class 4)
Status: Active
Last Updated: 2026-08-11

# QA Evidence Log — EPIC-06 (Operations & Governance Debt Closure)

**EPIC:** EPIC-06 — Operations & Governance Debt Closure
**Cycle:** 2026-08-11__release-v8.6
**Sprint goal:** Ship all 26 scoped v8.6 stories — trade-plan completion-rate tracking and an AI-assisted order-placement thesis digest, trade-plan-to-position linkage enforced with a DB-level integrity safeguard, the remaining shadcn design-token and secondary-text drift debt closed, and the financial-correctness, QA-coverage, and governance-debt carryover from v8.5 fully resolved
**Test scenarios used:** N/A — this EPIC is workflow/CI-config and governance-document fixes; verified via YAML syntax checks, a shell simulation of the ST-19 grep fix, and `git blame`/git log traceability for ST-22/ST-23.

| ST Item | Spec Reference | What was built | Acceptance criteria | Result | Deviations |
|---------|----------------|----------------|---------------------|--------|------------|
| ST-19 | `.github/workflows/api-key-cross-environment-check.yml` | Missing-secrets skip-guard now emits a `[ERROR]`-prefixed line (matching the script's own convention) alongside the pre-existing `::error::` GitHub Actions annotation, so the alert step's `grep '^\[ERROR\]'` actually finds the detail. Verified via a shell simulation confirming the grep now matches. | The missing-secrets skip-guard path's Telegram alert includes the specific error detail, verified once fixed | Pass | None |
| ST-20 | `.github/workflows/dependency-vuln-rescan.yml` | Added a cross-reference comment pointing to `vulnerability-scan.yml`'s existing full CVE-2026-4539 rationale (DRY, avoids duplicating the rationale text). | `dependency-vuln-rescan.yml`'s `CVE-2026-4539` ignore has an inline rationale comment or cross-reference to `vulnerability-scan.yml`'s | Pass | None |
| ST-21 | `.github/workflows/dependency-vuln-rescan.yml` | Not yet executable — this story's own AC requires observing a real post-merge workflow run, which cannot happen before this EPIC's PR merges. Sequenced last within EPIC-06 by design (own notes: "Must execute after this EPIC's PR merges"). | A confirmed-successful `dependency-vuln-rescan.yml` run exists post-merge, closing v8.5/ST-04's originally-deferred AC | **Deferred — intentional, by the story's own design** | None — not a gap; a post-merge observation follow-up will confirm this once the PR merges and the next scheduled/manual run completes |
| ST-22 | `docs/specs/frontend/pages/navigation.md#Known Deviations` | Filed `DEV-NAV-ST06-01` (RESOLVED) in a new Known Deviations section, matching the `DEV-REPORTS-ST01-02` format precedent, for the v8.5 dark-mode/Radix-portal `Layout.js` fix (shipped same-commit as its own discovery). | A `DEV-*` record exists for the `src/Layout.js` dark-mode/Radix-portal fix, matching the format used for `DEV-REPORTS-ST01-02` | Pass | None — cross-EPIC version collision with EPIC-03/ST-09 (both bumped `navigation.md` to v1.5 independently) flagged inline per CLAUDE.md §8.2a for merge-time renumbering |
| ST-23 | `claude/system/changelogs/shared_standards_changelog.md` | Backfilled the missing v3.27 changelog row, traced via `git blame` to commit `ec528651` (§20 Dependency Vulnerability Scan Cadence, ST-04/BLG-SEC-15/v8.5). Also appended a `prompt_change_log.md` entry per the STEP 8 convention. | `shared_standards_changelog.md`'s top row matches `shared_standards.md`'s current version (v3.27, §20 addition) | Pass | None |
| ST-24 | `claude/system/shared_standards.md#16.15`, `claude/system/schemas/execution_state_schema.json`, `claude/system/templates/qa_evidence_template.md` | New §16.15 documents `deviations_filed`'s actual meaning ("check performed", not literally "filed"). Matching `_deviations_filed_note` added to the schema. `qa_evidence_template.md`'s "Known deviations filed" line reworded to "Known deviations" with a wording note, preventing the apparent contradiction going forward. Full CLAUDE.md §6 governance file edit checklist applied (both files' versions bumped, `OPERATIONAL_GUIDE.md` §14 + header + Change Log updated, `prompt_change_log.md` entries appended). | `execution_state_schema.json` and `shared_standards.md` document the field's actual meaning without contradiction; no future qa_evidence log reads self-contradictory; Head of Specs Team sign-off | Pass | None |
| ST-25 | N/A — resolved as moot, no file edit | `BLG-FE-146`/`BLG-FE-139` were already shipped complete and retired to `backlog_archive.md` before this story executed; the requested "still unmet" annotation would have been factually false, and `backlog_archive.md` is outside this routine's write scope regardless. Escalated as `ESC-EXEC-20260811-01`, resolved by Head of Specs Team decision. | Both items carry the 2026-08-10 re-check confirmation inline in `backlog.md` | **Resolved — AC's premise overtaken by events; closed with documented decision instead of the literal file edit** | None |
| ST-26 | N/A — resolved as moot, no file edit | `BLG-GOV-288` was already shipped complete and retired to `backlog_archive.md`; correcting an archived record's historical AC text has no forward-looking value, and `backlog_archive.md` is outside this routine's write scope. Escalated as `ESC-EXEC-20260811-02`, resolved by Head of Specs Team decision. | `BLG-GOV-288`'s AC text matches the actual, correct implementation site (STEP 7) | **Resolved — AC's premise overtaken by events; closed with documented decision instead of the literal file edit** | None |

**QA test coverage:**
- Scenarios run: YAML syntax validation (`python3 -c "import yaml; yaml.safe_load(...)"`) on both modified workflow files; a shell simulation confirming ST-19's grep fix actually matches; `git blame`/`git log -p` traceability confirming ST-22's commit reference (`41619410`) and ST-23's commit reference (`ec528651`) are real and accurate.
- Regression areas checked: `api-key-cross-environment-check.yml`'s cross-wired-keys alert path (exit_code 1) unchanged — only the exit_code 2 (missing-secrets) path was touched; `dependency-vuln-rescan.yml`'s existing pip-audit/npm-audit logic unchanged, only a comment added.
- Known deviations filed: None (see §16.15 note above — this line intentionally does not read "filed" per this EPIC's own ST-24 fix).

**ST-21 disposition note:** unlike a genuine coverage gap, ST-21's AC is structurally sequenced to require this EPIC's own merge as a precondition — the story's own notes field states this explicitly ("Must execute after this EPIC's PR merges"). This is not deferred-for-convenience; it is deferred-by-design. Per `execution_prompt.md` §5.1 Result-value conventions (`shared_standards.md` §16.11's "Staging-deferred" precedent for AC that structurally cannot be verified pre-merge), this EPIC proceeds to PR with ST-21 explicitly tracked as a required post-merge follow-up action, not silently dropped — a live post-merge run of `dependency-vuln-rescan.yml` (scheduled monthly, or triggered manually via `workflow_dispatch`) must be confirmed and this story's `execution_state.json` entry updated to `done` before this EPIC's story set is considered fully closed.

---

## Sign-Off Block

**Autonomous class eligibility check (BLG-GOV-19):**
- [x] Criterion 1: All stories in this EPIC have `delegation_class: autonomous` — **partially** (ST-24/ST-25/ST-26 are `delegated_decision`; corrected 2026-08-12 — an earlier version of this line named only ST-25/ST-26, undercounting ST-24, caught by an agent-mediated Director of Quality review of the open PR) — qualifies via the Verification-class sub-criterion (LL-v4.5-EX-01): all three are pure governance-decision items verified by document inspection only, no observable UI/staging run/live system interaction (ST-24: `shared_standards.md`/`qa_evidence_template.md` clarification, agent-mediated Head of Specs Team review CONFIRMED; ST-25/ST-26: escalations resolved as moot, reviewed above) — ✓
- [x] Criterion 2: All AC verifiable by code review alone — no observable UI behaviour, no staging run required, no live system interaction (ST-21 excepted, explicitly deferred by design per above, not treated as blocking this sign-off) — ✓
- [x] Criterion 3: No frontend-visible change — confirmed: `git diff main -- 'src/pages/**' 'src/components/**'` on this branch is empty — ✓
- [x] Criterion 4: Engine signer field populated as "Sprint Execution Engine (autonomous class)" — ✓

- Signed off by: Sprint Execution Engine (autonomous class)
- Date: 2026-08-11
- Comments: Autonomous class sign-off — all four qualifying criteria met (governance/CI-config-only EPIC, ST-24/ST-25/ST-26's delegated_decision classification qualifies via the document-inspection verification sub-criterion, no frontend files touched, engine signer populated). ST-21 is a known, by-design post-merge follow-up, not a defect — tracked explicitly above and in `execution_state.json`, must be confirmed and closed after this EPIC's PR merges. Director of Quality may review and override at any time before merge per `execution_prompt.md` §3.2.A.
