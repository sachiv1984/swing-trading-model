Owner: PMO Lead
Class: Operational Record (Class 3)
Status: Active
Last Updated: 2026-08-12
Cycle: 2026-08-11__release-v8.6

## Phase 3

**Phase:** Sprint Execution
**Cycle:** 2026-08-11__release-v8.6
**Section anchor:** `## Phase 3` (stable — cycle_id in field above, not in header)
**Filed:** 2026-08-12
**Reviewed by:** PMO Lead
**Prior cycle checked:** 2026-08-08__release-v8.5 (`lessons_learnt_cycle.md` `## Phase 3`) — 2 friction items, 1 deferred patch. (1) Session-boundary landed between an EPIC's PR merge and the routine's own pre-halt persist writes, requiring STEP 4's resume-sync to correct stale `execution_state.json` — classified "no process patch required, the existing mechanism is exactly what this scenario is designed for" — **recurred identically this cycle** (all 6 EPICs had merged upstream between sessions; local `main` was 52 commits behind `origin/main` at session start), and the existing LL-v7.2-P3-01 session-start divergence check + LL-v3.9-P3-1 merge-gate resume-sync again recovered cleanly (git fetch/checkout/pull, `gh pr view` sync of all 5 remaining EPICs, 0 orphaned commits found) — confirms this class of recurrence is by design, not a gap; no new patch triggered. (2) `OPERATIONAL_GUIDE.md` §14's self-referential Version/Last Updated row going stale after a version bump, `governance-drift` skill's Step 1b check already exists but has no mandatory invocation point inside `execution_prompt.md` — **deferred**, target "next `execution_prompt.md` revision touching §3.2.A or §5.3". Checked against `prompt_change_log.md`: no entry applying this patch since `2026-08-10`. Carried forward across 2 consecutive cycles (`v8.5` → `v8.6`) without an entry — **automatic recurrence escalation per `lessons_learnt_prompt.md` §3.7**, applied action-now this session rather than deferred a 3rd time (see friction item 1 below). Its own live exercise, performed as part of applying the fix, immediately caught a real, un-deferred instance of the exact drift it targets (§14 self-row 2 versions stale) — validating the fix was needed, not merely precautionary.

| friction_item | phase | type | classification | action | owner | target_date |
|---------------|-------|------|----------------|--------|-------|-------------|
| `OPERATIONAL_GUIDE.md` §14's self-consistency check had no mandatory invocation point (carried from v8.5 Phase 3, now recurrence-escalated per §3.7 after 2 cycles unapplied) | Phase 3 | A | action-now | `execution_prompt.md` v3.66→v3.67: §3.2.A now mandatorily invokes the `governance-drift` skill's Step 1b 3-way self-consistency check before EPIC PR-open, whenever CLAUDE.md §6 fired for that EPIC. Applying the fix live immediately surfaced a real SELF-DRIFT (§14 self-row stale at `4.152`/`2026-08-10` against header/Change Log's `4.154`/`2026-08-11`) — corrected in the same edit. `prompt_change_log.md` entries filed for both `execution_prompt.md` (v3.66→v3.67) and `OPERATIONAL_GUIDE.md` (v4.154→v4.155). | Head of Specs Team | — |
| Three separate stories this sprint (ST-03/EPIC-02, ST-11+ST-12/EPIC-04, ST-24/EPIC-06) each had a first-pass claim of completeness or verification — a docstring "corrected", a rounding bound "independently re-verified and conservative", a sign-off block, a self-check count — that was **factually wrong**, caught only by a second, independently-scoped agent-mediated review pass explicitly requested by the user against the already-open PR, not by the story's own original completion step or its first agent-mediated sign-off. The ST-12 case is the most material: the first agent-mediated Financial Reporting & Records Owner review *claimed* to have independently re-verified the cost-basis drift bound and found it conservative — that claim was itself unsubstantiated, and a real bug (proportional-allocation drift compounding, not bounded, disproven by a concrete 37-exit counter-example 6x the asserted bound) shipped into the PR undetected until the second pass. | Phase 3 | D | defer | This is a 3-occurrence-in-1-sprint pattern, not previously seen at this frequency (0 comparable occurrences flagged in `v8.5`'s Phase 3 record) — high enough to warrant a structural response, but the right fix (e.g., a mandatory second, differently-scoped review pass for any story making a quantitative/"already verified" claim; a check that an agent-mediated sign-off's own "I re-verified X" language is itself evidenced, not just asserted) needs deliberate design rather than a rushed same-session edit on top of this run's other governance changes. Recommend `execution_prompt.md` §5.3 (Agent-Mediated Sign-Off protocol) as the target section. | Head of Specs Team | next `execution_prompt.md` revision touching §5.3 |
| ST-21's AC ("confirm `dependency-vuln-rescan.yml` runs successfully post-merge") assumed a workflow run would naturally fire once its owning EPIC merged, but the workflow (`Dependency Vulnerability Re-scan (Monthly)`) is cron-only (`0 7 1 * *`) with no push/PR trigger — no natural run existed to observe at session start despite EPIC-06 having merged ~3.5 hours earlier. Resolved cleanly this session via `gh workflow run <id> --ref main` (the workflow's own `workflow_dispatch: {}` trigger, already present, just not the documented verification path) — run `31595550051` completed `success` with 0 new findings, confirming ST-14's failure-detection fix behaves correctly under real CI. | Phase 3 | C | action-now | No prompt change required — the `workflow_dispatch` trigger already existed on the workflow and was simply used. Noting here for future authors of "confirm workflow X runs post-merge"-shaped ACs against schedule-only workflows: default to `gh workflow run` for immediate same-session observation rather than waiting on (or assuming) a natural trigger. | Sprint Execution Engine | — |

**Recurrence Notes:** Friction item 1 is the direct recurrence-escalation of v8.5 Phase 3's deferred patch — now resolved. Friction item 2 (the review-catches-first-pass-error pattern) does not recur from any single v8.5 Phase 3 item by name, but is a new pattern first observed at this frequency this cycle — flagged for structural follow-up rather than dismissed as three unrelated incidents. Friction item 3 is newly identified this cycle (ST-21 did not exist in any prior cycle).

## Process improvements actioned this run (Phase 3)

- `execution_state.json` synced for all 5 already-merged-upstream EPICs (`status`, `pr_status`, `merge_gate.epics_merged`/`epics_pending`/`all_merged`) — see friction item 1's prior-cycle note above.
- ST-21 completed in-session via manual `workflow_dispatch` — see friction item 3 above.
- `execution_prompt.md` v3.66→v3.67 + `OPERATIONAL_GUIDE.md` v4.154→v4.155: governance-drift Step 1b wired into §3.2.A; a real, live SELF-DRIFT instance found and fixed in the same edit — see friction item 1 above.

## Outstanding deferred patches (Phase 3)

- The review-catches-first-pass-error pattern (friction item 2 above) — structural fix to `execution_prompt.md` §5.3 needed, target Head of Specs Team, next revision touching §5.3.

## Escalations (Phase 3)

None this cycle. (Both mid-sprint escalations, ESC-EXEC-20260811-01 and ESC-EXEC-20260811-02, were filed and resolved within the sprint itself — see `execution_escalations.md` and `sprint_close.md`'s Open Escalations section; neither remained open into sprint close.)

## Phase 4

**Phase:** Delivery Verification
**Cycle:** 2026-08-11__release-v8.6
**Section anchor:** `## Phase 4` (stable — cycle_id in field above, not in header)
**Filed:** 2026-08-12
**Reviewed by:** PMO Lead
**Prior cycle checked:** 2026-08-08__release-v8.5 (`lessons_learnt_cycle.md` `## Phase 4`) — 2 friction items. (1) EPIC-level `execution_state.json.test_scenarios` roll-up cross-reference missing from `execution_prompt.md`, recorded as a 2nd-consecutive-cycle recurrence escalation (claimed "not applied this cycle either", target Head of Specs Team). Cross-checked against `prompt_change_log.md` this run: the patch (`LL-v8.4-P4-01`) was in fact applied `2026-08-08` (`execution_prompt.md` v3.65→v3.66, STEP 3.1.A step 12 roll-up backstop) — **before** v8.5's own Phase 4 record was filed (`2026-08-10`) and before v8.5's sprint execution even began. v8.5 Phase 4's "still unapplied after 2nd cycle" recurrence-escalation claim was therefore factually incorrect at the time it was written; the underlying fix was already live. However, STEP 5 of this very run found the backstop still has a real, narrower gap: EPIC-03's `execution_state.json.test_scenarios` array (8 entries) omits `tests/e2e/saved-filters-calendar-view.spec.js`, though it is present in ST-05's own story-level `spec_references` and has real, DoQ-confirmed Playwright coverage (`SC-SFC-10`). Root cause: that file was added to ST-05's `spec_references` only during the in-EPIC DoQ remediation pass (the first-pass review's Select-dead-code finding was corrected mid-EPIC), which ran **after** the EPIC-level roll-up had already executed — the backstop is a point-in-time check at EPIC seal, not one that re-triggers when a story's `spec_references` changes post-remediation but pre-merge. Treated as a new, more precise friction item (see friction item 1 below) rather than a re-escalation of the original (now-resolved-in-substance, but not airtight) gap. (2) DoQ sign-off protocol does not require explicit restatement of the final CI-green confirmation when a story's PR needed a post-open CI-triggered fix — deferred at v8.5, target `execution_prompt.md §5.3` or `qa_evidence_template.md`. Checked against `prompt_change_log.md`: no matching entry found since `2026-08-10`. Not applied this cycle (no story touched that section). This is the 1st carry-forward since being filed — not yet a 2-cycle recurrence escalation; deferred again (friction item 2 below).

| friction_item | phase | type | classification | action | owner | target_date |
|---------------|-------|------|----------------|--------|-------|-------------|
| The `LL-v8.4-P4-01` roll-up backstop (applied 2026-08-08, confirmed genuinely live and correct for 5 of 6 EPICs this cycle) still missed one file: EPIC-03's `execution_state.json.test_scenarios` omits `tests/e2e/saved-filters-calendar-view.spec.js`, added to ST-05's own `spec_references` only during a mid-EPIC DoQ remediation pass, after the EPIC-level roll-up check had already run once at initial EPIC seal. Not a real coverage gap (the test exists, is DoQ-confirmed, verified via a real `tailwindcss` build check) — a metadata-completeness gap in the sealed record, caught here but not correctable by this routine (`execution_state.json` is sealed, outside `delivery_verification_prompt.md §5`'s write scope). Separately, v8.5 Phase 4's own recurrence-check claim about this same patch ("still unapplied after 2nd cycle") was itself factually wrong — the patch had already shipped 2 days before that claim was filed — suggesting the `lessons_learnt_prompt.md` §3.7 recurrence-check instruction to "confirm whether the corresponding prompt change was subsequently applied" isn't reliably matching entries in `prompt_change_log.md`. | Phase 4 | A | defer | Two related fixes: (a) `execution_prompt.md` STEP 3.1.A step 12's roll-up backstop should re-run (or the DoQ review step should explicitly re-trigger it) whenever a story's `spec_references` is edited during in-EPIC remediation, not only once at initial EPIC seal — target §3.2.A. (b) `lessons_learnt_prompt.md` §3.7's recurrence-check instruction should specify searching `prompt_change_log.md` by the friction item's own patch-ID tag (e.g. `LL-v8.4-P4-01`) rather than by date range or filename alone, so a genuinely-applied patch isn't re-flagged as an unresolved recurrence — target §3.7. | Head of Specs Team | next `execution_prompt.md` revision touching §3.2.A, and next `lessons_learnt_prompt.md` revision touching §3.7 |
| DoQ sign-off protocol still does not require explicit restatement of the final CI-green confirmation (run ID / `head_sha` match) when a story's PR needed a post-open CI-triggered fix — carried from v8.5 Phase 4, not applied this cycle either. This cycle had 2 concrete instances where it would have mattered: EPIC-01's PR #1358 CI-fixed-then-confirmed-green Playwright locator bugs, and EPIC-03's PR #1359 same pattern (SC-RCN-07, SC-SS-08b) — both EPICs' own QA evidence logs did restate the final CI outcome in prose this cycle, but that is authors following good practice voluntarily, not a required field the sign-off protocol enforces. | Phase 4 | D | defer | Same fix as originally specified at v8.5: add an explicit field/instruction to the Standard Sign-Off Block (`execution_prompt.md §5.3` or `qa_evidence_template.md`) requiring the DoQ sign-off to restate the specific final CI run confirming green (run ID or "all N checks green" + `head_sha` match) whenever a post-open CI-triggered fix occurred. | Head of Specs Team | next `execution_prompt.md` revision touching §5.3, or next `qa_evidence_template.md` revision (2nd carry-forward if unapplied by next Phase 4 check) |
| `DEV-NAV-ST06-01` was filed this sprint as a P1 deviation, but it is a *retroactive record* of a defect already fully resolved in the same commit that discovered it (v8.5, `41619410`) — not an open gap in this sprint's own output. The Deviation Severity Policy (`delivery_verification_prompt.md §7`) states P1 = hard block requiring documented PO+DoQ acceptance, written for the case of an *open* deviation blocking the current sprint's merge. It has no explicit carve-out for a deviation that is filed-and-already-resolved in the same breath, retroactively, for a prior cycle's fix. This run had to infer (not read directly from the prompt) that "Resolved" status exempts a P1 record from the acceptance-recording requirement — a different verifying agent could reasonably have read the table literally and treated it as an unresolved hard block. | Phase 4 | C | defer | Add an explicit clause to `delivery_verification_prompt.md §7`/STEP 3: a deviation record filed with `Status: Resolved` (fix already shipped, confirmed in the canonical spec's Known Deviations entry) is recorded in the Deviation Register for traceability but does not trigger the severity policy's hard-block/acceptance-recording requirement — only *open* deviations do. | Head of Specs Team | next `delivery_verification_prompt.md` revision touching §7 or STEP 3 |

**Recurrence Notes:** Friction item 1 is a new finding about the accuracy of the v8.5 Phase 4 record itself (not a recurrence of the underlying gap, which is confirmed resolved). Friction item 2 is a confirmed recurrence of the v8.5 Phase 4 deferred patch (1st carry-forward since filing — not yet an automatic §3.7 escalation). Friction item 3 is newly identified this cycle (no equivalent finding in `2026-08-08__release-v8.5`'s Phase 4 record).

## Recurrence Escalations (Phase 4)

None. (Friction item 2 is a 1st carry-forward, below the 2-cycle automatic-escalation threshold in `lessons_learnt_prompt.md` §3.7.)

## Process improvements actioned this run (Phase 4)

None applied this run — all three friction items above are deferred (`lessons_learnt_prompt.md`, `execution_prompt.md`/`qa_evidence_template.md`, and `delivery_verification_prompt.md` changes are all out of this routine's write scope per `delivery_verification_prompt.md §5`).

## Outstanding deferred patches (Phase 4)

- `execution_prompt.md` §3.2.A's `test_scenarios` roll-up backstop (`LL-v8.4-P4-01`) should re-trigger when a story's `spec_references` is edited during in-EPIC remediation, not only once at initial EPIC seal; `lessons_learnt_prompt.md` §3.7's recurrence-check matching instruction should specify searching `prompt_change_log.md` by patch-ID tag, not date/filename alone (friction item 1) — Head of Specs Team, next `execution_prompt.md` revision touching §3.2.A and next `lessons_learnt_prompt.md` revision touching §3.7.
- DoQ sign-off protocol does not require explicit restatement of final CI-green confirmation after a post-open CI-triggered fix (friction item 2, 1st carry-forward) — Head of Specs Team, target next `execution_prompt.md` §5.3 or `qa_evidence_template.md` revision.
- `delivery_verification_prompt.md §7`/STEP 3 has no explicit carve-out for retroactively-filed, already-`Resolved` deviations against the P0–P3 hard-block/acceptance-recording requirement (friction item 3) — Head of Specs Team, next `delivery_verification_prompt.md` revision touching §7 or STEP 3.

## Escalations (Phase 4)

None beyond the deferred patches recorded above.
