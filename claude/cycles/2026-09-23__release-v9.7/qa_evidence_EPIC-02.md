Owner: Director of Quality
Class: Planning Document (Class 4)
Status: Active
Last Updated: 2026-09-24

# QA Evidence — EPIC-02 (Frontend & UX Correctness) — 2026-09-23__release-v9.7

**EPIC:** EPIC-02 — Frontend & UX Correctness
**Cycle:** 2026-09-23__release-v9.7
**Sprint goal:** Ship PO-05 Lightweight Replay Mode end-to-end and clear the full-capacity, category-balanced debt-clearance slice across Frontend/UX correctness, Backend financial reliability, QA coverage, Governance process debt, Spec/data-model debt, and Ops/security verification — 29 stories, 28.00 days.
**Test scenarios used:** `tests/e2e/trade-plan-clone.spec.js` (SC-TPC-07/08/09 new), `tests/e2e/monthly-pnl-fees-not-recorded.spec.js` (new, SC-MPF-01..07), `tests/e2e/monthly-pnl-restatement.spec.js` (new, SC-MPR-01..07 incl. 05b/06b), `tests/e2e/alert-thresholds-empty-state.spec.js` (SC-ATS-12 new), `tests/e2e/alert-history-empty-state.spec.js` (new, SC-AHE-01/02), `tests/test_ui_copy_forbidden_phrases.py` (new, 43 tests)

**Frontend-visible-change note:** ST-02 to ST-06 change rendered UI, so the Autonomous DoQ sign-off class is unavailable for this EPIC (BLG-GOV-135 detection rule: `src/pages/**` and `src/components/**` modified). The Standard Sign-Off Block below applies. Every observable AC of ST-02 to ST-06 has Playwright coverage (CLAUDE.md §2 option (a)) — no AC relies on "code review only" and no deferred-to-staging backlog item is needed.

| ST Item | Spec Reference | What was built | Acceptance criteria | Result | Deviations |
|---------|----------------|-----------------|----------------------|--------|------------|
| ST-02 | `trade_plan.md` v1.16 §4.5; `docs/design/2026-09-23__release-v9.7/trade-plan-clone-setup-type/decision_record.md` | `TradePlan.js` clone effect now copies `setup_type` (including `null`) before the watchlisted-signal auto-fill effect runs, so the auto-fill's existing `prev.setup_type \|\| "Momentum Continuation"` guard no longer overwrites it | **AC-1** a clone of a plan with Setup Type X shows X, including when the ticker has a watchlisted signal — SC-TPC-07 (with signal), SC-TPC-09 (without), SC-TPC-08 (source `null` still auto-fills — no regression). **AC-2** design record and `trade_plan.md` state which fields a clone copies — `trade_plan.md` §4.5 Copied/Reset table lists `setup_type` (v1.16) and the ordering footnote; design record §2.1–2.2 (both landed at the design gate, re-read and confirmed present). **AC-3/AC-4** new Playwright scenario passes in CI — passes locally (9/9 in the spec); CI result: see "CI confirmation" below. SC-TPC-07 and SC-TPC-09 were confirmed to **fail without the fix** (`Received "Momentum Continuation"` / `""`) and pass with it. | Pass | None |
| ST-03 | `reports.md` v0.19 §Fees-Not-Recorded Visibility; `docs/design/2026-09-23__release-v9.7/monthly-pnl-fees-surfacing/decision_record.md`; `reports_endpoints.md` v0.13 | `Reports.js` `MonthlyPnlTable`: per-month `"{trade_count} · {k} no fees"` indicator in the Trades cell (`aria-label` per spec), non-dismissible aggregate `StandingAlert` notice (N = client-side sum of `null_fee_trade_count`, as the API has no aggregate), always-shown basis caption "Realised P&L is net of recorded fees." (net per the v9.6 ST-07 audit, `metrics_definitions.md` §Fee-Netting Basis) | **AC-1** a month with `null_fee_trade_count > 0` shows a visible indicator; a month with `0` does not — SC-MPF-01 (both in one table), SC-MPF-04 (all-zero: no indicator, no notice). **AC-2** Playwright test covering the above passes in CI — see "CI confirmation". **AC-3** observable-AC Playwright rule — SC-MPF-01..07. The spec's aggregate notice and basis caption go beyond the AC's literal wording but are part of the design-gate-approved UX (decision record §2.2–2.3), so they were built and covered too (SC-MPF-02/03/05/06/07). | Pass | None |
| ST-04 | `reports.md` v0.19 §Monthly Restatement Marker + §Summary Bar; `docs/design/2026-09-23__release-v9.7/monthly-pnl-restatement-surfacing/decision_record.md`; `reports_endpoints.md` v0.13 | `Reports.js`: amber "Restated" marker (History icon, `aria-expanded`, keyboard-operable) on months with `restated = true`, collapsed-by-default detail row (As reviewed / Now / Change via the canonical `formatCurrency`, signed, toned), the trade-count-only fallback line (`restated_diff_gbp = 0`), the "Restatement check unavailable." line, and the Tax Year summary notice for `summary.restated_month_count ≥ 1` with a link to the Monthly tab | **AC-1** a restated month shows its diff on the Monthly P&L page — SC-MPR-01/02/03/04. **AC-2** a tax year with `restated_month_count > 0` shows the notice on its summary bar — SC-MPR-06 (also asserts the link switches to the Monthly tab), 06b (singular), 07 (absent at 0). **AC-3/AC-4** Playwright tests covering both pass in CI — see "CI confirmation". 7 of the 9 positive-behaviour scenarios were confirmed to fail against the pre-ST-04 source (the 2 that pass on it are the absence checks). | Pass with notes | DEV-v9.7-ST04-01 → BLG-SPEC-170 (P4): the spec's detail-row `{snapshot_date}` and the "check unavailable" trigger cannot be supplied by the live API; implemented as undated "As reviewed" and "no row carries the snapshot fields" respectively. Disclosed, not silently narrowed. |
| ST-05 | `design_system.md` §Data States (v1.22, no-trailing-period microcopy pattern) | `AlertThresholdsSection.js` empty-state heading now "No alert rules configured" (trailing period removed) | **AC-1** empty-state heading renders `"No alert rules configured"` — SC-ATS-12 (`toHaveText` exact match; a trailing period fails it). **AC-2** Playwright coverage confirms the rendered heading (wording-only; Playwright is used rather than the FI-P3-02 code-review substitute). **AC-3** Playwright test passes in CI — see "CI confirmation". The 11 pre-existing SC-ATS scenarios still pass. | Pass with notes | DEV-v9.7-ST05-01 → BLG-SPEC-169 (P4): `notifications.md` still specifies the heading with a trailing period (spec text stale vs. the design system and now the code). |
| ST-06 | `design_system.md` §Data States (v1.22) | `NotificationsHistory.js` empty-state heading now "No alert history yet" (trailing period removed) | **AC-1** empty-state heading renders `"No alert history yet"` — SC-AHE-01 (exact match), SC-AHE-02 (body text unchanged). **AC-2/AC-3** as ST-05. | Pass with notes | Same DEV-v9.7-ST05-01 / BLG-SPEC-169 (`notifications.md` §Alert History heading). |
| ST-07 | `scripts/check_ui_copy_forbidden_phrases.py`; `.github/workflows/ui-copy-boundary-lint.yml`; `strategy_rules.md` §13.2 (no governing frontend spec — Case D CI/tooling) | New CI lint scanning `src/` string literals, template parts and JSX text for the §13.2 prescriptive/prediction phrases, imported from `run_ai_output_boundary_sample_audit.py` (one canonical list); justified allow-list (`scripts/ui_copy_lint_allowlist.json`) keyed on file + exact literal | **AC-1** CI fails on a disallowed phrase in a new string literal — `test_disallowed_phrase_in_a_new_string_literal_fails`, `test_main_exits_nonzero_on_a_violation_and_zero_when_clean` (runs the real `main()`), plus detection tests per literal kind (string, template part, template expression, JSX text, attribute). **AC-2** allow-list entries require a justification — missing/empty/too-short/whitespace-only all rejected and the entry does not suppress the underlying flag; an entry cannot cover a *new* literal using the same phrase; stale entries fail. **AC-3** "Playwright test covering the observable AC passes in CI" — **not applicable as written**: the lint is CI tooling with no rendered UI, so there is no browser-observable behaviour to drive (the design gate classified ST-07 Design Not Applicable, no UI surface). The AC-1/AC-2 behaviour is covered by pytest instead. The boilerplate AC line appears to have been carried over mechanically from the frontend stories; this is flagged for the DoQ to rule on rather than resolved in the engine's own favour. | Pass with notes | None (see AC-3 note; RISK-02: EPIC-01's approved retrospective banner wording produces zero hits, so no allow-list entry was needed for it) |

**Observable-AC coverage map (CLAUDE.md §2 frontend-visible-change rule):**

| Story | Observable AC | Playwright scenario(s) |
|-------|---------------|------------------------|
| ST-02 | Cloned form shows source Setup Type (with and without watchlisted signal) | `trade-plan-clone.spec.js` SC-TPC-07/08/09 |
| ST-03 | Indicator on months with NULL-fee trades, absent otherwise; aggregate notice; basis caption | `monthly-pnl-fees-not-recorded.spec.js` SC-MPF-01..07 |
| ST-04 | Restated marker, expandable diff, fallback line, unavailable line; Tax Year notice + link | `monthly-pnl-restatement.spec.js` SC-MPR-01..07 |
| ST-05 | Heading text without trailing period | `alert-thresholds-empty-state.spec.js` SC-ATS-12 |
| ST-06 | Heading text without trailing period | `alert-history-empty-state.spec.js` SC-AHE-01/02 |

**QA test coverage:**
- Scenarios run: the 6 files above. Independently observed by the agent-mediated DoQ reviewer (single worker): **41/41** across the five EPIC-02 Playwright specs (9 `trade-plan-clone` + 7 `monthly-pnl-fees-not-recorded` + 9 `monthly-pnl-restatement` + 14 `alert-thresholds-empty-state` + 2 `alert-history-empty-state`), and **53/53** pre-existing regression specs (`monthly-pnl-avg-per-trade`, `-csv-export`, `-realized-unrealized`, `reports-*`). `test_ui_copy_forbidden_phrases.py`: 43/43 after the review fixes (34 at first submission; +9 regression tests for wrapped/irregular whitespace, `&nbsp;`, and `https://` in JSX text). (An earlier draft of this line cited 22/22 and 52/52; those figures omitted a spec and did not reproduce — corrected here per review finding N1.)
- Regression areas checked: full backend suite (Phase A/stub `DATABASE_URL`, `backend/.venv`) — **1792 passed, 12 skipped, 0 failed** (run before the review fixes, including 34 ST-07 tests; the 9 added since are in a file that passes 43/43 and CI re-runs the whole suite on the final head — see CI confirmation); the 53 pre-existing Reports/Notifications-adjacent Playwright specs above; the full Playwright suite runs in CI (below). Areas: Reports (Tax Year + Monthly tabs), Trade Plan clone/form, Notifications preferences + history, CI workflows/scripts.
- Known deviations: DEV-v9.7-ST04-01 (reports.md, P4, BLG-SPEC-170) and DEV-v9.7-ST05-01 (notifications.md, P4, BLG-SPEC-169) — both spec-text/API gaps, deviation documentation only in the specs; no P0/P1 deviations. All 6 stories' deviation checks completed.

**Implementation notes (not deviations — interpretations the spec left open):**
- ST-03/ST-04 notice wording uses grammatical singular at 1 ("1 closed trade has no fees recorded, so these figures may not reflect its costs."; "Includes 1 restated month …") — the spec's "trade(s)"/"month(s)" is read as a plural marker, consistent with the backend's own `restated_months_notice` pluralisation. Covered by SC-MPF-03 and SC-MPR-06b.
- ST-04's "Restatement check unavailable." trigger: the spec names none (see DEV-v9.7-ST04-01). Implemented as "rows present but none carries a boolean `restated`", i.e. the response cannot answer the restatement question. Covered by SC-MPR-05/05b.
- ST-03's per-month indicator puts `data-testid` and `aria-label` on the `<td>` (not a nested `<span>`): `aria-label` on a role-less `span` is an axe `aria-prohibited-attr` risk, and the `td` (role `cell`) permits it. The spec's attributes are otherwise unchanged.

**CI confirmation** (head `9086f5fe06b73d0d6a12ee071f66a94ddd9510d6`, the final code commit; later commits on this branch change only this evidence log, `execution_state.json` and the PR record). Observed on the `exec/2026-09-23__release-v9.7/EPIC-02` push, all `completed / success`:

| Workflow | Run ID |
|----------|--------|
| Playwright E2E Acceptance Tests (full suite, incl. all new specs) | 36022365450 |
| Critical-Path Smoke Tests (Playwright) | 36022365429 |
| CI Pytest Suite (incl. `test_ui_copy_forbidden_phrases.py`, 43 tests) | 36022365523 |
| Portfolio Integration Tests | 36022365550 |
| Golden Output Regression Gate | 36022365368 |
| Endpoint Coverage Report | 36022365375 |
| Service Layer Coverage Gate | 36022365425 |
| Governance Sync Loop (issues #1764–#1769 closed) | 36022365355 |

This satisfies every "Playwright test … passes in CI" AC clause for ST-02..ST-06 (real GitHub Actions run, not a sandboxed pass). None of the ACs is in the focus-restoration / interaction-timing sub-class (LL-v8.3-P3-02). **`ui-copy-boundary-lint.yml`** is triggered only by `pull_request` and `push` to `main`/`develop`, so it cannot run on an `exec/**` push: it first runs when the PR opens, and its result is to be appended here (see "Post-PR-open CI addendum" below) — the same script already passes locally and its logic runs in CI Pytest Suite above. For ST-07, the CI evidence of record is that workflow run plus the CI Pytest Suite run.

**Agent-mediated DoQ review (first pass) and its resolution:** an independent review returned *Blocked* on (B1) an ST-07 false negative — the lint searched the raw literal, so a phrase wrapped across lines / split by a tab or double space / joined by `&nbsp;` evaded the single-space patterns (reproduced by the engine before fixing; the Babel cross-check could not expose it because no such literal exists in `src/` today) — and (B2) placeholders in this log. B1 is fixed with 9 regression tests (commit `9086f5fe`); B2 is resolved by this edit and the CI section. Non-blocking: N1 counts corrected above; N2 tokeniser limits (a `//` URL in JSX text — fixed; concatenated literals, non-copy strings such as import paths/object keys, `.ts/.tsx`, `a > b < c` code read as JSX text — documented in the script docstring as known limits, none occurs in `src/` today); N3 SC-TPC-07 now waits on the auto-fill's own output instead of a fixed delay; N5 deviation targets changed from `TBD` to `Backlog` with a review cadence; N6 (`aria-label` reads "1 trades" at count 1 — the spec's literal string; basis caption also shows in the empty/error states — spec says never omitted) left as-is. The reviewer noted the unavailable-line trigger (ST-04) cannot fire against today's real API — already disclosed as DEV-v9.7-ST04-01.

**Tooling verification (ST-07):** the dependency-free tokeniser was checked against `@babel/parser` over all 211 `src/` files (~12,000 literals): identical single hit (the allow-listed TradePlan Entry Rationale placeholder), ≈99.9% literal coverage. The 14 uncovered literals are HTML-entity JSX text (`&amp;`) and quote-split JSX fragments — benign, and documented in the script's docstring. This surfaced and fixed a real false-negative path (string literals inside template-literal `${…}` expressions) before commit.

---

## Standard Sign-Off Block

- [x] All acceptance criteria verified against canonical spec
- [x] No unresolved P0 or P1 deviations
- [x] Regression areas checked
- [ ] For any frontend component making direct URL construction (not via api.* wrapper): confirm the URL-base variable is exposed on the imported object — N/A, no new direct URL construction; the changed components use the existing `apiFetch`/`base44.baseUrl` patterns unchanged
- Signed off by: Sprint Execution Engine (agent-mediated, Director of Quality role — §5.3)
- Date: 2026-09-24
- Comments: [Agent-mediated Director of Quality sign-off, performed on the user's explicit direction ("Open the PR and have DoQ and product owner provide sign off") — pending human Director of Quality confirmation; the merge-gate QA sign-off remains an always-human gate (execution_prompt.md §5.3).] Two independent review passes by a Director-of-Quality-role subagent. **Pass 1 — Blocked** on (B1) an ST-07 lint false negative (phrases wrapped across lines / split by tab, double space or `&nbsp;` evaded the single-space patterns) and (B2) unfilled placeholders in this log. **Pass 2 — Approved**, after B1 was fixed with 9 regression tests (43 total) and B2 resolved; the reviewer re-ran the first-pass bypass probes (all now detected), re-ran the `@babel/parser` differential (14 benign misses of 12,010 literals, unchanged), and independently observed 41/41 EPIC-02 Playwright scenarios, 53/53 pre-existing regression scenarios, 43/43 pytest and the lint PASSED. Non-blocking residuals accepted, none present in `src/`: obfuscation-grade bypasses (numeric/hex escapes and HTML numeric entities for a space, invisible characters), a multi-line JSX attribute string, and cross-node splits (`'you ' + 'should'`, `You{' '}should`) — the docstring lists the concatenation limit; the remainder are recorded as BLG-QA-194 (filed alongside this sign-off). **Answers to the open questions:** (1) ST-07 AC-3's Playwright clause is accepted as not applicable to a CI-tooling story with no rendered UI (CLAUDE.md §2's Playwright rule is scoped to frontend-visible changes); its behaviour is covered by pytest, with the CI Pytest Suite run and the `ui-copy-boundary-lint.yml` PR run as the CI evidence. (2) `Pass with notes` confirmed for ST-04/ST-05/ST-06 (no AC narrowed, so not `Pass_with_deviation`); P4 and the BLG-SPEC-169/170 routing confirmed; both deviations verified accurate against `reports_endpoints.md` v0.13 and `notifications.md` lines 199/370. ST-07 is `Pass with notes` now that B1 is fixed. Frontend testing gate satisfied: every observable AC of ST-02..ST-06 has a Playwright scenario that passed in real CI (table above).

**Open questions for the Director of Quality (answered in the sign-off Comments above):**
1. **ST-07 AC-3** — accept "not applicable, covered by pytest" for the boilerplate Playwright clause on a CI-tooling story with no UI surface, or require something further?
2. **ST-04/ST-05/ST-06 `Pass with notes`** — every AC of these stories is fully met (none narrowed), so `Pass_with_deviation` (defined for a narrowed/partially-unmet AC) was not used; the DEV-v9.7-ST04-01 / DEV-v9.7-ST05-01 spec-text gaps are recorded in the Deviations column. Confirm P4 and the BLG-SPEC-169/170 routing, or reclassify.

**Post-PR-open CI addendum (PR #1802, head `194e9b53`):** all **41 of 41** PR checks `pass`, observed 2026-09-24 after the PR opened — including **UI Copy Boundary Lint (ST-07)** (run 36023184953, its first-ever run, closing the ST-07 CI-evidence condition above), all 8 **Playwright E2E Acceptance Tests** shards (run 36023184786), **verify_governance** and **DoQ Sign-off Staleness Lint (ST-18)** (run 36023184707, which validates this log's sign-off format). PR merge state is `BLOCKED` solely on the required human review (`reviewDecision: REVIEW_REQUIRED`). Human Director of Quality confirmation, Product Owner acceptance and the merge remain outstanding. The commit adding this addendum changes only this log and `backlog.md`; no code changed after `9086f5fe`.
