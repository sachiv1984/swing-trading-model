Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-09-23
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-09-23

Class: Operational Record (Class 3)

---

# Run Manifest — v9.7

Invocation: `plan release v9.7` (natural-language equivalent of `plan release --version "v9.7"`; no `--capacity`/`--timebox`/`--mode`/`--issues` flags given — all default)

Session start (UTC): 2026-09-23T18:21:19Z
Session end (UTC): 2026-09-23T19:30:00Z — elapsed 1h08m41s (computed, not typed)

## STEP -1 — Preflight Gate

**-1.1 Required files present:** `claude/charter/team_charter.md`, `claude/charter/document_lifecycle_guide.md`, `claude/strategy/strategy_rules.md`, `claude/roadmap/current_roadmap.md`, `claude/backlog/backlog.md` — all present. No `--issues gh`/`import` requested this invocation, so the `gh_issue_template.md` prerequisite (IMP-48) does not apply.

**-1.2 Release exists on roadmap:** No formal `## v9.7` roadmap section exists (`current_roadmap.md` §1 "Next planned release: [TBD]"). Cleared via the Option(b)-equivalence rule: the most recent rebalance (`2026-09-19__scheduled`, still the most recent — no rebalance has run since) recorded at its own STEP 8.1 Empty Now Horizon Gate "PO decision (STEP 8.1): Option (b) — defer. Now horizon intentionally empty for this cycle… backlog-driven release; every roadmap Arc-gated item is independently unmet" (`claude/cycles/2026-09-19__scheduled/run_manifest.md` line 118; 6th consecutive firing). §-1.2 cleared via STEP 8.1 Option(b) decision from rebalance `2026-09-19__scheduled` — the **15th consecutive** release cycle relying on this equivalence (extending the v8.5–v9.6 pattern). Note: this is the same rebalance record already consumed as precedent for `v9.6`'s own opening; its written rationale explicitly named `v9.6` ("this rebalance immediately precedes `plan release v9.6`"), not `v9.7`. No newer rebalance has run since `v9.6` shipped (2026-09-23), so no fresher Option(b) record exists to cite. Reused here on the basis that the underlying condition the rebalance was clearing — an empty Now horizon, fully backlog-driven release cadence, no formal roadmap release section mechanism in current use — is unchanged, not because the record's own prose anticipated a second reuse. Flagged for visibility, not treated as a blocking ambiguity: this is the same class of precedent-stretching this repo has applied consistently for 14 prior cycles between rebalances that ran less frequently than every release.

**-1.3/-1.4 Roles and write test:** All 9 required role agent files confirmed present in `claude/agents/` (Product Owner, Head of Specs Team, PMO Lead, Director of Quality, Infrastructure & Operations Owner, Strategy Rules & System Intent Owner, FinOps & Resource Architect, Facilitator, Challenger). Write test passed (`.write_test` created and removed cleanly in the new cycle folder).

**-1.5 Prior cycle lessons learnt closure check (advisory):** `prior_cycle` = `2026-09-21__release-v9.6`. `lessons_learnt_closure.md` exists; no `action: now`/`action-now`-marked items found in its body. Its `## Carry-Forward` section (2 items) named `ESC-CLOSE-20260923-01` and `ESC-CLOSE-20260923-02` as open, SLA 2026-09-26 — both were resolved same-day per the most recent commit (`995ceb2b`, "Resolve ESC-CLOSE-20260923-01/-02 — post-ship closure v9.6 follow-up"), before this session started. No advisory raised.

**-1.6 Post-Ship Precondition Check (hard gate):** `post_ship_complete: true`, `next_cycle_unblocked: true` for prior cycle `2026-09-21__release-v9.6` — both confirmed in `.claude_current_state.json`. **SLA-breach carry-forward check (AUD-2026-09-14-001):** `open_escalations` holds 6 entries (`ESC-EXEC-20260921-02` through `-08`), all at `disposition: Resolved` — none at `disposition: Open`. `deferred_escalations` holds `ESC-EXEC-20260910-01` at `disposition: Deferred` — not `Open`, out of scope for this check. No breach found. Gate clears.

**-1.7 Prompt Change Log Integrity Check (advisory):** `release_planning_prompt.md` v2.55 present in `claude/system/prompt_change_log.md` (row 2026-09-23). No advisory raised.

**-1.8 Amendment In Progress Guard (hard gate):** `.claude_current_state.json.status` = `Closed`, not `Amendment_In_Progress`. Passes.

**-1.9 Stale Backlog Lock Preflight Check (advisory):** `claude/backlog/.lock` does not exist; `claude/roadmap/.lock` does not exist. Nothing to check.

**Branch:** `main`, working tree clean at session start.

```yaml
artifacts.run_manifest: present
```

---

## STEP 0 — Run Manifest + State Initialization

**Carry-Forward Advisory (ST-15):** Most recently completed cycle = `2026-09-21__release-v9.6` (`post_ship_complete: true`). See -1.5 above — both carry-forward escalations already resolved before this session. Reviewed: 2 items from cycle `2026-09-21__release-v9.6`, both closed.

**scored_initiatives.md Load (ST-14):** Loaded `claude/scoring/scored_initiatives.md` — 0 active roadmap initiatives (unchanged for many consecutive cycles). No pre-assigned Effort Bands exist for any candidate. scored_initiatives.md: 0 matching items — not present for this cycle's fully backlog-driven scope.

Cycle: `2026-09-23__release-v9.7`
Mode: standard (default — not specified in invocation)
Issues mode: none (not specified in invocation)
Auto-escalate: true (default)

```yaml
artifacts.run_manifest: present
status: Initialized
```

---

## Scope Construction — Ready Pool & Selection Method

### Gate-Detection Procedure (§1.3a, scripted)

`python3 scripts/scan_backlog_gate_conditions.py` run against `claude/backlog/backlog.md`: **200 items scanned** (post-groom count at v9.6 closure; no new idea intake window has run since). Result: **129 gated/conditional items; 4 data-quality warnings** (`BLG-FEAT-73`, `BLG-FEAT-74`, `BLG-FEAT-76`, `BLG-SPEC-156`). So 200 − 129 = **71 items the scan leaves ungated.**

### Manual dispositions applied to the scan's output (each item read individually, per §1.3a's LL-v9.5-Release-01 note)

**Data-quality warnings (4):**
- `BLG-FEAT-73` (own text: "may not enter sprint planning until independently reconfirmed met"; PO-set re-check not due before 2026-11-09 or 10 new linked `trade_plans`) — substantively gate-blocked. **Excluded.**
- `BLG-FEAT-76` (own `Depends on`: hard-blocked on `BLG-FEAT-73` and `BLG-FEAT-75` shipping first — neither has) — substantively gate-blocked, not just ambiguous free text. **Excluded.**
- `BLG-SPEC-156` (`Provisional-Target`: "Unscheduled (pre-work before PO-04 gate)") — describes a dedicated §13 pre-assessment performable *ahead of* the PO-04 gate, not work blocked by it. Same disposition as `v9.6`. **Retained in ready pool.**
- `BLG-FEAT-74` (`Provisional-Target`: "§13 pre-clearance PASS 2026-09-23 … gated only by normal Release Planning prioritisation given its VH (>2 weeks) effort size, not by §13") — **its gate has cleared this week.** `ESC-EXEC-20260921-05` (PO-05 §13 determinism pre-clearance) resolved PASS 2026-09-23, per `docs/product/decisions/po05_section13_preassessment.md`. This is the first cycle in which `BLG-FEAT-74` is genuinely ready, not merely gate-adjacent — it was one of only two P1 items ever flagged by the Perennial-Return Check (§1.4a.1 retroactive assessment, `v8.1`), both previously gate-blocked. **Retained in ready pool** — the cycle's sole ready P1 item.

**Date-lapsed gates (6, per the scan's separate list) — read individually, per §1.3a's mandatory pre-ready-pool check:**

| Item | Lapsed gate anchor | Verification performed this session | Disposition |
|------|---------------------|---------------------------------------|--------------|
| `BLG-FEAT-55` | ~2026-07-25 (30 days AI chat usage) AND §13 review for chat persistence | No §13 review record for chat persistence found anywhere in `docs/product/decisions/` (checked). Second condition unmet. | **Remains gated.** |
| `BLG-SPEC-65` | Same gate as `BLG-FEAT-55` | Same finding. | **Remains gated.** |
| `BLG-OPS-53` | Anchor 2026-05-22 (ship date); real gate is `claude_audit_log` 6+ months old, ~Nov 2026 | Not yet reached. | **Remains gated.** |
| `BLG-GOV-121` | 2026-07-04 anchor; real gate is SI-05 Phase 2 activation decision, dependent on SI-02 shipping | SI-02 gate confirmed NOT MET as of the 2026-09-19 rebalance (0 linked `trade_plans`, read-only staging query) — unchanged since. | **Remains gated.** |
| `BLG-FEAT-62` | 2026-06-23 anchor; real gate is ≥20 closed trades with ≥3 distinct `setup_type` values (≥3 trades each) post-PT-04 | Trade volume remains ~1–2/month per every recent rebalance's data-density read; condition not plausibly met. | **Remains gated.** |
| `BLG-FEAT-92` | 2026-09-03 anchor; inherits `BLG-FEAT-30`'s gate (screener live ≥60 days AND ≥60 closed trades with attribution), tracked via PO decision (`decisions--2026-09-07__release-v9.2.md`, reaffirmed each cycle since) | `BLG-FEAT-30` itself remains gated (still in this scan's 129). | **Remains gated**, consistent with the standing PO decision. |

No new date-lapsed items surfaced beyond this set; `BLG-FEAT-59`/`60`/`63`/`BLG-FE-84` (the `BLG-GOV-345` set cleared/re-gated at `v9.6`) remain conditional pending the 2026-09-24 AI feature usage review, which has not yet occurred (today is 2026-09-23) — unchanged, still excluded.

**Already-resolved item the scan does not detect (found this session, same failure class as `v9.6`'s Friction Item 1 — `BLG-GOV-335`/`336`/`337`/`326`):**
- `BLG-FE-189` (Cash Management unreachable from shipped UI) carries a full `**Resolution (2026-09-22):**` block — fixed same-session on `hotfix/cash-management-entry-point-and-silent-errors`, Playwright coverage added (`tests/e2e/cash-management-global-trigger.spec.js`, 6 ACs passing), left in the active backlog only for audit-trail purposes per this repo's established pattern (cf. `BLG-GOV-346`). It was the scan's only other P1 candidate besides `BLG-FEAT-74`. **Excluded** — already shipped, not sprint-ready work. Recommend `groom backlog` archive it at the next run (it is not itself the type of ephemeral-section item that engine currently targets, so flag explicitly rather than assume auto-capture).

### Ready pool

71 (scan-ungated) − 2 (`BLG-FEAT-73`/`76`, gate-blocked) − 1 (`BLG-FE-189`, already resolved) = **68 items, 61.35 estimated days** (per the effort-to-days rule below; `BLG-FEAT-74`'s VH figure is a PO-directed estimate, not derived from the rule — see Product Owner Decision below).

Priority distribution of the ready pool: **P1: 1** (`BLG-FEAT-74` — the first cycle this pool has ever held a genuinely ready P1 item), **P2: 6** (`BLG-BE-127`, `BLG-FE-186`, `BLG-FE-187`, `BLG-FE-188`, `BLG-OPS-168`, `BLG-QA-188`), **P3/P4: 61**.

### Product Owner Decision — BLG-FEAT-74 seating and effort estimate (user-directed, 2026-09-23)

`BLG-FEAT-74` (PO-05 Lightweight Replay Mode) is Arc 4's flagship long-term validation feature, gate-blocked since 2026-07-10, now cleared. Its `Effort` field states only `VH (>2 weeks)` — no canonical midpoint exists in `workforce_capacity.md`'s Effort Band table (which stops at `L`), and its own scope note flags "exact scope… to be confirmed by canonical spec before implementation." This was surfaced to the Product Owner directly (outside the routine's own escalation machinery, as a scope-shaping question rather than a blocker) given the stakes of committing roughly half a release's capacity to a single item with an open scope question. **Decision: seat it as firm P1 scope this cycle, and still use full capacity** — i.e. do not let this item's size crowd out the round-robin fill; continue selecting P2/P3/P4 items up to the top of the confirmed ~24–28 day band as usual. Effort estimated at **12.0 days** (a working mid-point of "2+ weeks," consistent with 10 working days plus margin for the §13 pre-clearance's 6 binding conditions carrying into implementation and the scope-definition work itself). This estimate is explicitly approximate — flag for a `sprint_planning_notes.md` phasing discussion (spec-definition first, then backend, then frontend) given the item's own "exact scope to be confirmed" caveat.

### Effort-to-days rule (per `workforce_capacity.md`'s codified precedence rule, added `LL-v9.6-P-Closure-01`)

An item's own explicit day range/midpoint in its `**Effort:**` field always takes precedence (range midpoint used where a range is given); the canonical band-letter midpoint (XS=0.15, S=0.5, M=2.5, L=3.5) applies only when the field gives a bare letter with no more specific figure. `BLG-FEAT-74`'s `VH` has no canonical entry — its 12.0d figure is the Product Owner's own estimate per the decision above, not a mechanical application of this rule.

### Selection method applied (§1.4c)

P1-then-P2-first (`BLG-FEAT-74` seated alone at P1; then all 6 ready P2 items, ascending ID), then category-balanced round-robin over the remaining ready P3/P4, oldest-filed-first within each category's turn, stopping at the top of the confirmed ~24–28 day band (28.00d, "use full capacity" per explicit user instruction, matching the `v8.5`–`v9.6` pattern). Categories (alphabetical, matching `v9.6`'s named set): Backend, Financial Reporting, Frontend/UX/Product, Governance/Process, Ops/Security, QA, Spec/Documentation. No ready items fell in the Financial Reporting category this cycle. Oldest-first key = latest date mentioned in the item's `Source` field, ties broken by ID number. An item that would overshoot 28.00d is skipped and the pass continues with the next item in that category's queue that fits (3 items skipped on this basis and left available for `v9.8`: `BLG-FE-184`, `BLG-FE-185`, `BLG-BE-128`).

**Result:** P1-then-P2-first, category-balanced, oldest-first selection from a **68-item / 61.35-day ready pool** → **29 items, 28.00 estimated days** (100.0% of the 28-day ceiling; exactly at the top of the band). Selected by category: Frontend/UX/Product 7 (incl. `BLG-FEAT-74`), Backend 6, QA 5, Governance/Process 4, Spec/Documentation 4, Ops/Security 3. **39 ready items (21.35 days) left unselected** on capacity grounds — available for `v9.8`.

No override of §1.4c's mechanical round-robin was applied beyond the explicit P1-seating decision recorded above.

### Advisory checks

**Backlog Age Advisory (§1.1):** No spec/documentation debt item in the selected pool has appeared in 2+ prior release cycles' ready pools without a story assignment — all 4 selected `BLG-SPEC-*` items (`147`, `149`, `150`, `151`) are newly filed this cycle or last cycle. Nothing to promote.

**Provisional-Target Advisory (§1.2):** Per `release_planning_prompt.md` §1.4c's `ESC-CLOSE-20260923-01` resolution (this week), same-release `Provisional-Target` tags carry no selection weight and were not used as a tiering signal. 1 selected item (`BLG-FEAT-74`) carries a substantive (non-tiering) `Provisional-Target` note; the rest carry `TBD`/`Unscheduled`/no matching `v9.7` tag (none exists yet, since this is the first `v9.7` plan).

**Design-Gate Language Scan (§1.3) / STEP 4.1 classification, including the v2.51 Scope-text scan:** `BLG-FEAT-74` ships an observable frontend selector and output view (explicit scope: "date range or trade-set selector," "clearly-labelled retrospective/deterministic output view"). `BLG-FE-186`, `BLG-FE-187`, `BLG-FE-188`, `BLG-FE-178`, `BLG-FE-179`, `BLG-FE-183` are all frontend-visible fixes/additions (clone-plan display, P&L audit-flag/restatement surfacing, empty-state heading text, predictive-language lint of UI copy). No other selected item's `Scope`/`Acceptance Criteria` text contains UI-shipping verbs/nouns. → **`design_gate_required: true`.**

**Gate-Condition Proximity Scan (§1.4):**

| Item | Gate condition | Current trajectory | Projected clear date |
|------|-----------------|----------------------|------------------------|
| `BLG-GOV-140`/`141`/`142`, `BLG-OPS-88` | 90-day AI review (90 days post-v6.2 ship) | Date-driven | 2026-09-24 |
| `BLG-FEAT-59`/`60`/`63`, `BLG-FE-84` | Named-owner verification at the 2026-09-24 AI review | Evidence due tomorrow | ~2026-09-24 |
| `BLG-OPS-92` | New CVE/deprecation, or quarterly hygiene cadence | Date-driven | ~2026-10-06 |
| `BLG-FEAT-58` | Arc 4 PO-02 data model (6+ months AI-summarised journal data) | Date-driven | ~2026-10-20 |
| `BLG-FEAT-73` | Not re-checked before 2026-11-09 or 10 new linked `trade_plans` | 0 linked at 2026-09-19 (read-only staging query, context only) | ≥ 2026-11-09 |
| `BLG-OPS-53` | `claude_audit_log` 6+ months old | Date-driven | ~2026-11 |
| SI-02 (20+ trades with plans) / PO-04 (50+ trades with plans) | Data density | Trajectory unknown — 0 linked `trade_plans` at the 2026-09-19 rebalance | Unknown |

**Arc 4 data density sub-check:** data not available — no production credential is used by this routine; per standing session memory, the `DATABASE_URL` present in this environment is confirmed to point at the **staging** Supabase instance, and querying it requires asking the user first (not done this session — no query was needed to resolve any gate this cycle). The 2026-09-19 rebalance's read (0 linked `trade_plans`; SI-02/PO-02/PO-04 gates NOT MET) is carried forward unchanged. Product Owner to surface at readiness review. Not a halt condition.

**Perennial-Return Check / Within-Sprint Date Gate (§1.4a/§1.4b):** Perennial-Return — `BLG-FEAT-74` has been flagged by this check across multiple prior cycles (`v8.0` onward, per the `v8.1` retroactive assessment) always on Option (a) "keep conditional"; this cycle its gate genuinely cleared, so the Perennial-Return question is now moot for it (it is firm scope, not conditional). No other selected item appears in `v9.6`'s `stage4_backlog_slice.md` with a `returned_to_backlog`/`deferred` marker. **§1.4b within-sprint date gates:** `BLG-GOV-140`/`141`/`142`, `BLG-OPS-88` (gate 2026-09-24) and `BLG-FEAT-59`/`60`/`63`, `BLG-FE-84` (owner verification at the 2026-09-24 review) fall within the plausible execution window of this sprint (planning 2026-09-23; recent sprints have run 1–2 calendar days planning-to-seal) and are therefore **classified: conditional**, not firm capacity, consistent with `v9.6`'s treatment. Not added as conditional stories — the firm pool already sits at the top of the capacity band.

**Skill-Silo / build-and-ship rotation (`roadmap_prompt.md` §7.1; `release_planning_prompt.md` STEP 3 rotation guideline):** the 2026-09-19 rebalance (most recent) reported the Skill-Silo Alert at 98.8% (5th consecutive worsening) and satisfied the ≥2 build-and-ship pull-forward requirement with `BLG-FEAT-96`/`97` in `v9.6`. This cycle leads with `BLG-FEAT-74` (EPIC-01) — a genuine, large, user-facing build-and-ship item, not a debt/governance item — satisfying the rotation guideline's intent directly without needing to manufacture scope. The EPIC table below leads with this execution-heavy EPIC as this cycle's rotation slot.

```yaml
artifacts.stage1_readiness: pass
```

---

## Friction Items (carried into `lessons_learnt.md`)

1. **A second already-resolved item slipped past the scripted scan, same failure class as `v9.6`'s Friction Item 1.** `BLG-FE-189` carried a full `**Resolution (2026-09-22):**` block (fixed same-session, Playwright-covered) but no `✅ COMPLETE` banner, so the gate scan (which classifies by field presence, not item state) surfaced it as an ungated ready P1 candidate. `v9.6`'s equivalent finding was `BLG-GOV-335`/`336`/`337`/`326` (which did carry `✅ COMPLETE` banners, so a banner-presence check would have caught them but would **not** have caught `BLG-FE-189`, which uses a different resolved-item convention entirely). Two different "already done" conventions now exist in `backlog.md` (`✅ COMPLETE` banner vs. inline `**Resolution (...):**` block) and neither is scanned for. Recommend the scripted gate-detection script (or a lightweight sibling check) also flag any item whose body contains a `**Resolution (...):**` or `**Resolved (...):**` field, the same way it already flags gate language.
2. **The `VH` effort band has no canonical midpoint**, and this cycle needed one for the first time (`BLG-FEAT-74`, the first-ever ready VH item). `workforce_capacity.md`'s Canonical Effort Band → Days Conversion Table stops at `L` (~3.5d). A Product Owner ad hoc estimate (12.0d) was used this cycle — recommend the table be extended with a `VH` row once a second real VH item is scored, so this isn't re-derived ad hoc each time.
3. **The STEP -1.2 Option(b) equivalence has now been reused twice from the same underlying rebalance record** (`2026-09-19__scheduled`), whose own prose named `v9.6` specifically as the release it was clearing the way for, not `v9.7`. No hard rule currently distinguishes "this rebalance's Option(b) decision is valid for the next release only" from "valid until the next rebalance runs, however many releases that spans." This session read it as the latter (consistent with `v8.5`–`v9.6`'s 14-cycle pattern of relying on stale rebalances generally), but flags it because this is the first time the *same single rebalance record* has been cited twice across two different release labels — worth a Head of Specs Team ruling on whether §-1.2 should require the cited Option(b) record to postdate the *previous* release-planning cycle, not just predate the current one.

```yaml
artifacts.friction_items_drafted: true
```
