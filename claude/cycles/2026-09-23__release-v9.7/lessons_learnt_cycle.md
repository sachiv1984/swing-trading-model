Owner: PMO Lead
Class: Operational Record (Class 3)
Status: Active
Last Updated: 2026-09-26
Cycle: 2026-09-23__release-v9.7

---

## Phase 3

**Phase:** Sprint Execution
**Cycle:** 2026-09-23__release-v9.7
**Section anchor:** `## Phase 3` (stable — cycle_id in field above, not in header)
**Filed:** 2026-09-25
**Reviewed by:** PMO Lead
**Prior cycle checked:** 2026-09-21__release-v9.6 (`lessons_learnt_cycle.md` `## Phase 3`) — 2 friction items filed: (1) `OPERATIONAL_GUIDE.md` §14 self-row drift after an ST-27 commit, action-now, resolved same cycle via ST-30's new audit cadence; (2) `.claude_current_state.json.open_escalations` dispositions and the top-level `completed_items`/`blocked_items` arrays left stale between mid-sprint resolution and sprint-close pre-seal correction — deferred, owner Head of Specs Team, target "next `execution_prompt.md` revision touching STEP 3.1/3.1.D". Checked each for recurrence this cycle: (1) not recurring — no equivalent `OPERATIONAL_GUIDE.md` self-row drift found or corrected this cycle. (2) **Recurrence, still unresolved**: the same defect class recurred — this cycle's own `execution_state.json.process_notes` record a stale top-level `blocked_items`/`delegated_items` entry for EPIC-04/ST-18 (already `done` via DEL-20260924-01) that had to be manually removed during a STEP 4 resume-sync pass, and `.claude_current_state.json.open_escalations` again required an opportunistic in-session correction at the prior cycle's own close (dispositions recorded as `Resolved` after the fact rather than at resolution time — visible in this cycle's own `.claude_current_state.json`). Searched `prompt_change_log.md` by patch-ID and by filename for an `execution_prompt.md` STEP 3.1/3.1.D change addressing this — none found; `execution_prompt.md` remains at v3.79 with no STEP 3.1 addition of this shape. This is the deferred patch's first carry (v9.6 → v9.7, 1 cycle) — below the 2-cycle automatic-escalation threshold, but flagged in Recurrence Notes below per §3.7.

| friction_item | phase | type | classification | action | owner | target_date |
|---------------|-------|------|----------------|--------|-------|-------------|
| `governance_sync.yml`'s `is_story_done()` (via `scripts/governance_sync_lib.sh`) looks up a commit tag's bare `ST-XX` key against `execution_state.json`. For a phased story (`ST-01a`/`ST-01b`/`ST-01c` — the sealed `sprint_backlog.md`'s own RISK-01 resolution mechanism, and `.githooks/commit-msg`'s numeric-tag-only constraint forces every phased commit to still use the bare `[ST-01]` tag), no `stories.ST-01` key exists at all — only the lettered sub-keys do. `is_story_done()` returns `unknown`, and the existing ST-19/BLG-GOV-314 safety fix correctly treats `unknown` as skip-not-close (to avoid a different false-positive-close bug) — net effect, a fully merged phased story's GitHub issue never auto-closes, silently, with no error. Found only because a human asked why issue #1792 was still open after PR #1803 merged; closed manually this session. | Phase 3 | Type C — Dependency Stall: A gate or pre-condition was invisible, ambiguous, or not enforced | defer | Filed as `BLG-GOV-349` (P2) with 3 candidate fix shapes (umbrella-`ST-XX` roll-up check in `is_story_done()`; engine writes a bare `stories.ST-XX` roll-up entry when phasing; or another reconciliation) — decision and implementation deferred to a future cycle touching `scripts/governance_sync_lib.sh`. No prompt or workflow change applied this run. | Head of Specs Team; Infrastructure & Operations Owner | `BLG-GOV-349` — next cycle scoping `scripts/governance_sync_lib.sh` |
| Six separate instances this cycle (EPIC-03, EPIC-05, EPIC-06 ×2, EPIC-04, EPIC-07, EPIC-02, EPIC-01) where a sibling EPIC's PR had already merged on GitHub before this engine's own session caught up — each time corrected via the existing STEP 4 `gh pr view --json mergedAt` resume-sync and the mid-session re-sync check (AUD-2026-08-21-010). The mechanism worked correctly every time (no state was ever lost or misreported downstream), but the sheer frequency — a sync gap on effectively every EPIC merge this cycle — indicates the underlying cause (this engine's sessions and human/CI-driven merges racing each other, especially in a heavily cross-EPIC-conflicted, human-active sprint) is structural to this cycle's shape, not a one-off. | Phase 3 | Type C — Dependency Stall: A gate or pre-condition was invisible, ambiguous, or not enforced | action-now | No prompt change — the existing resume-sync/mid-session-resync controls (LL-v3.9-P3-1, LL-v6.4-P3-01, AUD-2026-08-21-010) already cover this class and caught every instance this cycle with no downstream state corruption. Recorded here as a volume observation, not a new gap; no `execution_prompt.md` change proposed. | Sprint Execution Engine | — |

**Recurrence Notes:**
`.claude_current_state.json.open_escalations` / top-level `completed_items`/`blocked_items` staleness (v9.6 Phase 3 friction item 2, deferred to Head of Specs Team, target "next `execution_prompt.md` revision touching STEP 3.1/3.1.D") recurred this cycle in the same shape (a stale `blocked_items`/`delegated_items` entry for a since-resolved item, corrected only at a later resume-sync rather than at the moment of resolution) — see the prior-cycle-checked note above. This is the deferred patch's first carry (not yet 2 cycles, so not an automatic §3.7 escalation), but is flagged here so Head of Specs Team can weigh prioritising the STEP 3.1/3.1.D change against a second recurrence risk at the next cycle.

## Phase 4

**Phase:** Delivery Verification
**Cycle:** 2026-09-23__release-v9.7
**Section anchor:** `## Phase 4` (stable — cycle_id in field above, not in header)
**Filed:** 2026-09-26
**Reviewed by:** PMO Lead
**Prior cycle checked:** 2026-09-21__release-v9.6 (`lessons_learnt_cycle.md` `## Phase 4`) — 2 friction items filed: (1) `execution_state.json`'s top-level `open_escalations` array staleness (Type A, defer, owner Head of Specs Team, target "next `execution_prompt.md` revision touching STEP 3.1/3.1.D"); (2) `delivery_verification_prompt.md §7`'s `LL-v9.1-P4-01` "or equivalent" evidence clause applied by interpretive extension to open deviations without a prior ruling (Type B, decision, owner Head of Specs Team, target "next `delivery_verification_prompt.md` revision touching §7"). Checked both for recurrence this cycle, re-running the patch-ID/named-file search against current state per `LL-v8.6-P4-01b`/`BLG-GOV-312`/`LL-v9.1-Closure-01`: (2) is **✅ RESOLVED** — confirmed by reading `delivery_verification_prompt.md` directly: §7 now carries an explicit v3.12 addition ("Extended to open deviations with no natural canonical-spec home," `ESC-CLOSE-20260923-02`, Head of Specs Team ruling, 2026-09-23) that answers exactly the question this friction item raised. (1) remains **unresolved, first carry only** — `execution_prompt.md` has no `STEP 3.1`/`STEP 3.1.D` text addressing a top-level `open_escalations`/`completed_items`/`blocked_items` sync fix (confirmed via direct grep of the named file, per `BLG-GOV-312`'s read-the-named-file-directly rule); this cycle's own `execution_state.json.open_escalations` array is empty and internally consistent (no open escalations to go stale), so the symptom did not visibly recur this cycle, but the underlying gap this friction item flagged is still open. Cross-referenced against this cycle's own Phase 3 section above, which independently re-raised the same underlying defect class via `.claude_current_state.json` (a sibling, not identical, structure) — both point at the same missing `execution_prompt.md` STEP 3.1/3.1.D fix.

| friction_item | phase | type | classification | action | owner | target_date |
|---------------|-------|------|----------------|--------|-------|-------------|
| Two of seven `qa_evidence_EPIC-xx.md` files this cycle (`EPIC-04`, `EPIC-07`) record the literal `Signed off by: Director of Quality` — one of the accepted STEP -1.3 Tier 2 literal values — while each file's own `Comments` field discloses the review was actually agent-mediated and is "pending human confirmation." Two sibling files in the same cycle (`EPIC-01`, `EPIC-02`) instead use the fully-qualified, honestly-labelled `"Sprint Execution Engine (agent-mediated, Director of Quality role — §5.3)"` format for the same underlying situation (an agent performing the review, human confirmation still outstanding). The literal-string Tier 2 check in `execution_prompt.md`/`delivery_verification_prompt.md` STEP -1.3 cannot distinguish these two cases — it accepts both — so the inconsistency was only caught by reading each file's prose `Comments`, not by the structural check itself. | Phase 4 | Type B — Semantic Mismatch: The same concept was named or interpreted differently across documents or roles | defer | Recorded as a compliance finding in `verification_report.md §3` (not treated as a Tier 2 hard-gate failure this cycle — the literal string is on the accepted list). Recommend `execution_prompt.md §3.2.A`'s QA evidence sign-off protocol be tightened so that whenever the actual reviewer is an agent rather than the literal human role-holder, the fully-qualified agent-mediated format is mandatory (not merely the sibling EPICs' convention) — closing the gap between what the literal Tier 2 string check can verify and what the file's own prose actually states. | Head of Specs Team | Next `execution_prompt.md` revision touching §3.2.A / QA evidence sign-off protocol |

**Recurrence Notes:**
Gate sequencing proceeded cleanly: the Pre-seal gate (LL-v2.4-DV-01) held as designed — `verification_report.md` was fully assembled with a proposed `Verified_with_deviations` status and blank §9 dates, and STEP 8.5 onward did not proceed until the user was explicitly asked and gave direction for agent-mediated Director of Quality and Product Owner sign-off (2026-09-26), consistent with the governing invariant that this engine does not self-certify without that direction. No deviation severity calls were contested this cycle (0 P0, 0 P1, 0 P2; 2 P4-tier deviations, both routine, both already disclosed with confirmed backlog items and canonical-spec Known Deviations entries before this run began). No test scenario coverage gaps — EPIC-05/EPIC-06 correctly short-circuited as `not_applicable`. The one new friction item this run (EPIC-04/EPIC-07 signer-label ambiguity) is a first occurrence, not a recurrence of either v9.6 Phase 4 item.
