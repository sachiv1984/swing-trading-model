Owner: Infrastructure & Operations Documentation Owner
Status: Operational Record
Deployment Version: N/A
Report Date: 2026-07-27
Environment: Governance
Generated By: Claude Code (Release Planning Engine)
Filed: 2026-07-27

# Run Manifest — Release Planning — v7.9

Cycle: 2026-07-27__release-v7.9
Invocation: `plan release --version "v7.9"` (user-issued as "plan release v7.9. ensure you use the full capacity" — capacity instruction interpreted as an explicit direction to maximise sprint scope up to the top of the confirmed capacity band, per the `2026-07-21__release-v7.7` precedent of the same instruction)
Mode: standard (not specified in invocation; standard applied per this engine's default posture — proceed with explicit assumptions/flags, still halt on hard gates)
Issues mode: none (not specified)
Auto-escalate: true (default)

---

## STEP -1 Preflight

### -1.1 Required files present
Confirmed present: `claude/charter/team_charter.md`, `claude/charter/document_lifecycle_guide.md`, `claude/strategy/strategy_rules.md`, `claude/roadmap/current_roadmap.md`, `claude/backlog/backlog.md`. No issue-template prerequisite check needed (`--issues` not `gh`/`import`).

### -1.2 Release exists on roadmap
`current_roadmap.md` §1 records `Next planned release: [TBD]` — no formal `## v7.9` section exists. Checked the most recent rebalance cycle (`2026-07-27__scheduled`) for a documented STEP 8.1 Option (b) decision: found — "STEP 8.1 fired (condition 1a — Now horizon fully empty) — Option (b): defer, no newly-ready anchor content this cycle." Per STEP -1.2's own rule, a documented Option (b) decision from the prior rebalance is equivalent to a formal planned release section for this gate. **§-1.2 cleared via STEP 8.1 Option (b) decision from `2026-07-27__scheduled`.**

### -1.3/-1.4 Roles and write test
All 9 required agent role files confirmed present under `claude/agents/`. Write test performed at `claude/cycles/2026-07-27__release-v7.9/.write_test` — created and removed successfully.

### -1.5 Prior cycle lessons learnt closure check (Advisory)
`prior_cycle` = `2026-07-24__release-v7.8`. `lessons_learnt_closure.md` exists. Action-now items this cycle: 2 (release_planning_prompt.md STEP 9 status-value fix v2.42→v2.43; post_ship_closure.md STEP 6 v2.19→v2.20). Both confirmed present in `claude/system/prompt_change_log.md` (rows dated 2026-07-27). **No advisory gap.**

### -1.6 Post-ship precondition (Hard Gate)
`.claude_current_state.json`: `post_ship_complete = true`, `next_cycle_unblocked = true`. **PASS.**

### -1.7 Prompt change log integrity (Advisory)
Spot-checked current versions of all Class 6 prompts referenced this session (`release_planning_prompt.md` v2.44, `shared_standards.md` v3.19, `roadmap_prompt.md` v9.6, `idea_intake_prompt.md` v2.8) against `prompt_change_log.md` — all four have a matching row at their current version. **No advisory gap.**

### -1.8 Amendment in progress guard (Hard Gate)
`.claude_current_state.json.status` = `Closed` (not `Amendment_In_Progress`). **PASS.**

### -1.9 Stale backlog lock preflight (Advisory)
`claude/backlog/.lock` does not exist. **No action needed.**

### Data-consistency note (flagged, not a gate) — `prior_cycle` field staleness

`.claude_current_state.json.prior_cycle` read `2026-07-21__release-v7.7` at session start, even though `active_cycle` was `2026-07-24__release-v7.8` (i.e. it had not been advanced when v7.8 became active) — this field does not appear in any engine's documented write scope for the `plan release` → `post-ship closure` transition. STEP -1.5's lessons-learnt-closure check was performed against `2026-07-24__release-v7.8` (the cycle actually immediately prior to this one) rather than the literal stale field value, since that is clearly the intended target. This field's ownership is out of this engine's write scope, so it was not corrected here — flagged for Head of Specs Team as a second, related "field ownership never assigned" gap alongside the `next_release` pattern already fixed at v7.8 (OA-1).

### Data-consistency note (flagged, not a gate)
The 2026-07-27__scheduled rebalance's recorded outcome text (`current_roadmap.md` §1 / `.claude_current_state.json.last_rebalance_outcome`) names `BLG-FE-128` as one of two advisory pull-forward candidates for the next `plan release`. `BLG-FE-128` was already shipped as v7.8 EPIC-01 and is archived (`backlog_archive.md`, confirmed by direct read) — it cannot be a valid v7.9 candidate. This appears to be a stale/erroneous reference carried in that rebalance's own summary text, not a data state this engine can silently correct (out of write scope). Flagged here for the user/Head of Specs Team's awareness; not treated as a scope input. `BLG-FEAT-87` (the other named candidate) is valid and has been included in scope below.

---

## STEP 0 — Carry-forward and scoring loads

**Carry-Forward Advisory (ST-15):** Most recently completed cycle = `2026-07-24__release-v7.8` (`post_ship_complete = true`). `lessons_learnt_closure.md` §Carry-Forward has 3 items (execution_state.json cross-EPIC conflict recurrence; test-execution-over-trust-in-sign-off; STEP 6 gap-count verification). All three are Sprint Execution / Post-Ship Closure scoped, not Release Planning — surfaced here as advisory, no action required by this engine.

**scored_initiatives.md load (ST-14):** File exists. No rows match any of this cycle's 15 candidate items (all are backlog-driven items, not roadmap initiatives) — 0 matching items with effort band. STEP 4.5 will use inline (Tier 3) estimates for all EPICs.

---

## Scope Selection Rationale (STEP 1/2 basis)

No formal `## v7.9` roadmap section exists (scoped via the STEP -1.2 Option (b) equivalence, same backlog-driven pattern as v7.8). Candidate pool considered:

**P0/P1 items screened:**
- `BLG-FEAT-66` (P1, ungated, S) — **included**.
- `BLG-FEAT-67` (P1, ungated, M) — **included**.
- `BLG-FEAT-56` (P1, gate: "AI adoption window clears ~2026-07-25 AND existing AI touchpoints show established, validated usage patterns") — date component has elapsed, but no evidence available this session to confirm the "established, validated usage patterns" sub-condition. **Excluded** — gate not confirmed cleared. Recorded as a Gate-Condition Proximity note (§1.4 below), not silently dropped.
- `BLG-FEAT-73`, `BLG-FEAT-74` (P1, gated: SI-02 NOT MET / §13 pre-clearance not run) — **excluded**, consistent with the already-executed PO perennial-return disposition (`manage roadmap`, 2026-07-27: removed from Now horizon, Option (b) — park until gate clears). No re-litigation needed; STEP 1.4a Perennial-Return Check is satisfied by this prior disposition.
- `BLG-FE-43`, `BLG-SPEC-35` (P1, gated on other items' "sprint planning imminent" — neither SI-05 nor PO-02 is imminent this cycle) — **excluded**.

**Capacity-fill pool (P2, then P3, all ungated, all fresh from `IW-20260727-01`):** 9 P2 items and 4 P3 items selected (of 9 P2 / 12 P3 candidates available) to reach full confirmed capacity per explicit user instruction. Selection favoured P2 over P3 exhaustively, then added the smallest set of P3 items needed to reach the top of the capacity band. See `release_plan.md ## Capacity Check` for the full effort table.

**Total scope: 15 items / 15 EPICs, ~26.5 days midpoint against a confirmed ~24–28 day capacity band (~95–110% utilisation depending on which end of the band is used as the denominator) — intentionally at the top of the band per the user's explicit "use the full capacity" instruction, consistent with the `2026-07-21__release-v7.7` precedent for the same instruction type.**

---

## STEP 1.4a Perennial-Return Check

`BLG-FEAT-73`/`BLG-FEAT-74` returned/excluded from `stage4_backlog_slice.md` at the prior two cycles (v7.7 named-but-excluded at commit; v7.8 not named at all). PO active disposition already recorded this cycle (2026-07-27, `manage roadmap`): Option (b) — remove from horizon, park until gate permanently cleared. No further action required here; not silently re-entered.

## STEP 1.4b Within-Sprint Date Gate Classification

No candidate item in this release's scope carries a gate condition with an earliest-clearing date falling within the planned sprint execution window. N/A this cycle.

## STEP 1.4 Gate-Condition Proximity Scan

| Item | Gate condition | Current trajectory | Projected clear date |
|------|----------------|---------------------|----------------------|
| BLG-FEAT-56 | AI adoption window ~2026-07-25 AND validated AI-touchpoint usage | Date sub-condition elapsed (today: 2026-07-27); usage-validation sub-condition unconfirmable this session (no analytics/usage-data source available in this checkout) | Unresolved — Product Owner to confirm usage-validation sub-condition before this item may enter a future release |
| BLG-FEAT-73 | SI-02 (BLG-GOV-107) — trajectory unknown | NOT MET, 9th+ consecutive identical reading (0/11 linked trade plans) | trajectory unknown |
| BLG-FEAT-74 | §13 determinism pre-clearance not yet run | trajectory unknown | trajectory unknown |

**Arc 4 data density sub-check:** No production API credentials available in this checkout (consistent with every recent session this month, per `current_roadmap.md`'s own SI-02 fallback notes). PO-02/PO-04/SI-02 gate-clearing projections: data not available — Product Owner to surface at readiness review, citing the existing structured fields in `current_roadmap.md` §5 (unchanged since 2026-07-17).

## STEP 1.1 Backlog Age Advisory

The single spec/documentation-debt-type item in this release's candidate slice (`BLG-SPEC-105`) was filed this same rebalance cycle (`2026-07-27__scheduled`) — 0 prior cycles without story assignment. **No aged spec-debt advisory triggered.**

## STEP 1.2 Provisional-Target Advisory

All 15 in-scope items carry `Provisional-Target: TBD` (none pre-targeted at v7.9, since no roadmap section existed). 0 items with `Provisional-Target: v7.9` (expected — this plan is what will set that value at STEP 9 sync / backlog slice write). 15 of 15 in-scope items have no matching pre-signal; scope selection authority rests with this routine per STEP 1.2's own rule.

## STEP 1.3 Design-Gate Language Scan

No candidate item's body contains explicit "design decision required" / "pending design" / "requires UX decision" language. Design-gate classification is instead driven by STEP 4.1's observable-UI-AC rule (see `release_plan.md`).
